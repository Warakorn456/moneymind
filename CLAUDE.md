# MoneyMind — CLAUDE.md

แอปจัดการการเงินส่วนตัวภาษาไทย เขียนเป็น Single HTML File ไม่มี build process ไม่มี framework ไม่มี dependencies ที่ต้อง install

## กฎการทำงาน (ทำทุกครั้งโดยไม่ต้องรอให้สั่ง)

- **หลังแก้ไข/deploy ทุกครั้ง:** อัปเดต CLAUDE.md และ memory files ที่เกี่ยวข้องทันที ไม่ต้องรอให้สั่ง
- **Memory path:** `C:\Users\warakorn\.claude\projects\D--wabbest\memory\`
- อัปเดตเฉพาะส่วนที่เปลี่ยนจริง — ไม่เขียนซ้ำสิ่งที่มีอยู่แล้ว

## สถาปัตยกรรม

**ไฟล์เดียว:** `index.html` (~17,520 บรรทัด — เพิ่มขึ้นเรื่อยๆ จาก auto-commit ทุกครั้งที่แก้ ให้ `wc -l index.html` เช็คของจริงก่อนอ้างอิงเลขบรรทัดเสมอ) — HTML + CSS + Vanilla JavaScript ทั้งหมดอยู่ในไฟล์เดียว

```
index.html
├── <head>         — บรรทัด 3–967, CDN links (Chart.js, Firebase, Font Awesome, Sarabun)
├── <style>        — CSS ทั้งหมด (บรรทัด 9–966, 2 บล็อก)
├── <body>         — บรรทัด 968–2477, HTML structure, sidebar nav, หน้าทุกหน้า
└── <script>       — JavaScript ทั้งหมด (บรรทัด 2478–14739) — `<script>` เดียว ห้ามเปิดใหม่
```

**หา section ในไฟล์:** โค้ดคั่นแทบทุก section ด้วยคอมเมนต์ `══════`  — `grep -n "══════" index.html -A1` ได้ผังทั้งไฟล์ทันที เชื่อถือได้กว่าเลขบรรทัดที่จำไว้ในเอกสารนี้ (ไฟล์ถูก auto-commit เปลี่ยนเกือบทุกวัน)

ไม่มี `package.json`, `node_modules`, build tool, หรือ test suite — เปิดไฟล์ใน browser ได้เลย

## External Dependencies (CDN เท่านั้น)

| Library | Version | ใช้ทำอะไร |
|---------|---------|-----------|
| Chart.js | 4.4.0 | กราฟ Pie, Bar, Area |
| Firebase App + Firestore | 9.23.0 | Cloud sync ข้ามอุปกรณ์ |
| Font Awesome | 6.5.1 | Icons |
| Google Fonts (Sarabun) | — | ฟอนต์ภาษาไทย |
| Google Gemini API | v1beta | AI สแกนใบเสร็จ, AI Chat |
| CoinGecko API | v3 | ราคาคริปโตเรียลไทม์ |

## Data Model

Global object `DB` เก็บข้อมูลทั้งหมดในหน่วยความจำ:

```js
DB = {
  transactions: [],     // {id, date, type, amount, category, description, tags, isFixed, bankId, ...}
  savings: [],          // {id, name, target, current, ...}
  debts: [],            // {id, name, total, remaining, dueDate, pay, ...}
  investments: [],      // {id, name, type, value, ...}
  subscriptions: [],    // {id, name, price, cycle, nextDate, cat, color, active}
  calNotes: {},         // {"YYYY-MM-DD": [{id, text, color}]}
  forecastCfg: {},      // {startBal, dailyExp, scenario, customEvents}
  wishlist: [],         // (legacy — ไม่แสดง UI แล้ว ข้อมูลยังอยู่ใน DB)
  travel: { places:[] },// {name, lat, lng, visited, label, fromTx} — หมุดบนลูกโลก 3D หน้า travel
  settings: {
    apiKey: '',         // Google Gemini API Key
    geminiModel: '',
    customIncCats: [],
    customExpCats: [],
    fixedExpCats: [],   // categories ที่ถือว่าเป็นค่าใช้จ่ายคงที่
    dashOrder: [],      // ลำดับ section บน Dashboard
    navOrder: [],       // ลำดับ nav items ใน sidebar
  },
  tax: {},
  plan: { monthlyIncome:0, otherAssets:0, items:[], extraAssets:[], extraDebts:[] },
  profile: {},
  retire: { retireAge:60, lifeAge:85, monthlyExpense:30000, inflation:3, returnRate:7,
            postReturnRate:4, sso:750, pvd:0, portOverride:null, curAgeOverride:null },
  _savedAt: 0,          // timestamp สำหรับ conflict resolution กับ Firestore
}
```

## Storage Layer

| Storage | Key | ใช้เก็บ |
|---------|-----|---------|
| `localStorage` | `mm_users` | `[{username, salt, hash}]` — user list |
| `localStorage` | `mm_session` | `{username, ts}` — session ปัจจุบัน |
| `localStorage` | `mm_data_<username>` | DB object ของแต่ละ user |
| Firebase Firestore | `userdata/<username>` | Cloud backup (db = JSON ทั้งก้อน) — sync 2 ทาง |
| Firebase Firestore | `members/<username>` | mapping `{username, email, uid, lastSeen}` (ไม่มีข้อมูลการเงิน) |

ลำดับ sync: localStorage → Firestore (ถ้า login) → conflict resolution ด้วย `_savedAt`

### Firestore Security Rules (locked สนิท 2026-06-22)
ไฟล์ `firestore.rules` (deploy แล้วผ่าน Firebase Rules REST API):
- **`userdata/{userId}` ทุกคน (รวม warakorn)** = อ่าน/เขียนได้เฉพาะ Firebase user ที่ `ownsAccount()` คือ `members/<u>.uid == request.auth.uid` — **ไม่มี public แล้ว** (unauth = 403)
- **`members`** = read public (login lookup email→username + admin list), write เฉพาะเจ้าของ/`warakorn`/`unclaimed`
- **`admin/{doc}`** (เพิ่ม 2026-06-26) = `allow read: if ownsAccount('warakorn')` (owner อ่านคนเดียว), `allow write: if false` (client เขียนไม่ได้ — SA บายพาส); เก็บ `admin/summary` usage รวมหลังบ้านที่ `admin_stats.py` เขียนผ่าน SA
- legacy ที่ยังไม่มี uid (เช่น yok07) → `unclaimed()` ให้ผู้ใช้ที่ **login แล้วเท่านั้น** claim ครั้งแรก (auto บน login ใหม่); unauth claim ไม่ได้
- ทุก login ด้วย username เรียก `ensureAuthForUser()` → สร้าง Firebase Auth session (email จริง หรือ synthetic `<u>@moneymind.local` สำหรับ legacy/owner) + เขียน `members.uid`; **owner branch (decrypt `_ENC`) ก็เรียก `ensureAuthForUser(_ENC.user,'',pass)` แล้ว** (เดิมไม่เรียก → warakorn ไม่มี Firebase session/uid; แก้ 2026-06-22 บรรทัด ~2832) — owner Firebase account = `warakorn@moneymind.local` uid `7oxtLgXJ...`
- **GCP/Telegram/Gmail scripts (~31 ตัว) auth ผ่าน Service Account แล้ว** — ไม่ใช่ unauth REST อีกต่อไป:
  - SA `firestore-rest@moneymind-d97f3` (roles/datastore.user) → access token **บายพาส security rules** (IAM admin); key = `firestore_sa.json` (gitignore, อยู่บน VM `/home/warakornbest6/moneymind/` + laptop `Documents/`)
  - helper กลาง **`mm_firestore.py`**: `token()` (SA key → metadata → gcloud fallback, scope `datastore`) + `auth_header()`; ทุก call site แนบ `headers=mm_firestore.auth_header()` (requests.get/patch + urllib Request) — patch ด้วย `_apply_fs_auth.py` (one-time, ลบทิ้งได้)
  - n8n 3 workflow ที่อ่าน Firestore (budget-alert-01/mm-gemini-monthly-01/mm-notebooklm-01) **deactivate แล้ว** (มี Python equivalent บน VM); ต้อง restart n8n ให้ cron in-memory หลุด
- active ruleset: `ccdd8cc8-f3c6-4e4c-bffc-232582573bbd` (deploy 2026-06-26 — เพิ่ม `admin/{doc}`); ก่อนหน้า `f2d0d3a6-aa21-46eb-82be-d1f1c8d99a10` (2026-06-22 09:44 UTC)
- rollback: ruleset เปิด public ทั้งหมด `0fa4eb71-c312-4c98-a5b7-36af71266e5f` หรือ public-warakorn `1391d585-7167-4265-b0f5-6b8c2df6318a` (2026-06-21)
- redeploy: POST ruleset + PATCH release `cloud.firestore` ที่ `firebaserules.googleapis.com/v1/projects/moneymind-d97f3/...` ด้วย `gcloud auth print-access-token` + header `x-goog-user-project: moneymind-d97f3`
- **เพิ่ม script ใหม่ที่แตะ Firestore:** ต้อง `import mm_firestore` + แนบ `headers=mm_firestore.auth_header()` ทุก get/patch ไม่งั้น 403
- **`reports/{doc}` (เพิ่มใน `firestore.rules` local 2026-07-02, ยังไม่ deploy จริง)** — `allow create: if authed()`, `allow read/update/delete: if false` — เก็บ feedback ผู้ใช้ + client error report (ดู "Security Audit" ด้านล่าง); **ต้อง deploy rule นี้ก่อน** ฟีเจอร์ feedback/error-report ถึงจะใช้งานได้จริง (deploy ตามขั้นตอน redeploy ด้านบน)

## Security Audit & Fixes (2026-07-02)

ตรวจพบตอนเริ่มมีผู้ใช้หลายคน (ไม่ใช่แค่ owner คนเดียวอีกต่อไป) — แก้แล้วในโค้ด ยกเว้นที่ระบุว่า "รอ deploy":

| ปัญหา | สถานะ | รายละเอียด |
|-------|-------|-----------|
| **ตั้งรหัสผ่านใหม่โดยตรงไม่ต้องยืนยันตัวตน** (`doResetPassword`, tab "ตั้งรหัสใหม่โดยตรง") | ✅ **ลบออกแล้ว** | เดิมพิมพ์แค่ username (ไม่ต้องรู้รหัสเดิม/ไม่ต้อง verify email) ก็ตั้งรหัสใหม่ทับ `mm_users` (localStorage) ได้ — เสี่ยงกับเครื่องที่ใช้ร่วมกัน (username เคย login ไว้ก่อน) เหลือแค่ทาง "ส่งอีเมลรีเซ็ต" (Firebase `sendPasswordResetEmail`, ปลอดภัยอยู่แล้ว) ไม่มี email → ให้กด "ติดต่อทีมงาน" (feedback modal) แทน |
| **Telegram Bot Token hardcode ใน client JS** (feedback modal เดิมยิง `api.telegram.org` ตรงด้วย token ในซอร์ส) | ✅ **ย้ายออกจาก client + deploy ครบแล้ว 2026-07-02** (รอแค่ rotate token) | เปลี่ยนเป็นเขียนลง Firestore collection **`reports`** (`_sendReport()`) แทน — ไม่มี token ฝั่ง client อีกเลย. **Deploy แล้วจริง:** (1) `firestore.rules` (`reports/{doc}`: `allow create: if authed()`, อ่าน/แก้/ลบไม่ได้จาก client) — deploy ผ่าน Firebase Rules REST API, ruleset ใหม่ `a6ae7023-37ee-4ca7-85ce-d128262e5612` (2) `report_relay.py` บน VM (`/home/warakornbest6/moneymind/`) — polling ทุก 5 นาที (cron `*/5 * * * *`) ดึง `reports` collection ทั้งหมด (auth ผ่าน SA, บายพาส rules) ส่งต่อ Telegram topic เดิม (thread 907) แล้ว **ลบ doc ทิ้งทันทีที่ส่งสำเร็จ** (ไม่ mark เฉยๆ) (3) เพิ่ม `report_relay.log` เข้า `cron_health.py` REGISTRY (scan-only, `None`) — ทดสอบ end-to-end แล้วผ่าน (เขียน test doc → relay → Telegram → ลบ doc สำเร็จ). **ยังไม่เสร็จ:** rotate token ผ่าน @BotFather ยังไม่ทำ (ต้องทำเองผ่าน Telegram) — token เก่ายังใช้งานได้อยู่ ควร rotate เพราะเคยโชว์ใน public repo มานาน; **ห้ามเอา token ใหม่มาใส่ CLAUDE.md แบบตรงๆ อีก** (เป็นสาเหตุที่ token เก่าหลุดตั้งแต่แรก) — ให้จดแค่ว่า "ดูใน `.env`" |
| **Firestore เอกสาร 1MB ไม่มีการเช็คขนาด** | ✅ แก้แล้ว | `fsSave()` เช็คขนาด JSON ก่อน save เตือน toast ที่ ~900KB (`_checkDbSizeWarn`) + จับ error message ที่บ่งชี้ชนขีดจำกัดแล้วขึ้นข้อความอธิบายแทน error ดิบ |
| **Sync หลายอุปกรณ์ทับกันทั้งก้อน (last-write-wins)** | ✅ แก้แล้ว | เพิ่ม `_mergeDbSnapshot()` merge แบบ union-by-id สำหรับ array หลัก (`transactions`,`savings`,`debts`,`investments`,`subscriptions`,`insurance`,`dcaPlans`) ใน `fsLoad()`/`fsListen()` — item ที่มีแค่ฝั่งเดียว (ยังไม่ทัน sync) ไม่หายอีกต่อไป; item ชนกัน (id ซ้ำ) remote ยังชนะเหมือนเดิม (ยังไม่ใช่ CRDT เต็มรูปแบบ) |
| **Privacy policy ไม่บอกเรื่อง admin เห็นสรุปข้อมูลผู้ใช้ทุกคน** | ✅ แก้แล้ว | เพิ่ม section 4 "การเข้าถึงข้อมูลโดยผู้ดูแลระบบ" ใน `privacy.html` อธิบายว่า admin เห็นสรุป (txCount/net worth/ฟีเจอร์ที่ใช้) ไม่เห็นรายละเอียดรายการ |
| **สมัครสมาชิกไม่มีการกันสแปม** | ✅ แก้บางส่วน | เพิ่ม honeypot field (`#reg-hp`) + เช็คเวลาขั้นต่ำ 1.5 วิ ก่อน submit ใน `tryRegister()` — กันบอทพื้นฐาน ไม่ใช่ CAPTCHA เต็มรูปแบบ (ของจริงต้อง Firebase App Check ซึ่งต้อง config เพิ่มใน Firebase Console) |
| **ไม่มีลิงก์ privacy/terms จากในแอป + ไม่มี Terms of Service** | ✅ แก้แล้ว | เพิ่ม `terms.html` (ใหม่, สไตล์เดียวกับ `privacy.html`, มี disclaimer "ไม่ใช่ที่ปรึกษาการเงิน/ภาษี") + ลิงก์จาก PDPA consent modal และ footer หน้า login เสมอ — **`terms.html` ควรให้คนมีความรู้กฎหมายช่วยรีวิวก่อนถือว่าเป็นทางการ** |
| **ไม่มี error/crash tracking** | ✅ แก้แล้ว | เพิ่ม `window.addEventListener('error'/'unhandledrejection')` ส่งเข้า Firestore `reports` เดียวกับ feedback (`kind:'error'`) มี dedupe กันยิงซ้ำ error เดิมในเซสชันเดียว — **ก็ต้องรอ deploy rule + relay script เหมือนกันถึงจะเห็นจริง**

**สถานะ token rotation: ✅ เสร็จสมบูรณ์แล้ว (2026-07-02)**
- Revoke token เก่า + ได้ token ใหม่ผ่าน @BotFather (`/mybots` → `moneymind_alert_bot` → API Token → Revoke current token) แล้ว
- อัปเดต `MM_TG_TOKEN` ใน `.env` ครบ 2 ที่: `C:\Users\warakorn\Documents\.env` (โน้ตบุ๊ก) + `/home/warakornbest6/moneymind/.env` (VM)
- `sudo systemctl restart moneymind-bot` แล้ว — ทดสอบ `getMe` คืน `ok:true` username `moneymind_alert_bot` ยืนยัน token ใหม่ทำงานจริง
- **🐛 พลาด restart `gmail-bank-alert` ด้วย! (เจอ+แก้ 2026-07-03)** — service นี้เป็น long-running systemd (ไม่ใช่ cron) โหลด `mm_secrets.TG_TOKEN` เข้าหน่วยความจำครั้งเดียวตอน start ไม่ reload `.env` เองระหว่างรัน; ตอน rotate token 2 ก.ค. restart แค่ `moneymind-bot` ตัวเดียว ลืม `gmail-bank-alert` (รันมาตั้งแต่ 27 มิ.ย. ไม่เคย restart) → ถือ token เก่าที่ revoke ไปแล้วค้างในหน่วยความจำ → ส่ง Telegram แจ้งเตือนธุรกรรมธนาคารไม่ผ่านทั้งหมด **แบบเงียบๆ** (log แค่ `tg=False` ไม่มี alert เตือนใคร เจอเพราะ user บ่นว่าโอนเงินแล้วไม่เข้า Telegram) — restart แล้ว ยืนยัน token ใหม่ทำงานจริง (`getMe` ผ่านจาก VM). **กฎทั่วไป: ทุกครั้งที่ rotate `MM_TG_TOKEN` ต้อง restart systemd service ที่ long-running ทุกตัวที่ใช้ token นี้ ไม่ใช่แค่ตัวเดียว** — ตอนนี้มี 3 ตัว: `moneymind-bot`, `gmail-bank-alert`, `moneymind-ai-proxy` (ai_proxy ไม่ได้ใช้ TG_TOKEN โดยตรงแต่ import mm_secrets เหมือนกัน เผื่อไว้). cron scripts (~30 ตัว) ไม่กระทบเพราะรันจบแล้วจบเลย ได้ token ใหม่ทุก run อยู่แล้ว
- อัปเดต token ในไฟล์ dormant ที่เจอด้วย: `C:\Users\warakorn\Documents\stock_bot_remote.py`, `C:\Users\warakorn\Documents\vm_setup.sh` (ไม่ได้แตะ `refactor_secrets.py` เพราะเป็น search-pattern ของ tool migrate ครั้งเดียว ไม่ใช่ credential ที่ใช้จริง)
- **🐛 บั๊กเดิมเกิดซ้ำอีกรอบ (เจอ+แก้ 2026-08-07)** — user แจ้งว่า "อีเมล SCB เข้าว่าใช้จ่ายอะไรบ้าง แต่เทเลแกรมไม่ยอมดึงค่ามาเพิ่มรายรับรายจ่าย" — เช็ค `gmail_bank_alert.log` บน VM พบ `[19fd9a283f2ca16d] scb expense ฿110.00 — tg=False` (06:35 น. 7 ส.ค.) คือสาเหตุตรงๆ: parse อีเมลได้ยอดถูกต้อง แต่ส่ง Telegram ไม่สำเร็จ เลยไม่มีข้อความให้กดปุ่มเลือกหมวด → ไม่เข้ารายรับ-รายจ่าย. **root cause เดียวกับ 2 ก.ค. เป๊ะ:** `.env` บน VM ถูกแก้ล่าสุด **2026-08-04 04:15 UTC** (ไม่รู้แน่ชัดว่าใคร/สคริปต์ไหนแก้ — เดาว่ามี token rotation หรือ config เปลี่ยนแต่ไม่มีบันทึกไว้) — `moneymind-bot` restart ตาม 1 นาทีให้หลัง (04:16 UTC, ได้ token ใหม่) แต่ **`gmail-bank-alert` ไม่ได้ถูก restart เลยตั้งแต่ 3 ก.ค.** (รันยาวนานกว่า 1 เดือน) ถือ token เก่าค้างในหน่วยความจำต่อมาอีก 1 เดือนกว่า จนกว่าจะมี transaction ที่ต้องส่งจริง (7 ส.ค.) ถึงโผล่ error ให้เห็น. ยืนยันด้วยการยิง POST ทดสอบจาก VM ด้วย token สดจาก `.env` ปัจจุบัน → สำเร็จ 200 OK ทันที (พิสูจน์ว่า token ใน `.env` ใช้งานได้ดี ปัญหาอยู่ที่ process เก่าค้าง token ผิดในหน่วยความจำเท่านั้น). **แก้:** `sudo systemctl restart gmail-bank-alert` (เช็คแล้ว `moneymind-ai-proxy` restart ไปแล้วตอน 08:02 UTC วันเดียวกัน หลัง `.env` เปลี่ยน ไม่ติดปัญหานี้) + ลบ message id `19fd9a283f2ca16d` ออกจาก `gmail_seen.json` ให้ระบบ re-detect แล้วส่ง Telegram ใหม่รอบถัดไป (pending record เดิมที่เคย save ไว้ตอน fail กลายเป็น orphan เข้าไม่ถึงจากปุ่มไหนแล้ว ปล่อยทิ้งไว้เฉยๆ ไม่กระทบอะไร). **บทเรียนเพิ่มจากรอบนี้:** กฎเดิม "rotate token ต้อง restart ทั้ง 3 service" ยังไม่พอ เพราะรอบนี้ไม่มีใครรู้ตัวว่า `.env`ถูกแก้เมื่อไหร่ (ไม่มี log/แจ้งเตือนตอนแก้ `.env` เอง) กว่าจะรู้ตัวคือ error โผล่จริงในการใช้งาน 3 วันหลังจากนั้น — **ควรพิจารณาเพิ่ม watchdog เช็คอายุ process เทียบกับ mtime ของ `.env`** (ถ้า service start ก่อน `.env` ถูกแก้ล่าสุด = เสี่ยงถือ config เก่าค้าง) เข้า `health_monitor.py`/`cron_health.py` ในอนาคต แทนที่จะพึ่งจำเอาไว้ทำ manual ทุกครั้ง — ยังไม่ได้ implement รอบนี้ เป็นแค่ข้อสังเกตไว้ก่อน
- **token ใหม่จดไว้แค่ใน `.env` เท่านั้น ไม่จดใน CLAUDE.md ตรงๆ** (บรรทัดด้านบนในตาราง Infrastructure เปลี่ยนเป็น "ดูใน `.env`" แล้ว) — ป้องกันปัญหาเดิมซ้ำ

**บทเรียนจากรอบนี้ (2026-07-02):** อย่า `sed -i` แก้ text ภาษาไทยบนไฟล์ remote ผ่าน `gcloud compute ssh --command="..."` — การ escape ผ่านหลายชั้น (bash local → gcloud/plink → bash remote) ทำให้ UTF-8 byte เพี้ยน (`cron_health.py` เจอ `UnicodeDecodeError` หลัง sed ครั้งหนึ่ง) **วิธีที่ปลอดภัย:** แก้ไฟล์ local (source of truth ใน `Documents/`) ด้วย Edit tool ปกติ เช็ค syntax ผ่านก่อน แล้วค่อย `gcloud compute scp` ทับของเดิมทั้งไฟล์ — ไม่ใช้ sed ผ่าน SSH กับไฟล์ที่มีภาษาไทยเด็ดขาด

## Pages & Navigation

`const PAGE_TITLE` อยู่บรรทัด ~3592 — รวมทุก page key ไว้ที่เดียว  
Navigation ผ่าน `nav(page)` (~3595) — routing อยู่ใน `renderPage(p)` บรรทัด ~3611:

| Page key | หน้า | render function |
|----------|------|----------------|
| `dashboard` | Dashboard | `renderDash()` |
| `transactions` | รายรับ-รายจ่าย | `renderTx()` |
| `ot` | คำนวณ OT | `renderOT()` |
| `loan` | คำนวณสินเชื่อ | `renderLoan()` |
| `subscriptions` | Subscriptions | `renderSubscriptions()` |
| `calendar` | ปฏิทิน | `renderCalendarPage()` |
| `travel` | ท่องเที่ยว | `renderTravel()` |
| `savings` | เงินออม | — |
| `banks` | ธนาคาร | — |
| `debts` | หนี้สิน | — |
| `investments` | พอร์ตลงทุน | `renderInv()` |
| `tax` | ภาษีเงินได้ | `renderTax()` |
| `balance` | งบดุล & แผน | — |
| `retire` | แผนเกษียณ | `renderRetire()` |
| `profile` | ประวัติส่วนตัว | — |
| `settings` | ตั้งค่า | `renderSettings()` |
| `admin` | Admin Dashboard | `renderAdmin()` |
| `aiusage` | AI Usage / ค่า Token | `renderAiUsage()` (nav ล่างใกล้ Settings, icon fa-robot) |
| `forecast` | พยากรณ์กระแสเงินสด | `renderForecast()` (ไม่มีใน nav แต่ยังใช้งานได้) |

> **ถอดออกแล้ว 2026-06-26:** เมนู/หน้า **AI Inbox (`inbox`)**, **รายการซ้ำ (`recurring`)**, **จัดลำดับ (`priority`)** ถูกลบทั้ง nav + page HTML + render/CRUD functions + CSS + routing + PAGE_TITLE. เหลือเฉพาะ `applyRecurringTx()` (auto-apply รายการซ้ำเดิม — ปฏิทิน/Dashboard ยังอ่าน `DB.recurringTx`); ข้อมูล `DB.priorities`/inbox tags ยังอยู่ใน DB แต่ไม่มี UI แล้ว

**Sidebar nav** — items มี `data-nav="<key>"` และ `draggable="true"` รองรับ drag-and-drop reorder  
ลำดับถูกบันทึกใน `DB.settings.navOrder` — ดูโค้ดที่ `// NAV SORTABLE` บรรทัด ~14639 (ท้ายไฟล์ ก่อนปิด `</script>`)

## UI/CSS Conventions

- **CSS Variables:** `--primary`, `--primary-l`, `--surface`, `--surface2`, `--border`, `--text`, `--muted`, `--red`
- **Dark mode** เป็นค่าเริ่มต้น, toggle ด้วย class `body.light-mode`
- **Theme toggle transition — ไล่สีพร้อมกันทั้งจอ (เพิ่ม 2026-08-10)** — เดิมสลับ dark/light instant ทันที user ขอ "ลูกเล่น" ไล่สีค่อยๆ เปลี่ยน + ไอคอนพระอาทิตย์/จันทร์ค่อยๆ เปลี่ยน แทน swap ทันที (ลองวงกลมขยายจากปุ่มก่อน user feedback "เปลี่ยนสีพร้อมกันทั้งพื้นหลัง" → เปลี่ยนเป็น cross-fade พร้อมกันทั้งจอแทน). **แก้ด้วย View Transitions API** (`document.startViewTransition()`) แทนการไล่เพิ่ม `transition` ให้ CSS variable ทีละตัว (เหตุผล: ธีม "Liquid Glass" ส่วนใหญ่ hardcode `background:linear-gradient(...)` แยกกันคนละ rule ระหว่าง dark/light — gradient-to-gradient ไม่ interpolate ด้วย CSS transition ปกติได้เสถียร ถ้าจะไล่สีทุกองค์ประกอบต้องแก้ CSS หลายร้อยจุด) — View Transition snapshot หน้าเก่า/ใหม่แล้ว cross-fade ให้เอง (ปล่อยให้ browser cross-fade ตาม default animation ของมันเอง ไม่เขียน keyframe เอง) โดยไม่ต้องแตะ CSS การ์ด/component เดิมเลย. `toggleTheme()` (~บรรทัด 10206) ห่อ logic เดิม (`apply()`) ด้วย `document.startViewTransition(apply)`; CSS ใกล้ `.theme-btn` (~บรรทัด 680) แค่คุม `animation-duration:.5s` บน `::view-transition-old/new(root)` (ไม่ตั้ง `animation:none` เหมือนรอบแรก — ปล่อยให้ browser cross-fade ทั้งจอพร้อมกันตาม default). **Fallback:** ไม่รองรับ browser (`!document.startViewTransition`) หรือ `prefers-reduced-motion` → เรียก `apply()` ตรงๆ ทันที (พฤติกรรมเดิม). **ไอคอน sun/moon:** เปลี่ยนจาก `<i id="theme-icon">` ตัวเดียว swap className → **2 icon ซ้อนกัน absolute** (`.theme-icon-sun`/`.theme-icon-moon`) cross-fade+rotate ผ่าน CSS selector `body.light-mode .theme-icon-*` (เอา pattern มาจาก `landing.html` ที่มี `.icon-sun`/`.icon-moon` แบบเดียวกันอยู่แล้ว) — JS ไม่ต้องสั่ง `icon.className=...` อีกต่อไป (ลบออกจากทั้ง `toggleTheme()`/`applyTheme()`). **🐛 เจอ+แก้ระหว่างทดสอบ — สำคัญ ห้ามลืมถ้าใช้ View Transitions API ที่จุดอื่นในอนาคต:** `document.startViewTransition()` คืน object ที่มี **3 promise อิสระต่อกัน** (`.ready`, `.updateCallbackDone`, `.finished`) — รอบแรกจับ error แค่ `.finished` (ผ่าน `.finally().catch()`) แต่ `.ready` ที่ไม่มีใคร reference/handle เลย reject เองได้เงียบๆ (`InvalidStateError: Transition was aborted because of invalid state` — เกิดได้แม้ toggle แค่ครั้งเดียวโดดๆ ไม่ต้องกดรัว ในบาง environment/browser state) กลายเป็น **unhandled promise rejection จริงที่ window level** ยืนยันด้วย `window.addEventListener('unhandledrejection',...)` จับได้จริง — **อันตรายเพราะแอปมี global `unhandledrejection` handler ส่ง error เข้า Firestore `reports`→Telegram** จะสแปม error ทุกครั้งที่ toggle ถ้าไม่แก้. **แก้จริง:** ต้อง `.catch(()=>{})` ทั้ง `trans.ready` และ `trans.updateCallbackDone` แยกกัน (ไม่ใช่แค่ `.finished`) + ใช้ `trans.finished.then(onDone,onDone)` (both callbacks) แทน `.finally().catch()` ให้ชัดเจนว่า handle ครบทุก branch. มี module-level flag `_themeTransitionActive` กัน re-entrant call ระหว่างกดรัวด้วย (ทำ `apply()` ตรงๆ แทนเปิด transition ซ้อนถ้ามีอันเก่ายังไม่จบ). ทดสอบด้วย `unhandledrejection` listener จริงยืนยันว่า 0 rejection ทั้ง single toggle และ rapid 5x toggle ติดกัน
- **Premium polish** (ท้าย style block ~บรรทัด 934, additive) — bg radial glow, การ์ด gradient+เงา+hover lift, sidebar/topbar glass blur, nav active pill, btn-primary gradient, input focus ring; scoped `body:not(.light-mode)` เพื่อไม่กระทบ light mode
- **ปุ่ม (.btn-primary/.btn-ghost) อัปเกรดให้กระจกลึกขึ้น (เพิ่ม 2026-08-10)** — user ส่ง CodePen อ้างอิงสไตล์ "Apple Liquid Glass" segmented nav (ดึงค่าจริงผ่าน `getComputedStyle`/CSSOM เพราะดึง raw HTML source โดน content-filter ของ browser tool บล็อก) มาเทียบกับปุ่มเดิมในแอป (~บรรทัด 1068-1078, ~1145) แล้วยกระดับ: `.btn-primary`/`.btn-ghost` เพิ่มเงาหลายชั้น (ambient shadow ซ้อน inset highlight บน**และ**ล่าง — เดิมมีแค่ inset บนชั้นเดียว), `.btn-ghost` เพิ่ม `saturate(180%)` เข้า backdrop-filter + `:hover` state ของตัวเองแยกจาก base class เดิม (กันตกกลับไปดู flat ตอน hover), เพิ่ม spring transition (`cubic-bezier(.34,1.2,.64,1)`) แทน `.2s` เรียบๆ, เพิ่ม `:active{transform:scale(.96)}` ให้มี tactile feedback ตอนกด (pattern เดียวกับ `.pin-btn:active` ที่มีอยู่แล้วในไฟล์). **ไม่แตะ** `.btn-danger`/`.btn-sm` (ตั้งใจให้ต่างจากปุ่มทั่วไปชัดเจน) และไม่สร้าง segmented-control component ใหม่ (user เลือกแค่เอาสไตล์ผิวมาใช้กับปุ่มทั่วไปทั้งแอป ไม่ใช่สร้าง tab bar ใหม่). Light-mode `.btn-ghost` override (~1145) อัปเกรดคู่กัน; `.btn-primary` ไม่ต้องมี light-mode override แยก (gradient ม่วงเดิมอ่านได้ทั้ง 2 ธีมอยู่แล้ว). **เพิ่ม `.btn-ghost` เข้า mobile blur-cap list** (`@media(max-width:700px)` ~บรรทัด 547 ที่บังคับ `blur(6px)!important` กันปัญหาสกอลล์ฝืดที่เคยเจอมาก่อน — ดู gotcha ด้านล่าง) ป้องกันบั๊กเดิมกลับมาซ้ำตอนเพิ่ม `saturate()` เข้าไป. ทดสอบสด production แล้วทั้ง dark/light mode ไม่มี console error
- **🐛 Light mode contrast bug (เจอ+แก้ 2026-07-06)** — `--muted` (ตัวแปรที่ `var(--muted)` อ้างถึง **462 จุดทั่วไฟล์** สำหรับ label/subtitle/timestamp รอง) ใน `body.light-mode` เดิมตั้งเป็น `#64748b` เหมือนกับที่ตั้งใจให้เข้ากับพื้นหลังมืด ( contrast ~4.3:1 บนพื้นขาว = อ่านยาก โดยเฉพาะ font เล็ก 10-11px) ทำให้ตัวหนังสือ "รอง" เกือบทั้งแอปดูจางกลืนพื้นหลังตอนสลับเป็นโหมดสว่าง. **แก้แล้ว:** เปลี่ยนเป็น `#475569` (เข้มขึ้น contrast ~7:1). จุดอื่นที่ hardcode `#64748b`/`#94a3b8`/`rgba(255,255,255,.0X-.6)` ตรงๆ แทนที่จะใช้ `var(--muted)` (bypass การแก้ตัวแปรเดียว) ก็แก้ทีละจุดด้วย: `.nav-item`/`.icon-btn` light-mode override, chip เดือนที่ยังไม่จ่ายในหน้าหนี้สิน (`toggleDebtMonth` ~บรรทัด 5333, เปลี่ยนเป็น `var(--muted)`+`var(--border)`), sync badge (`setSyncStatus` idle state). **Chart.js ticks/gridlines** (10 จุดทั่วไฟล์ — dashboard, admin, retirement, AI usage ฯลฯ) hardcode สีขาวโปร่งแสงเสมอ (มองไม่เห็นบนพื้นขาว) แก้ด้วยฟังก์ชันใหม่ `_chartTickColor()`/`_chartGridColor(opacity)` (เช็ค `body.classList.contains('light-mode')` ตอนสร้าง chart คืนสีเข้ม/อ่อนตามธีม) แทน hardcode ทุกจุด. **ข้อควรระวังเรื่อง testing:** modal บางตัว (login/lock screen, PDPA consent, feedback modal) ตั้งใจ hardcode พื้นหลังมืดเสมอไม่ว่าธีมไหน (self-contained, ไม่ใช่บั๊ก) — ก่อนแก้สีตัวหนังสือจุดไหนต้องเช็คว่า container นั้นเปลี่ยนพื้นหลังตามธีมจริงหรือเป็นกล่องมืดถาวรก่อนเสมอ กันแก้ผิดจุด

**รอบ 2 (เจอ+แก้เพิ่ม 2026-07-06 ค่ำ, จาก screenshot จริงที่ user ส่งมา):**
- **input กล่องดำในหน้างบดุล/แผนการเงิน** — `.bs-inp` (สินทรัพย์/หนี้สินรายการย่อย) hardcode `background:#0f0f23` เสมอ ไม่มี light-mode override เลย (ต่างจาก `.finput`/`.fselect`/`.tax-inp` ที่แก้ไปแล้วรอบแรก) + input "รายรับที่คาดหวังต่อเดือน"/"ขึ้นปีละ"/"หยุดที่อายุ" hardcode `background:#1a1a2e` ตรงๆ ใน inline style (ค่าตรงกับ `--surface2` dark-mode พอดี) — **แก้แล้ว:** เพิ่ม `body.light-mode .bs-inp{background:#f8fafc;color:#0f172a;border-color:rgba(0,0,0,.15)}` + เปลี่ยน 3 inline input จาก `#1a1a2e` → `var(--surface2)` (ได้ทั้ง 2 ธีมอัตโนมัติ)
- **`.year-table th` (หัวตาราง 80 ปีในหน้างบดุล)** — hardcode `background:#1a1a2e` เสมอ + `color:var(--muted)` — ผลข้างเคียงจากการแก้ `--muted` ให้เข้มขึ้นรอบแรก (เพื่อให้อ่านง่ายบนพื้นขาว) กลับทำให้ตัวหนังสือหัวตารางนี้ **เข้มเกินไปจนอ่านยากบนพื้นดำที่ไม่เปลี่ยนสี** — บทเรียน: แก้ `--muted` ให้เข้มขึ้นสำหรับพื้นขาว อาจทำให้องค์ประกอบอื่นที่พื้นหลังไม่เปลี่ยนตามธีม (hardcode ทั้งคู่) แย่ลงได้ ต้องเช็คทุกจุดที่ใช้ `var(--muted)` ร่วมกับพื้นหลัง hardcode เข้ม. **แก้แล้ว:** เพิ่ม `body.light-mode .year-table th{background:var(--surface2)}`
- **"ขาวโล่งเกินไป ไม่มีมิติ" — เพิ่ม glassmorphism ให้ light mode** — เดิม light mode เป็นพื้นหลังเรียบสีเดียว (`#f1f5f9`) + การ์ดทึบขาวล้วนหมดทุกใบ (เทียบกับ dark mode ที่มี "Premium polish" glow+gradient+blur อยู่แล้วแต่ scope แค่ `body:not(.light-mode)`). **แก้แล้ว:** เพิ่มชุดเดียวกันให้ light mode โดยตรง (แก้ที่ CSS block เดิม บรรทัด ~562 ไม่ได้เพิ่ม rule ใหม่ซ้อน) — body มี 4 radial-gradient blobs สีพาสเทล (ม่วง/ฟ้า/ชมพู/เขียว) แบบ `background-attachment:fixed`, การ์ด/chart-card/set-card/tax-card เป็นกระจกฝ้า (`background:rgba(255,255,255,.62)` + `backdrop-filter:blur(18px) saturate(160%)` + border ขาวโปร่งแสง + shadow), sidebar/topbar/modal/toast ก็เป็นกระจกฝ้าเช่นกัน (blur 10-20px ตามองค์ประกอบ) — ทำให้แต่ละการ์ดดูมีสีสัน/ความลึกต่างกันไปตามตำแหน่ง (สี blob ที่ทะลุผ่านกระจกต่างกัน) แทนที่จะขาวเหมือนกันหมด
- **Responsive:** breakpoints ที่ 860px และ 768px
- **Component classes:** `.card`, `.btn`, `.finput`, `.fselect`, `.flabel`, `.ftab`, `.pbar/.pfill`
- **Toast:** `toast(message, type)` — type: `'success'` | `'error'` | `'info'`

## Helper Functions สำคัญ

```js
fmt(number)              // format ตัวเลขเป็น ฿X,XXX
saveDB()                 // save localStorage + sync Firestore
saveLocalOnly()          // save เฉพาะ localStorage
loadDB()                 // load จาก localStorage
nav(page)                // navigate ไปหน้า
monthTx(m, y)            // กรอง transactions ของเดือน/ปีที่ระบุ
calcNetWorth()           // คำนวณ Net Worth ปัจจุบัน
getCat(id, type)         // ดึง category object
toast(msg, type)         // แสดง notification
uid()                    // สร้าง unique ID
_escHtml(s)              // escape HTML string (บรรทัด ~14142, ใกล้ท้ายไฟล์ ไม่ใช่กลางไฟล์)
```

## โครงสร้าง Script Block (บรรทัด 2478–14739)

อัปเดตล่าสุดด้วย `grep -n "══════" index.html -A1` — ถ้าตัวเลขข้างล่างไม่ตรงกับที่ grep เจอ ให้เชื่อ grep:

```
~2478–3338   Firebase config (_ENC), DATA multi-user, Firestore sync setup
~3338–3472   AUTO-LOCK
~3472–3540   GEMINI MODEL — auto-retry on quota
~3540–3589   CATEGORIES (INC_CATS/EXP_CATS)
~3589–3633   NAVIGATION — PAGE_TITLE (~3592), nav() (~3595), renderPage() (~3611)
~3633–3745   AI USAGE tab (aiUsageTab)
~3745–3777   SIDEBAR (mobile), DATE, FORMAT (fmt)
~3777–4034   DASHBOARD (renderDash core)
~4034–4377   TRANSACTIONS (txHTML, renderTx)
~4377–4881   IMAGE IMPORT (สแกนใบเสร็จ) + BANK STATEMENT IMPORT
~4881–5975   SAVINGS, DEBTS, INVESTMENTS, DCA
~5975–6436   BANKS, SETTINGS, MODAL UTILS, TOAST, INVESTMENT AUTO-REFRESH
~6436–6959   TAX, BALANCE SHEET & PLAN
~6959–7571   RETIRE PLANNER
~7571–8015   PROFILE
~8015–8030   AI CHAT vars (ประกาศก่อนกัน TDZ) + INIT block — checkLogin() (~8030)
~8030–8426   AI Chat ต่อ (buildFinanceContext/toggleChat/sendChat) + RULE-BASED AI (rulesAI fallback, ~8204)
~8426–9514   FEATURE 1–9 (notifications→category budget) + NEW FEATURE A–H (PIN Lock→PDF Print)
~9514–9960   PATCHES เข้า render เดิม + Quick Add/Undo/Bank Transfer/Savings Rate/Top Merchants/NW Milestone/Monthly Comparison
~9960–10554  OT CALCULATOR, SPLIT TRANSACTION, SPENDING ANOMALY, FINANCIAL CALENDAR widget,
             SUBSCRIPTION TRACKER widget, TAX ADVISOR, TAX OPTIMIZER
~10554–10939 INSURANCE, PRINT MONTHLY REPORT, PATCH renderDash, EMERGENCY FUND, 50/30/20, PAY YOURSELF FIRST, CHANGE PASSWORD
~10939–11215 ADMIN DASHBOARD (renderAdmin)
~11215–11593 DASHBOARD SECTION REORDER, RVV (Recurring vs Variable), YTD SUMMARY
~11593–11881 LOAN CALCULATOR (renderLoan + accelerated schedule)
~11881–12203 CALENDAR PAGE (renderCalendarPage)
~12203–12382 SUBSCRIPTION MANAGER (renderSubscriptions, หน้าเต็ม)
~12382–12581 MAYA AGENT — mayaAgent()/MAYA_TOOLS/MAYA_IMPLS (tool calling ใน browser)
~12581–12682 TX ADVANCED FILTER + BULK SELECT
~12682–13448 TRAVEL (หน้าหลัก + 3D globe) + TRAVEL ROUTE PLANNER + MY LOCATION
~13448–14071 WEALTH RANKING (3 ฐาน TH/ASEAN/World, _WT_DATA/_RANK_TIERS/_calcPercentile)
             — มี WELCOME ONBOARDING (_showWelcomeOnboard) และ CHAT FAB DRAG (snap-to-edge) แทรกอยู่กลางบล็อกนี้
~14071–14144 FINANCE CHARACTER — topbar avatar + rank popup (ใช้ window._fcSvgG)
~14144–14358 4 mini-features เรียงเลข: 1.PDPA CONSENT · 2.EMPTY STATE DASHBOARD ·
             3.RATE LIMIT LOGIN (_rlCheck/_rlFail, ล็อก 5 นาทีหลังพลาด 5 ครั้ง) · 4.FEEDBACK MODAL
~14358–14639 GMAIL BANK AUTO-IMPORT (client-side) — poll `DB.settings.gmailImportUrl`,
             modal ยืนยัน/แก้ก่อนบันทึก (openGmailInbox/_renderGmailModal/confirmGmailTx)
~14639–14739 NAV SORTABLE (drag-and-drop sidebar reorder)
             — เลขบรรทัดด้านบน (จนถึงจุดนี้) ค้างมาตั้งแต่ก่อน auto-commit สะสมหลายพันบรรทัด ใช้ grep "══════" เช็คตำแหน่งจริงเสมอ
             ท้ายไฟล์ปัจจุบัน (หลัง NAV SORTABLE) มีเพิ่ม: NATIVE APP BRIDGE, PUSH NOTIFICATIONS, OFFLINE DETECTION,
             TRAVEL JOURNAL (บันทึกทริป — เพิ่ม 2026-08-03), FAVORITE STOCKS (หุ้นที่ชื่นชอบ),
             แล้วจบด้วย DAILY LOG STREAK (เพิ่ม 2026-08-07 — ดูตาราง Dashboard ด้านบน) ก่อนปิด </script>
```

## ฟีเจอร์ทั้งหมด

| กลุ่ม | ฟีเจอร์ |
|-------|---------|
| Core | รายรับ-รายจ่าย (Fixed/Variable toggle), เงินออม, ธนาคาร, หนี้สิน, พอร์ตลงทุน |
| Core | ภาษีเงินได้, งบดุล, แผนเกษียณ, Subscriptions |
| I18N (เกือบเสร็จสมบูรณ์ 2026-07-03) | **ระบบแปลภาษา TH/EN** — `I18N={th:{},en:{}}` (**1,310 keys ต่อภาษา ณ 2026-08-04** — จำนวนเพิ่มขึ้นเรื่อยๆ ตามฟีเจอร์ใหม่ เช็คของจริงด้วย `Object.keys(I18N.th).length` เสมอ, sync กันสมบูรณ์ — เช็คด้วย `Object.keys(I18N.th).length===Object.keys(I18N.en).length` ทุกครั้งหลังแก้) + **`tr(key)`** (⚠️ ไม่ใช่ `t()` — ชื่อนี้ชนกับตัวแปร loop `t`/`tx` ที่ใช้ทั่วไฟล์แทน transaction object ทำให้เกิด `TypeError` ถ้าตั้งชื่อ `t`) + `setLang()`/`toggleLang()` + `applyLang()`; เก็บที่ `localStorage.mm_lang` (ใช้ได้ก่อน login) + sync เข้า `DB.settings.lang` หลัง login; ปุ่มสลับภาษา `#lang-btn` อยู่ข้าง theme toggle; attribute รองรับ: `data-i18n` (textContent), `data-i18n-ph` (placeholder), `data-i18n-html` (innerHTML), `data-i18n-title` (title tooltip); `PAGE_TITLE` เป็น `Proxy` route ผ่าน `tr(page)`; ฟังก์ชันช่วย bilingual: `catLabel(c)` (หมวดหมู่ ใช้แทน `c.label` ตรงๆ ทุกที่ที่ iterate INC_CATS/EXP_CATS ดิบ), `getCat()` (คืน labelEn อัตโนมัติถ้า currentLang==='en'), `monthShortArr()`/`monthFullArr()`/`beYear()` (เดือน/ปี), `insTypeLabel()`/`insInsuredLabel()`/`insCycleLabel()` (ประกัน), `_tripStyleLabel()` (travel estimator style); **หน้าที่แปลครบแล้ว (ทุกหน้าหลัก):** Login/Register/Forgot, Settings, Dashboard (ทุก widget), Transactions (list/filter/modal/scan ใบเสร็จ/bank statement import), Savings, Banks (+โอนเงิน), Debts (+payoff planner), Insurance, Investments (โครงหลัก — modal เพิ่ม/แก้ไข, พอร์ต, กราฟ, DCA), Tax (ฟอร์มลดหย่อน+ผลคำนวณ+สแกนสลิป/ทวิ50), Balance/Plan (งบดุล+ตาราง 80 ปี+modal), Retire (ฟอร์ม+ผลลัพธ์+กราฟ), Profile (ข้อมูลส่วนตัว+การศึกษา+งาน+ทักษะ), Calendar (+แก้ bug `CAL_MON_FULL`/`CAL_MON_SHORT` เป็น stale var ไม่ re-evaluate ตอนสลับภาษา → เปลี่ยนเป็นเรียก `monthFullArr()`/`monthShortArr()` สดทุกครั้ง), Travel (แผนที่+สรุปทริป+ประมาณงบ), Subscriptions, OT Calculator, Loan Calculator (+แก้ bug `r.beYear(date...)` ผิด syntax เป็น `beYear(r.date...)` ในตาราง amortization), Admin Dashboard, AI Usage, **toast() messages ทั้งไฟล์ (แปลครบ 100%)**, AI Maya chat UI (quick prompts, header, placeholder); **ยังไม่แปล (ส่วนเล็กที่เหลือ):** Tax Optimizer sub-feature ในหน้า Tax, Rebalancing panel ในหน้า Investments, **คำตอบที่ AI Maya ตอบกลับ** (เนื้อหาสร้างจาก Gemini/rule-based AI แบบไดนามิก เป็นคนละงานจาก UI — ต้องแก้ system prompt ที่ส่งให้ Gemini + เขียน `rulesAI` fallback ใหม่ทั้งหมดถ้าจะให้ตอบเป็นอังกฤษ) เพิ่ม element ใหม่ต้องเติม `data-i18n="key"` + เพิ่ม key เข้า `I18N.th`/`I18N.en` คู่กันเสมอ (ห้ามลืมฝั่งใดฝั่งหนึ่ง); **gotcha เดียวกับ push token:** โค้ดที่ต้องรันหลัง login ทั้ง fresh-login และ session-restore ห้าม patch `checkLogin` (สายเกินไป) ต้องเช็ค `currentUser` ตรงๆ |
| ~~AI Inbox~~ | ❌ **ถอดออก 2026-06-26** (เมนู+หน้า+โค้ด `renderInbox`/`inboxItems`/`confirmInbox`/ฯลฯ ลบหมดแล้ว) |
| รายการซ้ำ | **⚠️ กลับมามี UI เต็มแล้ว (ยืนยันจริงในโค้ด 2026-08-04)** — เอกสารเดิมบอกว่า "ถอดหน้า/UI ออก 2026-06-26" แต่ตรวจโค้ดจริงพบว่า `renderRecurring()`, nav item `data-nav="recurring"`, page routing ใน `renderPage()` ยังอยู่ครบ ทำงานได้ปกติ (เอกสารเก่าผิด/ไม่ได้อัปเดตตอนเพิ่มกลับมา — ไม่รู้วันที่แน่ชัดที่เพิ่มกลับ) — page key `recurring` |
| ~~จัดลำดับ (Priority)~~ | ❌ **ถอดออก 2026-06-26** (`renderPriority`/`PQ_QUADS`/`getPriorities`/ฯลฯ + CSS `.pq-*` ลบแล้ว; `DB.priorities` ยังอยู่ใน DB ไม่มี UI) — ยืนยันซ้ำแล้ว 2026-08-04 ว่ายังไม่มีร่องรอยกลับมา |
| ~~หารบิลเพื่อน (Split)~~ | ❌ **ถอดออก 2026-08-04** ตามคำขอ user — ลบ nav item (`data-nav="split"`), page container (`pg-split`), routing ใน `renderPage()`, ฟังก์ชันทั้งหมด (`renderSplit`/`getSplitFriends`/`getSplitExpenses`/`getSplitSettlements`/`computeSplitBalances`/`openSplitFriendModal`/`saveSplitFriend`/`deleteSplitFriend`/`openSplitExpenseModal`/`saveSplitExpense`/`deleteSplitExpense`/`openSplitSettleModal`/`saveSplitSettlement`/ฯลฯ), และ modal ทั้ง 3 (`m-split-friend`/`m-split-expense`/`m-split-settle`) — ยืนยันด้วย grep ว่าไม่มี reference หลงเหลือเลย. **ไม่ได้ลบ:** i18n keys `split_*` ใน `I18N.th`/`I18N.en` (dead แต่ไม่กระทบ key-count sync เพราะไม่แตะทั้งคู่), และ **อย่าสับสนกับ "FEATURE: SPLIT TRANSACTION"** (`_splitRows`/`toggleTxSplit` — แยกหมวดหมู่ในรายการเดียว คนละฟีเจอร์ ไม่ได้แตะ) |
| ~~ครัวเรือน (Household)~~ | ❌ **ถอดออก 2026-08-04** ตามคำขอ user (พร้อมกับ Split) — ลบ nav item (`data-nav="household"`), page container (`pg-household`), routing, ฟังก์ชันทั้งหมด (`renderHousehold`/`loadMyHouseholds`/`householdListen`/`selectHousehold`/`createHousehold`/`inviteHouseholdMember`/`removeHouseholdMember`/`leaveHousehold`/`deleteHousehold`/`saveHouseholdTx`/`deleteHouseholdTx`/ฯลฯ), ตัวแปร `_household`/`_householdUnsub`/`_myHouseholds`, modal ทั้ง 2 (`m-household-create`/`m-household-tx`), และ auto-select-household hook ใน `enterApp()` (setTimeout เรียก `loadMyHouseholds()` หลัง login) — Firestore collection `households` ที่เคยใช้เก็บข้อมูลยังอยู่ (ไม่ได้ลบข้อมูลเก่าของ user ที่เคยสร้างครัวเรือนไว้ แค่ไม่มี UI เข้าถึงแล้ว) |
| Insurance | **หน้า `insurance` (`renderInsurance`)** — ทะเบียนประกันตัวเอง+ครอบครัว; `DB.insurance=[{id,name,type,insured,insuredName,benefits,coverage,premium,cycle,payUntilType,payUntil,coverUntil,taxDeduct,notes}]`; การ์ดสรุป (จำนวน/เบี้ยรวมต่อเดือน `insMonthly`/วงเงินรวม/เบี้ยลดหย่อน-ภาษีต่อปี) + แยก section **ของตัวเอง vs ครอบครัว** (insured==='self' หรือไม่); การ์ดต่อกรมธรรม์ (ไอคอนตาม `INS_TYPE`, วงเงิน, คุ้มครองอะไร, เบี้ย/รอบ, `insPayUntilText` จ่ายถึง ตลอดชีพ/อายุ/ปี/วันที่, แท็บลดหย่อน) คลิก→ modal `m-ins` (`openInsModal`/`saveIns`/`deleteIns`); nav item `data-nav="insurance"` |
| Tax | คำนวณภาษี (`calcTaxLive`, `calcTaxAmount` + `TAX_BRACKETS`), autofill จาก tx, สแกน ทวิ50/สลิป AI, ประวัติรายปี (`DB.taxHistory`); **Tax Optimizer (`taxOptimizer`/`_taxComputeFull`)** — section ในหน้าภาษี (ต่อจาก `#tax-result-panel`, สร้าง `#tax-optimizer` เอง, monkey-patch `calcTaxLive`): แสดงฐานภาษี marginal, "ซื้ออีกเท่าไหร่ถึงเพดาน → ประหยัดภาษีกี่บาท" ต่อช่อง (RMF/SSF/ThaiESG/PVD/ประกันชีวิต/สุขภาพ/บำนาญ), **เรียงลำดับควรซื้อก่อนตามระยะล็อก** (สุขภาพ→ชีวิต→ESG 5ปี→SSF 10ปี→PVD→บำนาญ→RMF อายุ55), ยอดประหยัดรวมถ้าซื้อเต็ม; caps mirror `calcTaxLive` (กลุ่มเกษียณรวม ฿500k); ถ้า net ต่ำกว่าเกณฑ์/เต็มแล้วแสดงข้อความเหมาะสม |
| Tools | คำนวณ OT (×1/×1.5/×3), คำนวณสินเชื่อ (ตาราง amortization + **โป๊ะเพิ่มลดต้นลดดอก**) |
| Loan Prepay | `renderLoan` คำนวณ 2 schedule: base + **accelerated (โป๊ะเพิ่ม)**; `_loanState.extraMonthly`/`lumpSum`/`lumpMonth` (lumpMonth ว่าง=งวดแรก); accelerated loop เงินโป๊ะตัดเงินต้นทั้งหมด ดอกงวดถัดไปลดตาม วนจนยอด=0; การ์ดเปรียบเทียบ: ผ่อนเร็วขึ้นกี่ปี-เดือน (`monthsSaved`), ดอกที่ประหยัด (`interestSaved`), วันผ่อนหมดเดิม→ใหม่ + ตาราง accelerated (`showAccTable`, ไฮไลต์งวดโป๊ะก้อน) |
| Calendar | ปฏิทินพร้อมโน้ต, แสดง transactions/recurring/debt due, month/year picker |
| Travel | หน้า `travel` (`renderTravel()`) — สรุปทริปท่องเที่ยวแยกตามสถานที่; หมวด `travel` (fa-plane) + ใช้ **tag = ชื่อสถานที่** จัดกลุ่ม; trip detection เว้นช่วง >14 วัน = ทริปใหม่ (`_travelTrips`); การ์ดสรุป (ยอดรวม/จำนวนทริป/สถานที่) + การ์ดสถานที่คลิกขยายดู trip breakdown; ปุ่ม `addTravelTrip()` prompt ชื่อสถานที่ → เปิด tx modal preset cat+tag; system tags ถูกกรองออก (`TRAVEL_SYS_TAGS`) |
| Travel Map (3D globe) | **แผนที่จริงบนลูกโลก 3D หมุนได้** หน้า travel — **MapLibre GL JS v5.6** (globe projection `map.setProjection({type:'globe'})`) + **OpenFreeMap** vector tiles (`tiles.openfreemap.org/styles/liberty` ฟรี ไม่ต้อง key) → **vector tiles คมชัดทุกระดับซูม ถึงระดับถนน** (เดิม globe.gl ใช้ texture เดียวซูมแล้วแตก — เปลี่ยนแล้ว); lazy-load JS+CSS ผ่าน `_loadScript`/`_loadCss` ตอนเปิดหน้า; พิมพ์ประเทศ/เมือง → `_geocode()` (Nominatim) → `_flyToPlace()` (`map.flyTo`) → ปุ่ม **✓ เคยไปแล้ว / ★ อยากไป** (`travelMark`); หมุดเขียว #34d399=เคยไป เหลือง #fbbf24=อยากไป (`maplibregl.Marker` DOM element, คลิก=fly); หมุนเอง = `_spinLoop()` (requestAnimationFrame นัดจน center.lng ตอน zoom<4, หยุดเมื่อแตะ/ลาก/ซูม), toggle `travelToggleSpin`; `_syncTravelPlacesFromTx()` geocode จาก tx อัตโนมัติ (1.1s/ครั้ง) + migration `geoVer=2`; **map อยู่ใน card ระดับ page (persistent) แยกจาก `#travel-content`** เพื่อไม่ทำลาย map instance — re-nav เรียก `_travelResize()` (map.resize); `DB.travel.places=[{name,lat,lng,visited,label,type,fromTx}]` |
| Travel Globe gotcha | **Nominatim geocode ต้องเรียง `importance` แล้วเลือกตัวสูงสุด** — ค้นหา "ญี่ปุ่น" (accept-language=th) ผลแรกคือ "บ้านญี่ปุ่น อยุธยา ประเทศไทย" (importance 0.147) ไม่ใช่ประเทศญี่ปุ่น (0.938); fix: `limit=8` + `sort((a,b)=>b.importance-a.importance)[0]`. **ค้นหาใน UI ใช้ `_geocode(q,true)` (fresh) เสมอ** เลี่ยง cache พิกัดผิดเก่า + อัปเดตหมุดเดิมที่ผิดให้ถูกทันที; `_geocode` คืน `addresstype`+`bbox` → `_flyToBbox()` (fitBounds) ก่อน fallback `_altForType()` zoom: country=4, state/province=6.5, city=10, town/village=12 |
| Travel Geocode dual-provider (เพิ่ม 2026-08-03) | **ปัญหาเดิม:** Nominatim อย่างเดียวหาที่พัก/แหล่งท่องเที่ยวเล็กๆ ไม่เจอบ่อย (เป็น address geocoder ไม่ใช่ POI search, ไม่มี fuzzy match) — user ขอทางเลือกฟรี (ไม่เอา Google Places เพราะมีค่าใช้จ่าย). **แก้:** เพิ่ม **Photon (`photon.komoot.io`, ฟรี ไม่ต้อง key, ใช้ฐาน OSM เดียวกันแต่ fuzzy/POI search ดีกว่า)** เรียกคู่กับ Nominatim ผ่าน `Promise.allSettled` ใน `_geocode()` แล้วเลือกผลด้วย `_pickBetterGeocode(nom,pho)`: ถ้า Nominatim importance ≥0.25 (เมือง/ประเทศ/แลนด์มาร์คดัง) ใช้ Nominatim, ต่ำกว่านั้น(รวมถึง POI เล็กๆ ที่ไม่มี importance)ใช้ Photon แทน — ยืนยันด้วยเคสจริง "ibis Chiang Mai": Nominatim เจอแต่ importance≈0.00006 (ต่ำมาก) → ระบบเลือก Photon ถูกต้อง. แยกเป็น `_geocodeNominatim(place)`/`_geocodePhoton(place)` คืน shape เดียวกัน `{lat,lng,label,type,bbox,importance}` ให้ `_geocode()` ห่อรวม — caller เดิมทั้งหมด (`travelSearch`/`travelFly`/`_syncTravelPlacesFromTx`/`_tripLogSyncPin`) ไม่ต้องแก้เพราะ signature/return contract เดิมทุกอย่าง. **⚠️ gotcha ตอน implement:** Photon public API **ไม่รองรับ `lang=th`** (รองรับแค่ `default/de/en/fr` — ส่ง `lang=th` ไปจะได้ error object `{lang:[...]}` ไม่มี `features` แม้ query ถูกต้อง) ต้องปล่อย default (ไม่ส่ง `lang` เลย) ซึ่งคืนชื่อภาษาไทยจาก OSM `name` tag ได้ปกติอยู่แล้วเพราะสถานที่ในไทยส่วนใหญ่มี name tag ภาษาไทย. Photon response เป็น GeoJSON (`features[].geometry.coordinates`=[lon,lat], `properties.extent`=[westLon,northLat,eastLon,southLat] ต้องแปลงเป็น `[south,north,west,east]` ให้ตรง format ที่ `_flyToBbox()` ใช้อยู่แล้ว — คนละ order กับ Nominatim `boundingbox`); label ต้องประกอบเองจาก `properties.name/street/city/state/country` (ไม่มี `display_name` สำเร็จรูปแบบ Nominatim) โดยให้ country อยู่ท้ายสุดเพื่อให้ `_travelCountry()`/`_simplePinCountry()` (parse comma-split ท้ายสุด) ทำงานถูกต้องเหมือนเดิม. **ข้อจำกัดที่ยังแก้ไม่ได้:** ทั้ง 2 ผู้ให้บริการพึ่งข้อมูล OSM เดียวกัน — สถานที่ที่ไม่เคยถูกแท็กลง OSM เลย (ทดสอบแล้วกับโรงแรมเล็กบางแห่ง) จะหาไม่เจอทั้งคู่ ต่างจาก Google Places ที่มีฐานข้อมูลธุรกิจของตัวเอง |
| Travel Journal auto-fill ตำบล/อำเภอ/จังหวัด/ประเทศ (เพิ่ม 2026-08-03) | **ขอเพิ่ม:** พิมพ์ชื่อสถานที่ใน modal บันทึกทริปแล้วกดค้นหา ให้ระบบใส่ที่อยู่ระดับ ตำบล/อำเภอ/จังหวัด/ประเทศ ให้อัตโนมัติ (ไม่ต้องพิมพ์เอง). **เพิ่ม field ใหม่ใน modal:** `_tl-subdistrict`(ตำบล/แขวง), `_tl-district`(อำเภอ/เขต) คู่กับ `_tl-province`/`_tl-country` เดิม + ปุ่มค้นหา (ไอคอน crosshair) ข้าง input สถานที่ เรียก `tripLogSearchPlace()` (ใหม่) → `_geocode(place,true)` → เอา `.admin` ที่ geocode คืนมาใส่ทั้ง 4 ช่องให้ทันที พร้อม status text "พบแล้ว!"/"ไม่พบ" ใต้ช่องสถานที่. **แกะที่อยู่จาก provider:** เพิ่ม `.admin={subdistrict,district,province,country}` เข้าไปใน object ที่ `_geocodeNominatim`/`_geocodePhoton` คืน (ผ่าน `_pickBetterGeocode` เหมือนเดิมไม่ต้องแก้) — ใช้ 3 helper ใหม่: `_stripThaiAdminPrefix(s,prefixes)` ตัด prefix ("ตำบล"/"แขวง"/"อำเภอ"/"เขต"/"จังหวัด"/"ประเทศ") ออกให้เหลือชื่อล้วน (no-op ถ้าไม่มี prefix), `_parseNominatimAdmin(addr)` ใช้กับ Nominatim's `address` object (ต้องเปิด **`addressdetails=1`** ในคำขอ — เดิมไม่ได้ขอ) โดย **ค้นหาด้วย prefix ของค่า ไม่ใช่ชื่อ key** เพราะ Nominatim ใช้ key ไม่คงที่ตามพื้นที่ (บางที่ตำบลอยู่ใน `city`, บางที่อยู่ใน `city_district` — ยืนยันจาก curl ทดสอบจริงหลายเคส) แต่ตัวค่าเองมี prefix ไทยติดมาเสมอ (เช่น `"city":"ตำบลช้างเผือก"`) ค้นหาแบบนี้แม่นกว่า, `_parsePhotonAdmin(p)` ใช้กับ Photon (key ตรงความหมายอยู่แล้ว: `district`≈ตำบล, `county`≈อำเภอ, `state`≈จังหวัด — แค่บางค่ามี prefix ติดมาบางค่าไม่มี ใช้ strip เดียวกันเผื่อไว้), และ `_parseAdminFromLabel(label)` แกะจาก comma-split label string ธรรมดา (ใช้กับ pin เก่าที่มีแค่ `.label` ไม่มี address object เต็ม — เรียกใน `_tripLogMergedList()`/`openTripLogModal` prefill ทำให้ **quick-pin เก่าที่เคยปักไว้ก่อนหน้าฟีเจอร์นี้ก็โชว์ที่อยู่เต็มย้อนหลังได้ทันทีโดยไม่ต้อง migrate ข้อมูล** เพราะ label เดิมก็มี prefix ไทยฝังอยู่แล้ว. เพิ่ม `p.district`/`p.county` เข้าไปใน label ที่ `_geocodePhoton` ประกอบเอง (เดิมข้ามสองฟิลด์นี้ไป) เพื่อให้ `_parseAdminFromLabel` แกะเจอด้วย. `_tripLogCard`'s sub-line ขยายเป็น `[subdistrict,district,province,country]` (เดิมมีแค่ province/country). ยืนยันด้วยเคสจริง "วัดพระธาตุดอยสุเทพ" → auto-fill ได้ครบ 4 ช่อง (สุเทพ/เมืองเชียงใหม่/เชียงใหม่/ไทย) ถูกต้องตรงกับที่ทดสอบผ่าน curl ไว้ก่อน implement. **🐛 ทดสอบสถานที่ต่างประเทศเพิ่ม (2026-08-03 รอบบ่าย) เจอบั๊กจริง 1 ตัว + แก้แล้ว:** ค้นหา "Eiffel Tower Paris" → ประเทศออกมาเป็น **"ฝรั่งเศสแผ่นดินใหญ่"** (ผิด! ควรเป็น "ฝรั่งเศส") เพราะ Nominatim ของสถานที่นี้มี `addr.region="ประเทศฝรั่งเศสแผ่นดินใหญ่"` (ชื่อทางการของ metropolitan France) ปรากฏอยู่**ก่อน** `addr.country="ประเทศฝรั่งเศส"` ใน object — ทั้งคู่ขึ้นต้นด้วย "ประเทศ" เหมือนกัน prefix-search แบบเดิม (`vals.find()`) หยิบตัวแรกที่เจอผิดตัว. **แก้:** ให้ `_parseNominatimAdmin` ใช้ `addr.country` (key ตรงความหมาย เชื่อถือได้ 100% ในทุกเคสที่ทดสอบ) เป็นค่าหลักก่อนเสมอ ผ่าน prefix-strip ตรงๆ ไม่ต้องพึ่ง prefix-search อีก, เหลือ `pick(['ประเทศ'])` เป็นแค่ fallback ท้ายสุดเผื่อ `addr.country` ไม่มีค่า (แทบไม่เกิดขึ้นจริง). **ทดสอบยืนยันด้วย 3 เคสต่างประเทศ** (Node script รันจาก extracted function จริงในไฟล์ เทียบกับ curl response จริง ก่อน deploy): Tokyo Tower → district="มินาโตะ" ถูก, country="ญี่ปุ่น" ถูก, province='' (ว่างจริง — Nominatim ของเคสนี้ไม่มี state/prefecture ใน address object เลยแม้แต่ตัวเดียว เป็น data gap ของ OSM เอง ไม่ใช่ bug ใน logic แก้ไม่ได้ด้วยวิธี parse address object); Times Square New York → province="New York" ถูก (fallback ไป `addr.state` เพราะไม่มี "จังหวัด" prefix), country="สหรัฐอเมริกา" ถูก; Eiffel Tower Paris → province="Île-de-France" ถูก, country="ฝรั่งเศส" ถูก (หลังแก้). ทดสอบซ้ำในเบราว์เซอร์จริงทั้ง Times Square + Eiffel Tower ผ่าน UI (ปุ่มค้นหาใน modal) ยืนยันตรงกับผลจาก Node test — **สรุปพฤติกรรมสำหรับสถานที่ต่างประเทศ:** ตำบล/อำเภอ (ศัพท์เฉพาะไทย) จะว่างเสมอสำหรับสถานที่นอกไทย (ตามคาด ไม่ใช่บั๊ก), จังหวัด/ประเทศ ใช้ได้เกือบทุกเคสยกเว้นบางสถานที่ที่ OSM ไม่มีข้อมูล state/province ในบันทึกนั้นเลย (เคส Tokyo Tower) |
| Travel Route Planner | ปุ่ม **วางแผนเส้นทาง** (`travelToggleRoutePanel`) + **ตำแหน่งฉัน** (`travelGetMyLocation` GPS → หมุดแดง) ใต้ globe; เลือก A→B จาก dropdown (`_updateRouteSelects`) + 4 โหมด (`travelSetMode`): driving/foot/bike → **OSRM** (`router.project-osrm.org` ฟรี ไม่ต้อง key) คืนระยะ+เวลา+เส้นทางจริง วาดด้วย `_drawRoute` (addSource/addLayer line), plane → `_haversine` + เวลาบิน (÷880 km/h) + airport overhead + **ประมาณค่าตั๋ว** (`_estFlightFare` ขั้นบันได บาท/กม. ตามระยะ); OSRM fail → fallback haversine เส้นตรง; `_flyToBbox`/`fitBounds` ซูมเห็นทั้งเส้นทาง; **gotcha:** route panel อยู่ใต้ globe 380px → เปิดต้อง `panel.scrollIntoView` (ไม่ใช่ scroll ไป globe), globe canvas ต้อง `overflow:hidden` กัน MapLibre overlay ทับปุ่ม |
| Travel Budget/Country | **งบทริปต่อสถานที่** (`travelSetBudget` → `DB.travel.budgets={name:amount}`) — bar ในการ์ด detail (เขียว/แดงเมื่อเกิน) + chip "เกินงบ" ที่ header; **ค่าใช้จ่ายต่อประเทศ** (`_travelCountry` ดึงประเทศจาก `place.label` segment ท้าย ตัดรหัสไปรษณีย์) แสดงเป็น bar เมื่อมี >1 ประเทศ |
| Travel Trip Estimator | **ประมาณงบทริปล่วงหน้า** (`_travelEstimatorHtml`/`tripEstCalc`) — การ์ดคำนวณคร่าวๆ ก่อนไปจริง: เลือกหมุดปลายทาง + จำนวนวัน + สไตล์ (`_TRIP_STYLES` budget/standard/comfort) → ตั๋วเครื่องบินไป-กลับ (`_estFlightFare` haversine จาก `_myLocation` หรือ กทม. default) + ที่พัก (nights×hotel) + ค่ากินเที่ยว (days×daily); ระยะ <300km = ไม่รวมตั๋ว (แนะขับรถ); ปุ่ม `tripEstSaveBudget` → เซฟเข้า `DB.travel.budgets` เชื่อมกับ budget tracker; แสดงทั้งหน้า มี/ไม่มี tx |
| Travel Journal (บันทึกทริป) | **เพิ่ม 2026-08-03, merge กับ pin list เดิม 2026-08-03 (รอบ 2)** — สมุดบันทึกการเดินทาง เก็บ `DB.travel.tripLogs=[{id,place,province,country,startDate,endDate,desc,visited}]`; ปุ่ม "เพิ่มบันทึกทริป" เปิด modal (`openTripLogModal`/`saveTripLog`/`deleteTripLog`) กรอกประเทศ/จังหวัด/สถานที่/วันที่ไป-กลับ/สถานะ(เคยไป-อยากไป)/คำบรรยายทริปเป็นข้อความยาวได้; การ์ดแยก 2 กลุ่ม (เคยไปแล้ว เรียงวันที่ล่าสุดก่อน / อยากไป) คลิกชื่อสถานที่ = `travelFly()`; ปุ่ม "✓ เคยไปแล้ว" บนการ์ดอยากไปเรียก `markTripLogVisited()`; หลัง save เรียก `_tripLogSyncPin()` (async) upsert เข้า `DB.travel.places` เดิมด้วย (geocode ผ่าน `_geocode` ถ้ายังไม่มี pin) แล้ว **re-render อีกครั้งหลัง sync resolve** (`.then(renderTravel)`) กัน pin-chip ส่วนเก่าค้างสถานะเก่า; โมดัลสร้างแบบ dynamic-inject (`document.body.appendChild`, แบบเดียวกับ feedback modal) ใช้ class `.overlay`/`.modal`/`.finput` เดิมเพื่อ theme ตามแอปอัตโนมัติ (ไม่ hardcode สีเหมือน feedback modal); เพิ่ม i18n keys `travel_journal_*` (23 keys) เข้า `I18N.th`/`I18N.en`. **Merge กับ pin list เดิม (รอบ 2, user ขอให้ไม่โชว์ซ้ำ):** ลบ `placesSec` (chip list เดิมของ `DB.travel.places` ที่เคย render แยกเป็นหมวด "เคยไปแล้ว/อยากไป" ในตัว `renderTravel()` เอง) ออกจากทั้ง 2 จุดที่ประกอบ `el.innerHTML` — ตอนนี้ `_tripLogMergedList()` รวม `DB.travel.tripLogs` + pin จาก `DB.travel.places` ที่ยังไม่มี tripLog ตรงกัน (จับคู่ด้วยชื่อ, case-insensitive) เป็นลิสต์เดียวแสดงในการ์ด journal; pin ที่มาจากทางลัด (ช่องค้นหาแผนที่ด้านบน → `travelMark()`, หรือ auto จาก tx ผ่าน `_syncTravelPlacesFromTx`) ที่ยังไม่มีบันทึกละเอียดจะติด label "(ปักหมุดด่วน)" + ปุ่ม `+` (แทน pencil) เรียก `openTripLogModal(null, placeName)` prefill ชื่อ/ประเทศ(จาก `_simplePinCountry` parse `place.label`)/สถานะ ให้ "อัปเกรด" เป็นบันทึกละเอียดได้โดยไม่ต้องพิมพ์ชื่อซ้ำ (save แล้วกลายเป็น tripLog จริง มี id); ปุ่มลบ/มาร์กเคยไปของ pin แบบ simple เรียก `travelRemovePlace()`/`travelToggleVisited()` ตรงๆ (ของเดิม ไม่ใช่ `deleteTripLog`/`markTripLogVisited`) เพราะไม่มี id ใน tripLogs |
| AI | แชทกับ Maya (Gemini agent + tool calling + rule-based), สแกนใบเสร็จ/สลิปภาษี |
| AI | Maya agent สั่งงานได้ (10 tools): บันทึก/แก้/ลบ/ค้นหารายการ, เพิ่มออม/จ่ายหนี้, ตั้งงบ, สร้างเป้าออม/หนี้, what-if (tools ทำงานกับ DB ใน browser, ใช้ apiKey ของ user, Gemini OpenAI-compat) |
| AI | Bot agent 19 tools (ai_agent.py): read (summary/budget/debt/saving/portfolio/prices) + write (add/edit/delete tx, create goal/debt, set SL-TP, dca) + search_transactions + what_if_saving + detect_subscriptions |
| AI | จัดหมวดเรียนรู้ได้ — `_learn_cat()` จำ desc→หมวดจากที่ user เลือกเอง (callback `cat:`) / แก้หมวด; `_detect_cat` เช็ค learned cache ก่อน keyword |
| AI Usage | **หน้า `aiusage` (`renderAiUsage()`)** — มอนิเตอร์ค่า token/ค่า API มี **2 tab** (`_aiUsageTab` + `aiUsageTab()`): **Gemini** (`_aiUsage`) + **Claude Code** (`_claudeUsage`). การ์ดสรุป (วันนี้/เดือนนี้/เฉลี่ย/คาดการณ์เดือน) + แยกตามรุ่นโมเดล (bar) + กราฟแท่งรายวัน (Chart.js, สี accent ต่อ provider) + ตารางรายวัน; self-fetch ทั้ง 2 field ถ้าหน่วยความจำว่าง |
| AI Usage · Gemini | ข้อมูล**จริง**จาก Cloud Monitoring (project Best1, ครอบคลุมเว็บ Maya + บอท + ทุกสคริปต์) + spend-cap card (฿100). **แหล่ง:** `gemini_usage_report.py --firestore` (cron 08:15 บน VM) → Firestore **field `aiUsage`** (PATCH updateMask=aiUsage ผ่าน SA token, แยกจาก `db` blob กัน race) |
| AI Usage · Claude | token Claude Code จาก **transcript ในเครื่อง** (`~/.claude/projects/**/*.jsonl`, มี `message.usage` ต่อข้อความ) — **ประมาณการแบบ API** (Pro/Max เหมารายเดือน ไม่ใช่เงินจ่ายจริง → banner เตือน, ไม่มี cap). **แหล่ง:** `claude_usage_report.py --firestore` **รันบนโน้ตบุ๊กเท่านั้น** (VM ไม่มี transcript) → Firestore **field `claudeUsage`**; dedupe ตาม `message.id`, ราคา opus $5/$25 · cache read 0.1× · write 1.25×(5m)/2×(1h). หมายเหตุ: Anthropic Admin Usage/Cost API ใช้ไม่ได้กับ subscription/individual จึงต้อง parse transcript เอง. **Automation (ตั้ง 2026-06-26):** Windows Scheduled Task **"MoneyMind Claude Usage Report"** รัน `pythonw claude_usage_report.py --firestore` (cwd `Documents`) ทุกวัน **09:00 + 20:00** (StartWhenAvailable = catch-up เมื่อโน้ตบุ๊กปิด); ก่อนหน้านี้ไม่มี automation → tab Claude ค้างบ่อย (เคยค้าง 06-23). จัดการ: `Get-ScheduledTask "MoneyMind Claude Usage Report"`, รันมือ `Start-ScheduledTask "MoneyMind Claude Usage Report"` |
| Admin | **หน้า `admin` (`renderAdmin()`, owner เท่านั้น)** — **Part A (จาก `members`):** การ์ด total/online/DAU/WAU/MAU + stickiness/สมัครวันนี้/churn>30วัน/มี email จริง%, กราฟผู้ใช้สะสม (Chart.js `_adminChart` line), แท่ง platform (web/android/ios), ตารางผู้ใช้ + search (`filterAdminTable`) + **Export CSV** (`exportAdminCsv`, `window._adminMembers`). **Part C (จาก `admin/summary`):** อ่าน doc `admin/summary` (field `json` stringValue → parse) แสดง usage รวมต่อคน (txCount/netWorth/lastTxDate/ฟีเจอร์ที่ใช้) + totals; ถ้าไม่มี doc → ขึ้นคำแนะนำรัน `admin_stats.py`. ⚠️ rules block อ่าน userdata คนอื่นจาก browser → ต้องสรุปผ่าน SA ฝั่ง VM เท่านั้น. **กรองบัญชีทดสอบออกจากสถิติ (เพิ่ม 2026-08-07):** ตรวจ churn/stickiness จริงพบว่า 14/21 บัญชีใน `members` เป็นบัญชีทดสอบ/เดโม่ (test1-5, testuser, demo_6815, warakorn_k, best_05 ฯลฯ) ปนอยู่กับ user จริงทำให้ตัวเลขเพี้ยนหนัก (churn ดูสูงเกินจริง, stickiness ต่ำเกินจริง) — เพิ่ม `_isTestAccount(m)` กรองด้วย pattern ชื่อ/อีเมล (`test|demo`, `warakorn`, `best_05`, owner) **ไม่ใช้ "0 transaction" เป็นเกณฑ์** (user ใหม่จริงที่ยังไม่ activate ก็มี 0 tx เหมือนกัน ไม่อยากกรองออกเงียบๆ); ยืนยันผ่าน Firestore ตรงว่า `yok07` (31 tx จริง) ไม่ใช่บัญชีทดสอบแม้ชื่อดูแปลก. **การ์ดสถิติ/กราฟ growth/platform breakdown** คำนวณจาก `realMembers` (กรองแล้ว); **ตารางผู้ใช้ + `window._adminMembers` (ที่ `exportAdminCsv` ใช้)** ยังเป็น list เต็มไม่กรอง (ไม่อยากซ่อนข้อมูลจาก CSV export) — แถวบัญชีทดสอบใส่ badge "ทดสอบ" (`admin_test_account`) + `data-test="1"` + `display:none` เริ่มต้น, มี checkbox "แสดงบัญชีทดสอบ (N)" (`admin_show_test_accounts`) เรียก `_adminToggleTestRows(show)` ตั้ง `window._adminShowTest` แล้ว re-apply `filterAdminTable()` (แก้ให้เช็คทั้ง search query และ toggle พร้อมกัน) — reset `window._adminShowTest=false` ทุกครั้งที่ `renderAdmin()` รันใหม่กันไม่ตรงกับ checkbox ที่วาดใหม่ (unchecked เสมอ) |
| Dashboard | โหมด "รายจ่ายจริง" — toggle `hideMoneyMove` (default on) ตัด transfer/cash ออกจากรายจ่าย/งบ/กราฟ; helper `isRealExp()`, `isMoneyMove()`, ปุ่มในการ์ดกราฟหมวด |
| Dashboard | คลิกหมวดในกราฟ pie → `openCatDrill()` modal `m-catdrill` แสดงรายการในหมวดนั้น |
| Transactions | กรองตามหมวด (`tx-cat-filter`) + bulk select (`toggleBulkMode`): ติ๊กหลายรายการ → เปลี่ยนหมวด (`applyBulkCat`)/ลบ (`bulkDelete`) ทีเดียว; floating `bulk-bar` |
| Dashboard | **Finance Character** (`renderFinanceChar`, `#dash-char-section`) — ตัวละคร SVG chibi **8 ระดับ** (Iron→Legend, lv 1–8) แสดง Net Worth ชนชั้น + progress bar + Health Score + คำแนะนำ; monkey-patch บน renderDash |
| Dashboard | **Rank Popup Character** — ใน `_buildRankPopupBody` แสดงตัวละคร chibi แบบ **SVG vector** (ไม่ใช่ sprite image); ฟังก์ชัน `_fcSVG(lv, gender)` อยู่ใน IIFE ที่ expose เป็น `window._fcSvgG`; `_buildRankPopupBody` ต้องเรียกผ่าน `window._fcSvgG` ไม่ใช่ `_fcSVG` ตรงๆ; gender toggle `window._fcGender` 0=หญิง 1=ชาย; K array index 1–8 (null placeholder ที่ 0) |
| Dashboard | **Wealth Ranking (percentile)** — `_WT_DATA` เก็บ 8 tier ต่อ 3 ขอบเขต (`th`/`asean`/`world`, ข้อมูลอ้าง Credit Suisse 2023) แต่ละ tier มี min/max net worth + `pct` (สัดส่วนประชากร); `_calcPercentile(nw, scopeKey)` คำนวณ percentile จริงจาก NW ปัจจุบัน (interpolate ในช่วง tier); `_getRank(lv)` แปลง lv→ชื่ออันดับ (`_RANK_TIERS` Iron→Legend, คนละชุดกับ `_WT_DATA` แต่ lv 1–8 ตรงกัน); `_rankPopupScope`/`_wrankScope` toggle ขอบเขตที่แสดงในป็อปอัพ |
| Dashboard | **🔥 Daily Log Streak (เพิ่ม 2026-08-07)** — การ์ดใหม่ท้ายไฟล์ (`#dash-streak-card`, monkey-patch บน `renderDash` แบบเดียวกับ Wealth Rank/Finance Character) นับ "วันติดต่อกันที่บันทึกอย่างน้อย 1 รายการ" (`DB.settings.streak={current,longest,lastLogDate,graceWeek,milestonesShown}`) — ตอบช่องว่างที่ gamification เดิม (rank/level) ผูกกับ net worth อย่างเดียว ไม่มีกลไกให้รางวัลพฤติกรรมสม่ำเสมอรายวันเลย ทั้งที่ stickiness ปัจจุบันแค่ 13% (churn 53% จาก Admin Dashboard). **Logic:** `_updateStreak()` เช็คทุกครั้งที่ `renderDash()` รัน (ไม่ hook จุดสร้าง transaction ใหม่ทุกจุดเพราะมีหลายทางมาก — manual/quick add/AI scan/bank import ฯลฯ); ครั้งแรกสุด (`lastLogDate` ว่าง) เรียก `_backfillStreak(s)` คำนวณ baseline ย้อนหลังจากประวัติ `DB.transactions` จริง โดย **anchor ที่วันที่ล่าสุดที่มี transaction จริง ไม่ใช่ "วันนี้" ตรงๆ** (🐛 เจอตอนทดสอบจริง: ถ้า anchor ที่วันนี้ตรงๆ แล้ววันนี้ยังไม่บันทึกอะไรเลย backfill จะไม่ทำงาน ทำให้เห็น "0" ทั้งที่ streak ยังไม่ขาดจริง — แก้เป็น anchor ที่ transaction date ล่าสุดในประวัติแทน); มี **grace 1 วัน/สัปดาห์** (`_canUseGrace`+`_isoWeekKey`, ISO week) กันพลาดวันเดียวรีเซ็ตทั้งยวง (churn สูงอยู่แล้ว ไม่อยากให้ฟีเจอร์นี้เร่ง churn เพิ่ม) — พลาดติดกัน ≥2 วันเต็มถึงจะรีเซ็ตเป็น 1. **Display ไม่ผูกกับ commit โดยตรง:** `_streakDisplayCurrent(s,today)` คำนวณค่าที่โชว์แยกจาก `s.current` ที่เก็บจริง (ยังไม่บันทึกวันนี้แต่ gap≤2 → โชว์ค่าเดิม "ยังไม่ขาด", gap>2 → โชว์ 0 กันตัวเลขค้างหลอก) โดยไม่แก้ state จริงจนกว่าจะมี transaction วันใหม่มา commit — **ทดสอบจริงบนบัญชี production (owner) แล้ว: backfill เจอ streak จริง 34 วัน (อิงจากประวัติ 26 ก.ค.–4 ส.ค.), แต่ gap ปัจจุบัน (4→7 ส.ค. ขาด 5-6 ส.ค. 2 วันเต็ม) เกิน grace ที่ให้ได้ → โชว์ current=0/best=34 ถูกต้องตามดีไซน์**. milestone toast ที่ [7,30,100,365] วัน (`_checkStreakMilestone`, กันซ้ำด้วย `milestonesShown`). แถบจุด 7 วันล่าสุด (เขียว=บันทึกแล้ว, เทา=ไม่ได้บันทึก, วันนี้ไฮไลต์ขอบสีม่วง) คำนวณจาก `DB.transactions` ตรงๆ ไม่พึ่ง state เก็บ. **เพิ่ม i18n keys `streak_*` 6 คีย์** ต่อท้าย `I18N.th`/`I18N.en` (เช็คแล้ว 1346=1346). **🐛 gotcha ตอนทดสอบ:** เจอ browser/CDN cache เก็บโค้ดเก่าไว้ระหว่างทดสอบ (`_backfillStreak` เก่ายัง 2 argument ทั้งที่ deploy โค้ดใหม่ (1 argument) ไปแล้วจริงบน production ตาม `curl` ยืนยัน) — แก้ด้วยการแนบ query string กันแคช (`?_cachebust=...`) ตอน navigate ทดสอบ ไม่ใช่บั๊กจริงของโค้ด แค่ข้อจำกัดของ environment ทดสอบ ควรจำไว้ทำแบบนี้ทุกครั้งที่ทดสอบ production URL สดๆ หลัง deploy |
| Onboarding | **Welcome Onboarding** (`_showWelcomeOnboard`) — overlay 4 ขั้นตอนแนะนำผู้ใช้ใหม่ (บันทึกรายการแรก→เพิ่มธนาคาร→ตั้งเป้าออม→ลอง AI มายา) คลิกแต่ละขั้น `nav()` ไปหน้านั้นเลย ยกเว้น step 1 (ดู gotcha ด้านล่าง — เปิด modal บันทึกรายการตรงๆ ไม่ nav) |
| System | **PDPA Consent** — banner/modal ขอความยินยอมเก็บข้อมูล (เพิ่มเป็น mini-feature "1." ท้ายไฟล์) |
| Login | **Login logo image (2026-08-10)** — เดิมทำ "Login Mascot" หุ่นยนต์ CSS (2026-08-10, มี 4 mood + อัปเกรดทรง "Volt" ตาม CodePen อ้างอิงในวันเดียวกัน) แต่ user feedback "ไม่สวยเลย เอาหุ่นยนออก" → **ลบหุ่นยนต์ทิ้งทั้งหมด** (HTML/CSS/JS IIFE/i18n keys `mascot_*` 6 คีย์ ลบครบ ไม่เหลือ dead code — เช็คด้วย `grep -c mascot index.html` ต้องได้ 0) **แทนที่ด้วยโลโก้แอปจริง** (`<img src="assets/logo.png" class="login-logo-img">`, ไฟล์เดียวกับที่ `landing.html` ใช้อยู่แล้ว) วางไว้เหนือข้อความ "MoneyMind" ใน `.login-box` ตรงๆ — เรียบง่าย ไม่มี animation/interaction ใดๆ. ยืนยัน syntax ผ่าน (`new Function()` parse OK), CSS brace balance ผ่าน (4 style blocks, ทุกบล็อก matched), i18n key sync ผ่าน (1352=1352), ทดสอบจริงทั้ง dark/light mode ไม่มี console error |
| Dashboard | **Empty State** — หน้า Dashboard ว่าง (ยังไม่มี transaction) แสดง placeholder แนะนำแทนกราฟ/การ์ดว่างเปล่า |
| System | **Rate Limit Login** (`_rlCheck`/`_rlFail`/`_rlReset`, key `mm_login_fail`) — ใส่รหัสผ่านผิดครบ 5 ครั้ง (`_RL_MAX`) → ล็อก 5 นาที (`_RL_LOCK_MS`); monkey-patch บน `tryLogin` |
| System | **Feedback Modal** — ฟอร์มส่งความคิดเห็น/รายงานปัญหาในแอป (mini-feature "4." ท้ายไฟล์) |
| System | **Chat FAB Drag** — ปุ่มลอย (FAB) เปิดแชท Maya ลากได้ทั้งจอ, snap ไปขอบที่ใกล้ที่สุดอัตโนมัติ, จำตำแหน่งไว้ `localStorage` (`mm_fab_pos`) |
| Transactions | **Gmail Bank Auto-Import (client-side)** — poll URL จาก `DB.settings.gmailImportUrl` (n8n เขียนลง Google Sheet "PendingTx") ทุก 1 นาที (`_gmailPoll`/`_gmailStartPoll`), badge แจ้งจำนวนรายการค้าง (`_gmailUpdateBadge`); คลิกเปิด modal (`openGmailInbox`/`_renderGmailModal`) แก้ไข/ยืนยันทีละรายการ (`confirmGmailTx`) หรือยืนยันทั้งหมด (`confirmAllGmailTx`) ก่อนบันทึกเข้า `DB.transactions` จริง — คนละส่วนกับ GCP script `gmail_bank_alert.py` (ฝั่งนั้นดัก Gmail แล้วเขียน Sheet, ฝั่งนี้ดึง Sheet มาให้ผู้ใช้ยืนยันในเว็บ) |
| Dashboard | Health Score, NW History (ปุ่มเลือกช่วง 6M/1Y/ทั้งหมด — เพิ่ง unhide จาก `display:none` ที่ซ่อนถาวรมานาน + แก้ label ปี พ.ศ. ผิด 2026-08-05 ดู Gotchas), Monthly Comparison, Spending Heatmap, YTD Summary |
| Dashboard | RVV (Fixed vs Variable), 50/30/20, Emergency Fund, Pay Yourself First |
| Dashboard | Drag-and-drop reorder sections (DB.settings.dashOrder) |
| Transactions | Tags, Search, Export CSV, Quick Add, Undo, Category Budget |
| Advanced | Cash Flow Forecast, Debt Payoff Planner, Investment Rebalancing |
| Investments · Favorite Stocks (เพิ่ม 2026-08-04, เพิ่มคะแนนความชอบวันเดียวกัน, แยกเป็นแท็บเอง 2026-08-04 รอบเย็น, เพิ่มชื่อบริษัท+หมวดหมู่+ติ๊กกรอง+ดูข้อมูลเชิงลึก 2026-08-04 ค่ำ) | **"หุ้นที่ชื่นชอบ"** — **แท็บแยกของตัวเองในหน้าพอร์ตลงทุน** (ปุ่ม `#inv-tab-fav` ในกลุ่ม `.ftab` เดียวกับ พอร์ต/DCA, `switchInvTab('favstocks')`) **ไม่ได้ฝังอยู่บนสุดของแท็บพอร์ตหลักแล้ว** (ของเดิมทำแบบ monkey-patch insert `afterbegin` เข้า `#inv-content` ทุกครั้งที่ดูพอร์ต — user ขอแยกหน้าเพราะปนกับพอร์ตจริงแล้วรก) `renderInv()` เช็ค `_invTab==='favstocks'` ก่อน `'dca'` แล้วเรียก `renderFavStocksView()` (ฟังก์ชันใหม่ แทนที่ `#inv-content` ทั้งก้อนด้วย `_favStocksHtml()` เหมือน pattern `renderDcaView()`) — `switchInvTab()` ซ่อนปุ่ม Rebalancing/จัดการพอร์ต/เพิ่มการลงทุน (`#inv-btn-rebal`/`#inv-btn-port`/`#inv-btn-add`) ตอนอยู่แท็บนี้เพราะไม่เกี่ยวกัน (ใช้ปุ่ม "+ เพิ่มหุ้น" ในการ์ดเองแทน) รายการหุ้นที่อยากติดตาม **ไม่ต้องถือจริง** เก็บที่ `DB.favStocks=[{id,symbol,note,rating,category,addedAt}]` (`category` เพิ่ม 2026-08-04 ค่ำ, คนละก้อนกับ `DB.investments`/`DB.wishlist` เดิม); ปุ่ม "+ เพิ่มหุ้น" เปิด modal dynamic-inject (`openFavStockModal(id)` รองรับทั้งเพิ่ม/แก้ไข, symbol field disabled ตอนแก้ไข — `saveFavStock`/`removeFavStock`) ราคาปัจจุบันดึงผ่าน `_fetchQuotesBatch()`/VM proxy ก่อน (ดู [[project_ai_proxy]]/หัวข้อ Gotchas) แล้ว fallback `getPriceFor(symbol,'หุ้น')` เดิม แบบ async ไม่บล็อก render (`_loadFavStockPrices`) — **ให้คะแนนความชอบได้ 1-5 ดาว** ในโมดัล (`_setFavStockRating`, ไอคอน `fas fa-star` สลับสีเอาไม่ใช้ solid/regular variant กันปัญหา Font Awesome Free ไม่มี "far" ในบันเดิลที่ใช้) แสดงดาวบนการ์ดด้วย (`_favStockStars`) และ **เรียงลิสต์ตามคะแนนมากไปน้อยอัตโนมัติ** (`_favStocksHtml` sort) — **บอท `stock_news_watch.py` อ่านรายการนี้จาก Firestore ไปวิเคราะห์ข่าว โดยเรียงตามคะแนนเช่นกัน** (ดูตาราง cron ด้านล่าง). **ชื่อเต็มบริษัท (เพิ่ม 2026-08-04 ค่ำ):** `STOCK_COMPANY_NAMES` — dict แบบ static ล้วน (ไม่เรียก API เพิ่ม เร็ว+ฟรี 100%) map ticker→ชื่อเต็ม ครอบคลุมหุ้นยอดนิยม/ที่บอทติดตามอยู่แล้ว (~45 ตัว); ticker ที่ไม่อยู่ในนี้แสดงแค่ symbol เฉยๆ **ไม่เดาชื่อ** กันโชว์ชื่อผิด — เพิ่ม ticker ใหม่ที่รู้จักแน่นอนเข้า dict นี้ได้เรื่อยๆ. **คำอธิบายธุรกิจ (เพิ่ม 2026-08-04 ค่ำ, ต่อจากชื่อเต็มบริษัทวันเดียวกัน):** `STOCK_COMPANY_DESC` — dict static คู่กัน (key ตรงกับ `STOCK_COMPANY_NAMES` ทุกตัว, 45 ticker) บอกสั้นๆ ว่าบริษัททำธุรกิจอะไร (เช่น AAPL: "ผลิตและจำหน่าย iPhone, Mac, iPad...") แสดงใต้ดาวให้คะแนนในการ์ด สีเดียวกับ `var(--muted)`; ticker ที่ไม่อยู่ใน dict ไม่แสดงบรรทัดนี้เลย (ไม่เดาเหมือนชื่อบริษัท) — โน้ตส่วนตัวของ user (`f.note`) แยกให้เห็นชัดว่าเป็นคนละอย่างกับคำอธิบายธุรกิจ (factual) โดยใส่สี `var(--primary-l)` + ไอคอน note-sticky นำหน้า. **เพิ่ม ticker ใหม่ต้องแก้ทั้ง `STOCK_COMPANY_NAMES` และ `STOCK_COMPANY_DESC` คู่กันเสมอ** ไม่งั้นชื่อขึ้นแต่คำอธิบายจะหายไปเงียบๆ. **หมวดหมู่ + ติ๊กกรอง (เพิ่ม 2026-08-04 ค่ำ, ปรับ UI เป็น Liquid Glass pill ดึกๆ รอบ 2):** `FAVSTOCK_CATS={mega,china,thai,fund}` (หุ้นเมกา/หุ้นจีน/หุ้นไทย/กองทุนรวม, แต่ละหมวดมีสี badge เอง) เลือกได้ใน modal ผ่าน `<select id="_fs-category">` (ไม่บังคับเลือก, ค่าว่าง = "ไม่ระบุ"); เหนือลิสต์มีแถวติ๊กกรอง 4 หมวด — **UI เดิมเป็น `<input type="checkbox">` ธรรมดา user บอกว่า "ดูไม่สวย" ขอเป็นปุ่มกดสไตล์ Liquid Glass แทน (เพิ่ม 2026-08-04 ดึกๆ):** เปลี่ยนเป็น `<button class="favcat-pill">` ทรงแคปซูล (`border-radius:100px`) กระจกฝ้า (`backdrop-filter:blur(12px)`, ตาม pattern เดียวกับ `.btn-ghost`/การ์ด Liquid Glass อื่นๆ ในไฟล์ — ดู CSS class `.favcat-pill` ~บรรทัด 1048 dark mode + ~1122 light mode override) ไอคอน `fa-circle-check`/`fa-circle` สลับตามสถานะ active/inactive; **สีของหมวดยังคงไดนามิกต่อหมวด** (ผสมกับกระจกผ่าน inline style คำนวณจาก `info.color` ตอน active เท่านั้น — inactive ใช้กระจกเรียบสีเทาจากคลาส ไม่ผสมสีหมวด) กันปนกับ CSS class ที่ fix สีตายตัวไม่ได้เพราะมี 4 สีต่างกัน; `_toggleFavStockCatFilter(cat)` เปลี่ยน signature จาก `(cat,checked)` เป็น `(cat)` เดี่ยว (toggle เองจาก `_favStockCatFilter.has()` แทนอ่านจาก `this.checked` ของ checkbox ที่ไม่มีแล้ว) → `_favStockCatFilter` ยังเป็น `Set` เหมือนเดิม **เก็บแค่ใน session ไม่ persist ลง DB** รีเซ็ตเป็นติ๊กครบทุกครั้งที่โหลดหน้าใหม่ — หุ้นที่ยังไม่ได้ตั้งหมวด (`category` ว่าง) **โชว์เสมอไม่ว่าจะติ๊กหมวดไหนอยู่** (กันของเก่าที่ยังไม่ได้จัดหมวดหายไปจากลิสต์ทันทีที่ deploy ฟีเจอร์นี้) ตัวเลขนับที่หัวการ์ด `(N)` ยังนับ**ทั้งหมด**ไม่ใช่หลังกรอง — ทดสอบยืนยันแล้วทั้ง dark/light mode, toggle เปิด-ปิดถูกต้อง, ไม่มี console error ใหม่. **ดูข้อมูลเชิงลึก/งบการเงินย้อนหลัง + โปรไฟล์บริษัท (เพิ่ม 2026-08-04 ค่ำ, ขยาย business profile ค่ำเดียวกันรอบ 2):** ปุ่ม `fa-chart-column` ในการ์ด — **แสดงทุกหมวดรวมกองทุน** (รอบแรกซ่อนถ้าหมวด `fund` เพราะคิดว่ากองทุนไม่มีข้อมูล แก้แล้วหลังพบว่า yfinance `.info` มีคำอธิบายกองทุนให้ใช้จริง) เปิด modal กว้าง (`openFavStockDetailModal`) แสดง sector/industry/พนักงาน/ประเทศ + คำอธิบายธุรกิจเต็มจาก Yahoo Finance (ตัดสั้น+ปุ่ม "อ่านเพิ่มเติม", ภาษาอังกฤษล้วน) ก่อน แล้วต่อด้วยกราฟกำไรขาดทุน + กระแสเงินสดย้อนหลัง 4-5 ปี (ถ้ามีงบการเงิน) ดึงผ่าน endpoint `/v1/financials` บน VM (yfinance server-side, cache 24 ชม. — ดูรายละเอียดเต็มในตาราง "AI Proxy" ด้านล่าง) — lazy load เฉพาะตอนกดดู ไม่ prefetch ทั้งลิสต์ ตามที่ user กังวลเรื่อง "กินข้อมูลใหญ่" |
| System | PIN Lock, Dark/Light mode, Multi-currency, Bank Transfer, NW Milestones |
| System | Sidebar nav reorder (DB.settings.navOrder) |

## Pattern การเพิ่มฟีเจอร์ใหม่

ฟีเจอร์ใหม่ **ไม่แก้โค้ดเดิม** — ใช้ monkey-patch แทน:

```js
const _orig = renderDash;
renderDash = function(){
  _orig();
  renderMyNewFeature();
};
```

เพิ่มใหม่ที่ **ท้าย script block** (ก่อน `</script>` ที่บรรทัด ~14739 ปัจจุบัน) หรือต่อจาก section สุดท้าย — เช็คตำแหน่งจริงด้วย `grep -n "══════" index.html -A1` ก่อนเสมอ  
**ระวัง:** อย่า redeclare `let`/`const` ที่มีอยู่แล้ว — จะเกิด SyntaxError

## ข้อควรระวัง (Gotchas)

- **⚠️ `fsSave()` เขียนทับ `db` field แบบเต็มก้อนเสมอ (ไม่ merge) — session เก่าที่ค้างอยู่เขียนทับข้อมูลใหม่จากที่อื่นได้แบบเงียบๆ (เกิดจริง 2026-08-07)** — `fsSave()` (index.html ~2855) ทำ `.set({db:JSON.stringify(DB),...},{merge:true})` — `{merge:true}` ป้องกันแค่ field ระดับบนสุดอื่น (เช่น `aiUsage`) ไม่ให้หาย แต่ตัว **`db` เองเป็น string field เดียว ถูกแทนที่ทั้งก้อนเสมอด้วยสิ่งที่อยู่ใน `DB` ของ session ที่เรียก** — ต่างจาก `fsLoad()` ที่มี `_mergeDbSnapshot()` union-by-id ป้องกันข้อมูลชนกัน. **เหตุการณ์จริง:** ระหว่างทดสอบฟีเจอร์ streak ในเบราว์เซอร์ (เปิด tab ค้างไว้ตั้งแต่ก่อน user ยืนยัน 11 รายการผ่าน Telegram) เรียก `_updateStreak()` ตรงๆ ใน console เพื่อทดสอบ — ฟังก์ชันนี้จบด้วย `saveDB()` ซึ่ง debounce แล้วยิง `fsSave()` อัตโนมัติ → เขียนทับ Firestore ด้วย `DB` เก่าในหน่วยความจำ tab นั้น (ยังไม่มี 11 รายการที่ bot เพิ่งบันทึกไปหมาดๆ) → **ลบ 11 รายการที่ user เพิ่งยืนยันไปหายหมดแบบไม่มี error ใดๆ ให้เห็น** (กู้คืนแล้วโดยดึงอีเมลต้นฉบับมา re-parse ใหม่ + เขียนตรงผ่าน SA ไม่ผ่าน client). **บทเรียนสำคัญ:** ห้ามเปิด browser tab ค้างไว้นานแล้วเรียกฟังก์ชันที่จบด้วย `saveDB()`/`fsSave()` ผ่าน console เพื่อ "ทดสอบ" โดยเฉพาะถ้ามีทางอื่น (bot, อุปกรณ์อื่น) กำลังเขียนข้อมูลใหม่เข้าบัญชีเดียวกันพร้อมกัน — ถ้าจำเป็นต้องทดสอบฟังก์ชันที่ mutate `DB`/`saveDB` ควร **navigate ใหม่ (cache-bust) ทันทีก่อนเรียกทุกครั้ง** ให้ได้ข้อมูลสดสุด ไม่ใช่พึ่ง tab ที่เปิดค้างจากก่อนหน้า — หรือทดสอบ logic แยกส่วนแบบ pure-function (ไม่เรียกผ่าน `DB`/`saveDB` จริง) แทนเมื่อทำได้ (ดู `_streakDisplayCurrent` เป็นตัวอย่างที่ทดสอบแยกได้แบบไม่กระทบ state จริง)
  - **✅ แก้ต้นตอแล้ว 2026-08-07 (ต่อจากที่บันทึกไว้ข้างบนวันเดียวกัน) — `fsSave()` เปลี่ยนเป็น Firestore `runTransaction()`:** อ่าน `db` field ล่าสุดจาก Firestore สดๆ ณ ตอน commit เสมอ (ไม่พึ่ง timing ของ listener/debounce guard อีกต่อไป) แล้ว merge ก่อนเขียนจริง — เพิ่มฟังก์ชันใหม่ `_mergeForSave(remote,local)` (index.html ~2917, คนละตัวกับ `_mergeDbSnapshot()` เดิมที่ใช้ตอน "อ่าน/ฟัง") **เพราะเจอบั๊กซ้อนระหว่างทดสอบจริง:** ถ้าใช้ union-by-id เดิมตรงๆ ตอน "เขียน" ด้วย การลบรายการผ่านหน้าเว็บจะไม่มีผลจริง เพราะ remote (ที่ยังไม่รู้เรื่องการลบ) จะถูก union กลับเข้ามาใหม่ทุกครั้งที่เซฟ — แก้ด้วยการเก็บ baseline `_lastSyncedIds` (id ทุกตัวที่เคย sync แล้ว, อัปเดตหลัง merge สำเร็จทั้งใน `fsLoad()`/`fsListen()`/`fsSave()`) แยกแยะ "id ที่ไม่เคยรู้จักมาก่อนเลย = ของใหม่จากอีกฝั่งจริงๆ เก็บไว้" ออกจาก "id ที่เคยรู้จักแต่ตอนนี้ local ไม่มี = ลบตั้งใจ ห้าม revive". **ทดสอบ end-to-end บนบัญชี production จริงแล้ว 2 เคส** (สร้าง test transaction ผ่าน SA แล้วลบทันทีด้วย script คนละตัวจำลอง delete จริงในเว็บ): (1) ลบรายการ local แล้วเซฟ พร้อมกับมี "bot" เขียนรายการใหม่แทรกเข้ามาก่อนเซฟเสร็จ (จำลองผ่าน SA เขียนตรงระหว่างนั้น) → ยืนยันผ่าน Firestore ตรงๆ: **รายการที่ลบหายจริง + รายการที่ bot เพิ่งเพิ่มยังอยู่ครบ ทั้งคู่ถูกต้อง 100%**. **ข้อจำกัดที่รู้ตัวแล้วและไม่ได้แก้ (นอกสโคป):** `_mergeDbSnapshot()` เดิมที่ใช้ใน `fsLoad()`/`fsListen()` ("อ่าน") ยังเป็น pure union-by-id เหมือนเดิม ไม่รู้จักการลบ — ถ้าอุปกรณ์ A ลบรายการแล้วอุปกรณ์ B (ที่ localStorage ยังมี cache เก่าของรายการนั้นอยู่) โหลดข้อมูลจาก Firestore (ซึ่งตอนนี้ไม่มีรายการนั้นแล้ว) ผลจาก `fsLoad()`'s merge จะยัง "ฟื้น" รายการที่ถูกลบกลับมาที่อุปกรณ์ B ชั่วคราว (จนกว่าอุปกรณ์ B จะ `fsSave()` เองอีกครั้งซึ่งตอนนี้ปลอดภัยแล้วเพราะแก้ฝั่งเขียนแล้ว) — เจอเองระหว่างทดสอบ (ต้องเคลียร์ `localStorage` มือเพื่อความสะอาดตอนทดสอบซ้ำ) ถือเป็น known limitation รอแก้ในอนาคตถ้าจำเป็น (ต้องทำ `fsLoad()`/`fsListen()` ให้เคารพ `_lastSyncedIds` แบบเดียวกัน). **ยังไม่ได้แก้ฝั่ง bot/cron scripts (~30 ตัว)** ที่ทำ read-modify-write ผ่าน `mm_firestore.py`+REST แบบเดียวกัน (เสี่ยงชนกันเองระหว่าง bot กับ bot หรือ bot กับ client แบบเดียวกัน) — ขอบเขตใหญ่กว่ามาก เป็นงานแยกในอนาคต
  - **✅ แก้จุดเสี่ยงสูงสุดฝั่ง bot แล้ว 2026-08-07 — เพิ่ม `mm_firestore.safe_write_db()` + ย้าย `stock_bot.py` มาใช้:** ตรวจแล้วพบว่ามีแค่ 5 สคริปต์ที่เขียน `userdata/{user}.db` จริง (`stock_bot.py`, `sync_prices.py`, `gmail_body_parser.py`, `gmail_image_import.py`, `gmail_statement_import.py`) ไม่ใช่ ~30 ตัวอย่างที่กังวลไว้แต่แรก (ตัวอื่นๆ อ่านอย่างเดียว หรือเขียนคนละ collection เช่น `admin/summary`,`reports`,`adminHistory`) — **`stock_bot.py` เสี่ยงสูงสุดชัดเจน** เพราะเป็น systemd ระยะยาวที่โต้ตอบได้ตลอดเวลา (ทุกครั้งที่ user กดปุ่มหมวดหมู่ใน Telegram) ชนกับ web app ที่ user เปิดพร้อมกันได้ง่ายที่สุด (ตรงกับสาเหตุจริงของเหตุการณ์ข้อมูลหาย 11 รายการที่เกิดขึ้นแล้ว) ส่วนอีก 4 ตัวรันตาม cron เวลาตายตัว (วันละครั้ง/ทุก 4 ชม.) โอกาสชนน้อยกว่ามาก เลยแก้ตัวเดียวก่อนเป็นจุดคุ้มค่าสุด. **กลไก:** เพิ่ม `mm_firestore.safe_write_db(url, mutate_fn, max_retries=5)` — อ่าน document พร้อม `updateTime`, เรียก `mutate_fn(db)` ได้ dict ใหม่, เขียนกลับด้วย query param **`currentDocument.updateTime`** (Firestore precondition, optimistic concurrency) — ถ้ามีคนเขียนแทรกระหว่างนั้น (updateTime ไม่ตรง) Firestore ปฏิเสธการเขียนแทน overwrite เงียบๆ แล้วฟังก์ชัน retry เอง (อ่านใหม่+เรียก `mutate_fn` ใหม่+เขียนใหม่) — คนละวิธีจาก `runTransaction()` ฝั่งเว็บ (SDK ทำให้อัตโนมัติ) แต่ผลลัพธ์เทียบเท่ากันเพราะ REST API ล้วนไม่มี SDK transaction helper ให้ใช้ตรงๆ. `stock_bot.py`'s `save_to_firestore()`: สร้าง `id` ของ transaction ใหม่**ครั้งเดียวนอก mutate_fn** (สำคัญ — กัน retry สร้าง id ใหม่ซ้ำๆ จนได้ transaction ซ้ำหลายรายการ), `mutate_fn` เช็ค id ซ้ำก่อน append (idempotent ปลอดภัยเรียกซ้ำได้จาก retry). **ทดสอบแบบบังคับให้เกิด race จริง** (เขียน test script แยก ยิง concurrent write แทรกเข้าไปกลาง `mutate_fn` การเรียกครั้งแรกโดยตั้งใจ, ใช้ document ทดสอบแยกไม่แตะข้อมูลจริง): ยืนยัน `mutate_fn` ถูกเรียก 2 ครั้ง (retry เกิดขึ้นจริง) และ**ข้อมูลทั้ง 3 ฝั่งรอดหมด** (base + รายการที่ "อีก writer" แทรกเข้ามา + รายการของเราเอง) — deploy ขึ้น VM + restart `moneymind-bot` แล้ว ทำงานปกติ (เช็ค log ยืนยัน "Bot started" ไม่มี error). **✅ ย้ายอีก 4 สคริปต์ที่เหลือให้ครบแล้ว 2026-08-07 (ต่อทันทีตามคำขอ):**
  - `sync_prices.py`: เดิม fetch ราคาจาก yfinance ครั้งเดียวแล้วเขียนทับ `db` ตรงๆ — แยกเป็น 2 ขั้น (fetch ราคาเก็บใน `price_map` ครั้งเดียวเหมือนเดิม เพราะเรียก yfinance แพง ไม่อยาก retry ซ้ำ) แล้ว apply `price_map` เข้ากับ `db.investments` **สดทุกรอบ retry** ผ่าน `mutate_fn`. **ทดสอบรันจริงบน production แล้ว** (28 หุ้น อัปเดตราคาสำเร็จ ฿691,693 รวม) ยืนยัน `transactions`/`investments` count ไม่เพี้ยนหลังรัน
  - `gmail_body_parser.py`/`gmail_image_import.py`: เดิม `firestore_get()`+`firestore_patch()` คู่มือ (อ่าน-แก้-เขียนทับตรงๆ) — ลบทิ้งทั้งคู่ (dead code หลังย้าย) เปลี่ยนจุดเขียนจริง (`save_transaction`/`add_transaction`) ไปใช้ `safe_write_db()` แทน
  - `gmail_statement_import.py`: ซับซ้อนกว่าเพื่อน — เดิม `load_db()`/`save_db()`/`existing_dedup_keys()` แยกจากกัน เขียนครั้งเดียวหลัง loop dedup+append หลายรายการพร้อมกัน — ต้องรวม logic dedup+append ทั้งหมดเข้าไปใน `mutate_fn` เดียว (เพื่อให้ dedup ทำงานกับ `db` สดทุกรอบ retry ไม่ใช่ snapshot เก่า) — `id` ที่สุ่มด้วย `uid()` ต่างกันได้ในแต่ละรอบ retry โดยไม่กระทบความถูกต้อง เพราะ dedup key คือ `(date, amount)` ไม่ใช่ id
  - **เพิ่ม feature ใหม่ใน `safe_write_db()`:** รองรับ `mutate_fn` คืนค่า **`None`** = ไม่ต้องเขียนอะไรเลย (ข้าม PATCH ไปเลย, คืน db เดิมที่อ่านมา) — ใช้กับเคส dedup ซ้ำ (`gmail_body_parser.py`, `gmail_statement_import.py`) กันยิง write เปล่าๆ ที่ไม่มีอะไรเปลี่ยนทุกครั้งที่เจอ email ซ้ำ (พฤติกรรมเดิมก่อนแก้ก็ไม่เขียนตอน dedup อยู่แล้ว ฟีเจอร์นี้แค่รักษาพฤติกรรมเดิมไว้ให้ตรงกับของเก่า ไม่ใช่เพิ่มใหม่)
  - **ทดสอบ:** `py_compile` ผ่านทั้ง 6 ไฟล์ (`mm_firestore.py`+5 สคริปต์), import-check ผ่านทั้ง 3 ตัวที่ไม่ได้รันสด (gmail_*), `sync_prices.py` รันจริงผ่าน production สำเร็จ, deploy ขึ้น VM + restart `moneymind-bot` แล้ว (schedule cron เดิมของอีก 4 ตัวไม่ต้อง restart อะไร แค่รอบถัดไปจะใช้โค้ดใหม่อัตโนมัติ)
  - **สรุป:** ตอนนี้ทุกจุดที่เขียน `userdata/{user}.db` (ทั้งฝั่งเว็บ 1 จุด + ฝั่ง bot/cron 5 จุด) ใช้กลไก optimistic-concurrency ป้องกันเขียนทับกันแบบเดียวกันหมดแล้ว — ปิดความเสี่ยงคลาสนี้ทั้งระบบ (ยกเว้น `admin_stats.py`/scripts อื่นที่เขียนคนละ collection ซึ่งไม่ชนกับ `userdata` เลยไม่ต้องแก้)
- **✅ เสถียรภาพ VM เพิ่มเติม 2026-08-07 (ตรวจสุขภาพระบบหลังบ้านแล้วเจอ 2 จุดเสี่ยง):**
  1. **ไม่มี swap เลย** บน e2-micro (958MB RAM, เหลือใช้ได้จริงแค่ ~350MB ตอนตรวจ) — เสี่ยง OOM kill service สำคัญแบบไม่มีสัญญาณเตือนถ้า memory spike ชั่วคราว. **แก้:** เพิ่ม swapfile 1GB (`/swapfile`, `mkswap`+`swapon`) + เพิ่มเข้า `/etc/fstab` ให้ติดตั้งอัตโนมัติหลัง reboot ด้วย — ดิสก์มีที่เหลือพอ (3.4GB ว่างตอนนั้น จาก 9.6GB ทั้งหมด)
  2. **ไม่มี logrotate สำหรับ log ของ cron/service เลยสักไฟล์** — `gmail_bank_alert.log` โตถึง 3.3MB แล้วและจะโตไม่มีวันจบ (poll ทุก 5 นาที log ทุกครั้ง). **แก้:** เพิ่ม `/etc/logrotate.d/moneymind` — หมุนทุกสัปดาห์ เก็บ 4 สัปดาห์ย้อนหลัง compress ไฟล์เก่า + cap ที่ 20MB/ไฟล์กันไฟล์เดียวบวมเร็วเกินระหว่างสัปดาห์ ใช้ `copytruncate` (จำเป็นเพราะ `moneymind-bot`/`gmail-bank-alert` เป็น process ระยะยาวที่เปิด file handle ค้างไว้ ไม่ restart ทุกครั้งที่ log หมุน)
  - **✅ ทดสอบ disaster-recovery path จริงแล้ว 2026-08-07 (ต่อเนื่องจากเหตุการณ์เดียวกัน) — เพิ่ม `firestore_restore.py`:** เดิมมีแค่ `firestore_backup.py` สำรอง `userdata/warakorn` ขึ้น Drive ทุกวัน (`Backups/moneymind_YYYY-MM-DD.json`, เก็บ 90 วัน) แต่**ไม่เคยมีใครทดสอบว่ากู้คืนได้จริงหรือเปล่า** — เขียนสคริปต์ใหม่ `firestore_restore.py` (source `C:\Users\warakorn\Documents\`, sync เข้า `moneymind-bot` repo copy ด้วย) ทดสอบแล้วครบวงจร: list backup (เจอ 15 ไฟล์ย้อนหลังถึง 24 ก.ค. ตรงตาม retention 90 วัน) → ดาวน์โหลด+parse backup ล่าสุด (1,686 tx, ครบทุก field 28 ตัว) → เขียนกลับเข้า Firestore ไปที่ document ทดสอบแยกต่างหาก (`userdata/_restore_test`, **ไม่แตะข้อมูลจริงเลย**) → อ่านกลับมาเทียบจำนวนตรงกัน → ลบ document ทดสอบทิ้ง — ผ่านทุกขั้นตอน ยืนยันว่า pipeline backup→restore ใช้งานได้จริงถ้าเกิดเหตุฉุกเฉินจริงในอนาคต. **Safety design:** ค่าเริ่มต้นเขียนไปที่ `userdata/_restore_test` เสมอ (ปลอดภัย, ทดสอบซ้ำได้ไม่จำกัด) — ต้องระบุ `--target-real --confirm` **สองอันพร้อมกัน** เท่านั้นถึงจะเขียนทับ `userdata/warakorn` จริง (กันมือลั่น). วิธีใช้: `python firestore_restore.py --list` (ดูรายชื่อ) / `python firestore_restore.py` (ทดสอบกู้คืนล่าสุดแบบปลอดภัย) / `python firestore_restore.py --date YYYY-MM-DD` (ทดสอบวันที่ระบุ) / `python firestore_restore.py --date YYYY-MM-DD --target-real --confirm` (กู้คืนจริง — ใช้เฉพาะเหตุฉุกเฉินจริงเท่านั้น). **🐛 เจอ+แก้ระหว่างทดสอบ:** รอบแรก crash กลางทางด้วย `UnicodeEncodeError` (Windows console cp874 encode ตัวอักษร `→` ไม่ได้) — พังหลังเขียน test doc สำเร็จแต่ก่อนถึงขั้นตอนลบทิ้ง ทำให้มี document ทดสอบค้างอยู่ใน Firestore ต้องลบมือ 1 ครั้ง; แก้ด้วย `sys.stdout.reconfigure(encoding='utf-8')` ต้นสคริปต์ กันปัญหาเดียวกันครั้งหน้า

- **TDZ:** `let`/`const` ที่ declare หลัง INIT block (`checkLogin()` ~8030) จะ error ถ้า renderDash เรียกก่อน — declare ก่อน `checkLogin()` เสมอ
- **Gemini response:** กรอง thinking parts ด้วย `.filter(p=>!p.thought)` — `gemini-2.5-flash` ส่ง thinking parts มาด้วย
- **ไฟล์ใหญ่:** ต้อง Grep หาตำแหน่งก่อนทุกครั้ง ห้าม Read ทั้งไฟล์ ใช้ offset/limit เสมอ (ไฟล์โตเรื่อยๆ จาก auto-commit — เลขบรรทัดในเอกสารนี้ drift ได้ไว)
- **Firebase config:** เข้ารหัสใน `_ENC` object (บรรทัดยาวมากใกล้ต้นไฟล์) — ไม่ต้องแตะ
- **Single `<script>` block:** บรรทัด 2478–14739 (ปัจจุบัน, จะขยับขึ้นเรื่อยๆ) — อย่า add `<script>` tag ใหม่
- **Admin nav:** แสดงเฉพาะ owner (`currentUser === _ENC.user`) — จัดการโดย `_syncAdminNav()`
- **Dashboard flash fix:** `renderDash` ซ่อน container ก่อน reorder แล้ว restore เพื่อไม่ให้กระพริบ
- **`renderCatChart()` null-check (แก้ 2026-07-06):** เจอ crash จริงผ่านช่อง feedback/error-report ของ user "test1" — `document.getElementById('ch-cat')` คืน null ถ้า canvas ไม่อยู่ใน DOM ตอนฟังก์ชันรัน (`.getContext()` บน null พังทันที); เพิ่ม guard `if(!canvasEl) return;` ก่อนเรียก `getContext` แล้ว — ฟังก์ชันอื่นที่เรียก `getElementById(...).getContext('2d')` ตรงๆ (ch-trend, ch-port ฯลฯ) ยังไม่มี guard เดียวกัน ถ้าเจอ crash ซ้ำให้เติมแบบเดียวกัน
- **Push debug spam ลบแล้ว (2026-07-06):** `_requestPushTokenIfNative()` เคยมีบรรทัด debug ชั่วคราว (ใส่ไว้ 2026-07-02) ที่ยิง `_sendReport({kind:'error',message:'push debug: ...'})` ทุกครั้งที่ login ทำให้ช่อง feedback/error-report ใน Telegram เต็มไปด้วย noise — ลบออกแล้ว ปัญหา push token ไม่เข้า Firestore (ถ้ายังไม่ได้แก้) ต้อง debug วิธีอื่นแทน
- **🐛 target="_blank" ทำแอปมือถือเด้งออกไป Safari ทุกครั้งที่กด privacy/terms (เจอ+แก้ 2026-07-31)** — user รายงานว่าเปิดแอปบน iPhone (TestFlight build) แล้ว "เด้งไปเบราว์เซอร์ตลอด"; วินิจฉัยจาก error report จริงใน Firestore `reports`/Telegram feedback topic (ไม่เจอ error ตรงๆ ที่อธิบายอาการนี้) + ตรวจโค้ด `mobile/src/screens/WebApp.tsx` พบว่า `handleShouldStartLoad` เปิดเบราว์เซอร์นอกเฉพาะ host ที่ไม่ตรง `WEB_HOST` เท่านั้น (ถูกต้องอยู่แล้วสำหรับลิงก์ภายนอกจริง) แต่ไม่มี `onOpenWindow` handler — **react-native-webview (13.15.0) ไม่มี handler นี้ = ลิงก์ที่มี `target="_blank"` (แม้ href เป็นหน้าเดียวกัน/same-origin) จะ fallback ไปเปิด Safari เสมอ** เจอ 5 จุดในไฟล์ที่ href ชี้ privacy.html/terms.html (same-origin) แต่ใส่ `target="_blank"` ผิด: footer หน้า login (2 จุด, เจอทุกครั้งที่เปิดแอปครั้งแรกยังไม่ login), PDPA consent modal (2 จุด, โชว์ทุกบัญชีใหม่/เครื่องใหม่), AI consent modal ที่เพิ่งเพิ่มเมื่อวาน (1 จุด) — ตรงกับอาการ "ตัวทดลอง" (TestFlight ทดสอบบัญชีใหม่ๆ บ่อย) เจอบ่อยเป็นพิเศษ. **แก้แล้ว:** ลบ `target="_blank"` ออกทั้ง 5 จุด (ลิงก์ same-origin ให้ navigate ในหน้าเดิมผ่าน `handleShouldStartLoad` ปกติ ไม่ต้องแก้ native/rebuild). **เจอบั๊กที่สองระหว่างตรวจ error log:** `lockLogout()` (ปุ่ม "ออกจากระบบ" บนหน้าจอ PIN lock) เรียก `logout()` ที่ไม่มีอยู่จริง (`ReferenceError: logout is not defined`, ยืนยันจาก error report จริงของ user `demo_6815` 28 ก.ค.) — ที่ถูกต้องคือ `doLogout()` แก้แล้ว. ทั้งสองจุดเป็น web-only fix deploy ผ่าน GitHub Pages ทันที ไม่ต้อง submit build ใหม่
- **✅ ปุ่ม Google/Apple login เอากลับมาใช้แล้ว 2026-08-10 (เจอสาเหตุ "ค้าง" จริงครบ 3 จุด ไม่ใช่แค่เดา)** — เดิมถอดออกไป 2026-08-03 เพราะ user แจ้งว่าใช้แล้วค้าง ไม่เคยหาสาเหตุจริงตอนนั้น รอบนี้ตรวจโค้ดตรง + Firebase API เจอ 3 สาเหตุจริง:
  1. **บั๊ก Android** — `mobile/src/screens/WebApp.tsx` `startGoogleSignIn()`: error code `statusCodes.IN_PROGRESS` (sign-in ค้างอยู่แล้ว เช่น แตะปุ่มซ้ำเร็วๆ) เดิมทำแค่ `return` เฉยๆ **ไม่เรียก `injectError()`** — ฝั่งเว็บ (`tryGoogleLogin()`) disable ปุ่ม + รอ callback ตลอดไปไม่มี timeout เลย → ปุ่มค้างถาวรจริง ตรงกับอาการที่ user รายงาน. **แก้แล้ว:** inject error ก่อน return เสมอ
  2. **โค้ดเก่าทับโค้ดใหม่แบบเงียบๆ (สาเหตุใหญ่ที่สุด)** — index.html เคยมี `function handleNativeGoogleError(code){...}` เวอร์ชันเก่า (จาก `AndroidBridge` API รุ่นก่อน, ไม่มีการ re-enable ปุ่มเลย) เป็น **top-level function declaration** — native (`WebApp.tsx`'s `injectedJavaScriptBeforeContentLoaded`) inject `window.handleNativeGoogleError` เวอร์ชันใหม่ (ที่ re-enable ปุ่มถูกต้อง) **ก่อน page โหลด** แต่พอ index.html script รันตามปกติ (หลังจากนั้น) function declaration ของ index.html **overwrite ทับเวอร์ชันของ native เงียบๆ** โดยไม่มี error ใดๆ ให้เห็น — ทำให้ error callback จริงจาก native ไปเรียกโค้ดเก่าที่ไม่ re-enable ปุ่มแทน = ปุ่มค้างทุกครั้งที่เจอ error ไม่ว่าเหตุผลอะไร (ไม่ใช่แค่เคส IN_PROGRESS ข้อ 1). **แก้แล้ว:** ลบ `handleNativeGoogleToken`/`handleNativeGoogleError`/`setupAndroidGoogleBtn` (ของเก่าทั้งหมด, ยืนยันไม่มี caller อื่นเหลือ) + ลบ `window.AndroidBridge` check ใน `tryGoogleLogin()` ออกจาก index.html — ให้ native's injected version ทำงานได้เต็มที่ไม่มีอะไรมาทับ
  3. **Google Sign-In ไม่เคยตั้งค่าให้ใช้ได้บน iOS เลย** — เช็คผ่าน Firebase Management API ตรงยืนยันว่าไม่มี Firebase iOS app ในโปรเจกต์เลย (`iosApps` คืนค่าง่าง), `GoogleSignin.configure()` เดิมส่งแค่ `webClientId` ไม่มี `iosClientId`, `app.json` ไม่มี `iosUrlScheme`. **แก้แล้ว:** สร้าง Firebase iOS app ใหม่ (`app.moneymind.th`, App ID `1:668138190451:ios:8eee8b792d66170ec09c06`) ผ่าน Firebase Management API → ได้ iOS OAuth Client ID `668138190451-j0uc44mu6jhke92tl2njsdgbcb0erqo6.apps.googleusercontent.com` (auto-provisioned เพราะโปรเจกต์เปิด Google Sign-In ไว้แล้ว) → เพิ่ม `iosClientId` เข้า `GoogleSignin.configure()`, เพิ่ม `mobile/GoogleService-Info.plist` (commit แยกด้วยมือเหมือน `google-services.json` เดิม เพราะสร้างผ่าน Bash ไม่ผ่าน auto-commit hook), แก้ `app.json` plugin `@react-native-google-signin/google-signin` จาก bare string → array พร้อม `{"iosUrlScheme":"com.googleusercontent.apps.668138190451-j0uc44mu6jhke92tl2njsdgbcb0erqo6"}`
  4. **เพิ่ม safety net ที่ web-side ด้วย** (กันเคสที่ยังไม่รู้จักในอนาคต ไม่ใช่แค่ 3 บั๊กที่เจอวันนี้) — `tryGoogleLogin()`/`tryAppleLogin()` เพิ่ม `setTimeout` 25 วิหลัง `postMessage` เช็คว่าปุ่มยังค้าง `disabled` อยู่ไหม (ถ้า callback จริงมาก่อนแล้วจะ re-enable ไปแล้ว เช็คนี้จะ no-op) — ถ้ายังค้างจริงหลัง 25 วิ ให้ re-enable ปุ่ม + โชว์ "หมดเวลาเชื่อมต่อ ลองใหม่อีกครั้ง" เอง ไม่ต้องรู้สาเหตุล่วงหน้าก็กู้คืนได้. **Apple Sign-In (iOS) พบว่าสร้างไว้ครบแล้วตั้งแต่ 19 ก.ค.** (`startAppleSignIn()` ใน WebApp.tsx) — เอกสารเดิมที่บอกว่า "ยังไม่ได้สร้าง" ล้าสมัย. **คืนปุ่มกลับหน้า login** (ไม่ใช่ form-register ซึ่งไม่เคยมีปุ่มพวกนี้มาก่อน) ลบ class `ios-hide` ออก (ทั้ง Apple bridge และ Google iOS config พร้อมแล้ว). Bump version 1.3.1→**1.3.2** (iOS buildNumber 12, Android versionCode 10) — build ผ่าน EAS แล้วทั้ง 2 แพลตฟอร์ม (iOS build finished สำเร็จ, ยืนยันว่า config ใหม่ไม่ทำ native build พัง) **รอ user ทดสอบบนอุปกรณ์จริงก่อนส่งเข้า store** (browser automation ควบคุม native Google/Apple sign-in sheet ไม่ได้ ต้องพึ่งอุปกรณ์จริง)
  - **🐛 เจอสาเหตุที่ 4 ทันทีที่ user ทดสอบจริงบนเว็บ (2026-08-10, ไม่ใช่แค่ 3 ข้อบนที่เจอตอน implement):** user ลองกดปุ่ม Google บน `app.moneymindth.com` จริงแล้วแจ้ง "login ไม่ได้" — เช็ค console error ย้อนหลังเจอ `auth/unauthorized-domain` (4 ครั้งติดกัน ตรงเวลาที่ user ทดสอบ) — **`app.moneymindth.com` ไม่เคยอยู่ใน Firebase Auth authorized domains list เลย** (เช็คผ่าน Identity Toolkit Admin API ตรง: มีแค่ `localhost`/`moneymind-d97f3.firebaseapp.com`/`moneymind-d97f3.web.app`/`warakorn456.github.io` — โดเมนจริงที่ user ใช้อยู่ทุกวันหายไปเลย). สาเหตุ: โดเมนนี้เพิ่งย้ายมาใช้จริงช่วง ก.ค. (จาก GitHub Pages เดิม) แต่ไม่มีใครเคยกด Google/Apple login บนโดเมนนี้เลยตั้งแต่ย้าย (ปุ่มถูกถอดไปตั้งแต่ 3 ส.ค. ก่อน bug นี้จะมีโอกาสโผล่) — **login ปกติ (username/password) ไม่ต้องพึ่ง authorized domains เลยไม่เคยมีปัญหา** เป็นบั๊กเฉพาะ Google/Apple OAuth เท่านั้น. **แก้แล้ว:** PATCH `authorizedDomains` เพิ่ม `app.moneymindth.com` เข้าไปผ่าน Identity Toolkit Admin API (`identitytoolkit.googleapis.com/admin/v2/projects/moneymind-d97f3/config`) — ทดสอบซ้ำจริงแล้วกดปุ่ม Google ไม่ขึ้น `unauthorized-domain` อีก (ปุ่มเข้า state "กำลังเปิด Google Sign-In..." ถูกต้อง ไม่ error ทันทีเหมือนก่อนแก้). **บทเรียน:** ทุกครั้งที่เพิ่ม custom domain ใหม่ให้แอปที่ใช้ Firebase OAuth (Google/Apple/etc.) ต้องเช็ค/เพิ่มเข้า Authorized domains ด้วยเสมอ ไม่ใช่แค่ตั้ง DNS/CNAME ฝั่ง hosting — สองเรื่องนี้แยกกันคนละระบบ
  - **🐛 เจอสาเหตุที่ 5 — Apple Sign-In บน iOS จริงขึ้น "ไม่รองรับบนอุปกรณ์นี้" ทั้งที่เครื่องรองรับแน่นอน (เจอ+แก้ 2026-08-10/11 ต่อเนื่อง):** ส่ง build 1.3.2 (12) เข้า TestFlight ให้ user ทดสอบจริงบนไอโฟน — กด Apple Sign-In แล้วขึ้น "Apple Sign-In ล้มเหลว: Apple Sign-In ไม่รองรับบนอุปกรณ์นี้" ทั้งที่เป็นไอโฟนรองรับ 5G (ต้อง iOS 14+ ซึ่งเกิน iOS 13 ที่ `AppleAuthentication.isAvailableAsync()` ต้องการมาก) — วินิจฉัยด้วยการดึง EAS build log จริง (`eas build:view --json` → decode Brotli-compressed log ผ่าน node `zlib.brotliDecompressSync`, URL หมดอายุใน 15 นาทีต้องดึงใหม่ทุกครั้งก่อนโหลด) grep หา `ExpoAppleAuthentication` ใน Xcode build log **เจอ 0 ครั้งทั้งไฟล์** (เทียบกับ `RNGoogleSignin` ที่เจอ 106 ครั้ง, `GoogleSignIn` SDK 236 ครั้ง — ยืนยันว่า pod Google compile ปกติ แต่ Apple ไม่เคยถูก compile เข้า build เลย) — **สาเหตุจริง:** เจอใน `RUN_EXPO_DOCTOR` phase ของ build log ตรงๆ ว่า `expo-apple-authentication` version **57.0.1** ถูกติดตั้งไว้ (ผิดปกติมาก) ทั้งที่ Expo SDK 54 ต้องการ `~8.0.8` — mismatch ข้าม major version ขนาดใหญ่ (57.x ไม่ใช่เวอร์ชันจริงที่ SDK 54 รองรับเลย) ทำให้ native module compile/link ไม่ผ่านแบบเงียบๆ (build โดยรวมไม่ fail เพราะ `expo doctor` แค่ warn ไม่ block, แต่ pod ตัวนี้หายไปจริงจากผลลัพธ์สุดท้าย) — JS bridge เลยรายงาน "ไม่รองรับ" เพราะ native module ไม่เคย register เข้า Expo Modules Registry จริงๆ ไม่ใช่เรื่อง device/iOS version เลย. **แก้แล้ว:** `npx expo install expo-apple-authentication --fix` (ดึงเวอร์ชันที่ตรงกับ SDK อัตโนมัติ) → ได้ `~8.0.8` ถูกต้อง + เจอ mismatch เล็กอีกจุด (`expo` เอง patch version ~54.0.36 expected vs 54.0.34 found) แก้ด้วย `npx expo install expo --fix` เช่นกัน — ยืนยันด้วย `npx expo install --check` ว่า "Dependencies are up to date" ครบ. Bump iOS buildNumber 12→**13** build ใหม่ + ส่ง TestFlight รอบ 2 (ยังไม่ยืนยันผลจริงบนเครื่อง รอ user ทดสอบซ้ำ). **บทเรียนสำคัญ:** `npm install <pkg>` ตรงๆ (ไม่ผ่าน `npx expo install`) เสี่ยงดึง version ที่ SDK ปัจจุบันไม่รองรับมาโดยไม่รู้ตัว โดยเฉพาะ Expo unimodule packages ที่มี native code — **ต้องใช้ `npx expo install <pkg>` เสมอสำหรับ Expo/native packages ไม่ใช่ `npm install` ตรงๆ** และควรรัน `npx expo install --check` เป็นประจำ (หรือก่อน build ทุกครั้งที่ touch dependencies) เพื่อจับ mismatch แบบนี้ตั้งแต่ก่อน build แทนที่จะมารู้ตัวหลัง user เจอ bug จริงบนมือถือ
- **🐛 Maya ตอบ "รับทราบครับ" เฉยๆ ไม่ตอบคำถามจริง (เจอ+แก้ 2026-08-04)** — user ถาม "เดือนนี้เติมน้ำมันไปเท่าไหร่" ได้คำตอบแค่ "รับทราบครับ" (แถมเพศผิดด้วย — Maya เป็นตัวละครหญิง ทักทาย "สวัสดีค่ะ" แต่ fallback ใช้ "ครับ"); เจอสาเหตุที่ `mayaAgent()` (~บรรทัด 14211) — Gemini (ผ่าน OpenAI-compat endpoint) บางครั้งตอบกลับมาแบบไม่มี `tool_calls` และ `content` ว่างเปล่าเลย (ทั้งที่ system prompt สั่งชัดว่าคำถามยอดหมวดหมู่ต้องเรียก `search_transactions` เสมอ) — โค้ดเดิม `if(!calls||!calls.length) return (m.content||'').trim()||'รับทราบครับ'` เจอ content ว่างก็ fallback ไปข้อความ hardcode ทันทีโดยไม่ลองใหม่เลย. **แก้:** เพิ่ม retry 1 ครั้งเมื่อเจอ content ว่าง+ไม่มี tool call (push user message เตือนให้ตอบ/เรียก tool แล้ว `continue` loop เดิม, กิน 1 ใน 5 step ที่มีอยู่แล้วไม่ต้องเพิ่ม cap) — ถ้ายัง fail อีกรอบค่อย fallback เป็นข้อความขอโทษที่บอกให้ลองถามใหม่ (ไม่ใช่ "รับทราบครับ" ที่ทำให้งงว่าตอบอะไร) + แก้ "ครับ"→"ค่ะ/นะคะ" ทั้ง 2 จุด (fallback ว่างเปล่า + fallback "ทำรายการซับซ้อนเกินไป" ท้ายฟังก์ชัน) ให้ตรงเพศตัวละคร. **ยังไม่รู้ root cause แท้จริงว่าทำไม Gemini ไม่เรียก tool** (อาจเป็น model quirk ของ `tool_choice:'auto'` กับ system prompt ยาว) — retry เป็นการลดผลกระทบ ไม่ใช่แก้ต้นตอ ถ้าเจอปัญหานี้บ่อยอาจต้องพิจารณาเปลี่ยนเป็น `tool_choice:'required'` สำหรับคำถามที่เข้า pattern คำถามข้อมูล หรือปรับ system prompt ให้สั้น/ชัดกว่านี้
- **🐛 หุ้นที่ชื่นชอบดึงราคาไม่ได้เป็นแถว (เจอ+แก้ 2026-08-04)** — user รายงานพร้อม screenshot: บัญชีจริงมีหุ้นในลิสต์ ~19 ตัว ส่วนใหญ่ขึ้น "ดึงราคาไม่ได้" สีแดง มีแค่ไม่กี่ตัวสำเร็จ (NVDA/PLTR/FLY). สาเหตุ: `getPriceFor()`→`yhFetch()`→`proxyFetch()` (index.html ~6283) พึ่ง **free public CORS proxy 4 เจ้า** (allorigins.win/corsproxy.io/codetabs.com/thingproxy) เพราะเบราว์เซอร์เรียก Yahoo Finance ตรงไม่ได้ (CORS) — proxy ฟรีพวกนี้มี rate-limit ต่ำ ส่วน `_loadFavStockPrices()` เดิมยิง request ให้ทุกหุ้นรัวๆ ไม่มีหน่วงเวลาเลย (`for...of` + `await` ต่อกันไม่มี delay) พอมีหุ้นเยอะ (~19 ตัว) ก็ชน rate-limit ของ proxy เกือบหมด. **แก้ 3 จุด (รอบแรก, client-side only):** (1) เพิ่ม cache ราคาไว้ 90 วิ (`_favStockPriceCache`) กัน re-fetch ซ้ำทุกครั้งที่ `renderInv` ทำงาน (2) หน่วง 400ms ระหว่างคิวหุ้นแต่ละตัว (3) การ์ดที่ fail กดที่ข้อความ "ดึงราคาไม่ได้" เพื่อลองใหม่เฉพาะตัวได้ (`_retryFavStockPrice`). **รอบแรกแก้ได้ไม่ 100%** (ยังพึ่ง third-party proxy ฟรีที่ไม่การันตี uptime อยู่ดี) — user ขอ "แก้แบบไม่เสียเงิน" ต่อ นำไปสู่รอบสอง
- **✅ รอบสอง — ย้ายไปดึงราคาผ่าน VM ของเราเอง แทน CORS proxy สาธารณะ (แก้จริง 2026-08-04, ยังฟรี ไม่มีค่าใช้จ่ายเพิ่ม)** — ใช้ `ai_proxy.py` (Flask บน VM, systemd `moneymind-ai-proxy`, รันอยู่แล้วสำหรับ AI subscription proxy) เพิ่ม endpoint ใหม่ **`POST /v1/quote`** — รับ `{symbols:[...]}` (จำกัด 50 ตัว/request) → ดึงราคาจริงด้วย **`yfinance`** ฝั่ง server (ตัวเดียวกับที่บอท Telegram ใช้อยู่แล้วทั่วทั้งโปรเจกต์ ไม่ต้องติดตั้งอะไรใหม่) → คืน `{quotes:{SYM:{price,currency}}}` ต่อ request เดียวจบทุกตัว (ไม่ใช่ยิงทีละตัวเหมือน CORS-proxy chain เดิม) — auth แค่ Firebase ID token ที่ login ถูกต้อง (`verify_id_token()`) **ไม่เช็ค subscription/quota** เพราะเป็นฟีเจอร์ฟรี ไม่ใช่ AI. **Client-side:** เพิ่ม `QUOTE_PROXY_URL` (ผูก IP เดียวกับ `MAYA_PROXY_URL`/`CHECKOUT_URL`/`FINANCIALS_PROXY_URL` (เพิ่ม 2026-08-04 ค่ำ) — **แก้ทั้ง 4 จุดพร้อมกันถ้า VM IP เปลี่ยน**) + `_fetchQuotesBatch(symbols)` (POST พร้อมกันทีเดียว, คืน `{}` เงียบๆ ถ้า fail — ไม่ throw กัน UI พัง) + `_quoteToThb(q)` (แปลงสกุลเงินผ่าน `getFxRate()` เดิม) — ทั้ง `_loadFavStockPrices()` และ `refreshAllInvPrices()` (auto-refresh หน้า Investments หลักทุก 2 นาที ~บรรทัด 7320) ตอนนี้เรียก batch proxy **ก่อน** เป็นทางหลัก แล้ว fallback ไปเชนเดิม (`getPriceFor`→CORS proxy 4 เจ้า) เฉพาะ symbol ที่ proxy ใหม่ resolve ไม่ได้ (เช่น crypto ที่ proxy ยังไม่รองรับ) — **ของเดิมไม่ได้ถูกลบ เป็น fallback safety net** ถ้า VM proxy ล่มเมื่อไหร่ก็ยังพอใช้งานได้บางส่วนเหมือนเดิม ไม่ regression. **บั๊กแถมที่เจอระหว่างทำ (แก้ไปด้วย):** `ai_proxy.py` เดิม `ALLOWED_ORIGIN` (ตัวแปรเดี่ยว, ใช้ตรวจ CORS ทุก endpoint รวม `/v1/chat`/`/v1/generate`/`/v1/checkout` เดิม) hardcode แค่ `warakorn456.github.io` **ไม่เคยอัปเดตตอนย้ายไปโดเมนจริง `app.moneymindth.com`** (ย้ายตั้งแต่ต้น ก.ค.) — แปลว่า endpoint AI subscription proxy ทั้งหมดน่าจะโดน CORS บล็อกเงียบๆ มานานสำหรับทุกคนที่ใช้เว็บผ่านโดเมนจริง (ไม่มีใครสังเกตเพราะ error แค่ใน console ไม่มีข้อความชัดเจน). แก้เป็น `ALLOWED_ORIGINS` (tuple 2 โดเมน: `warakorn456.github.io` + `app.moneymindth.com`) + `_cors()` echo origin ที่ตรงกลับแทน hardcode ค่าเดียว. **ทดสอบยืนยันแล้วทั้งหมด:** curl `OPTIONS /v1/quote` คืน `Access-Control-Allow-Origin` ตรงทั้ง 2 โดเมนถูกต้อง, ทดสอบจริงบนบัญชี production (`app.moneymindth.com`, ~39 หุ้นใน favStocks) ราคาทุกตัว resolve สำเร็จผ่าน proxy ใหม่ไม่มีตัวไหน fail, ไม่มี console error. **ข้อจำกัดที่เหลือ:** ราคาผ่าน `yfinance` server-side อาจหน่วง/ผิดพลาดได้เหมือนกันถ้า Yahoo เปลี่ยน schema (เคยเกิดกับบอทมาก่อน) แต่ไม่ใช่ปัญหา rate-limit ของ third-party ฟรีอีกต่อไป — เสถียรกว่าเดิมมาก เพราะเป็น request เดียว batch ทั้งหมดจาก IP เดียว (VM) ไม่ใช่หลาย browser session แชร์ rate-limit เดียวกันของ CORS proxy สาธารณะ
- **✅ ปิด auto-refresh ราคาทุก 2 นาที เปลี่ยนเป็นปุ่มกดดึงเองแทน (2026-08-07)** — ตอนตรวจสุขภาพระบบหลังบ้านพบว่า `ai_proxy.log` มี error `possibly delisted` ซ้ำๆ ไม่จบสำหรับกองทุนรวมไทยบางกอง (เช่น สลากออมสิน, ES-SET50RMF, K-WORLDX) — Yahoo Finance ไม่มีข้อมูล NAV กองทุนไทยเลยตามโครงสร้าง (ไม่ใช่บั๊ก แค่ไม่มีข้อมูลจริง) แต่ `refreshAllInvPrices()` เดิมพยายามดึงซ้ำทุก 2 นาทีตลอดเวลาที่อยู่หน้า Investments แบบไม่มีวันจบ เปลืองโดยไม่จำเป็น (ยิง request ไป VM proxy + fallback CORS chain ซ้ำๆ สำหรับ symbol ที่รู้อยู่แล้วว่าจะ fail). **แก้:** ลบกลไก auto-refresh ทิ้งทั้งหมด (`_invRefreshTimer`/`_invCountdownTimer`/`INV_INTERVAL`/`_invNextAt` — ไม่มีตัวแปรพวกนี้เหลือในไฟล์แล้ว) เหลือแค่ `refreshAllInvPrices()` ที่เรียกเมื่อกดปุ่มใหม่ **"🔄 ดึงราคา"** (`#inv-btn-refresh`, ข้าง Rebalancing ในหน้าพอร์ต/DCA) เท่านั้น — `startInvAutoRefresh()`/`stopInvAutoRefresh()` **คงชื่อฟังก์ชันเดิมไว้** (ยังถูกเรียกจากหลายจุดตอน nav เข้า/ออกหน้า investments) แต่เปลี่ยนเป็น no-op/แค่ตั้งข้อความสถานะเริ่มต้น กันต้องไล่แก้ทุก call site. Label สถานะเปลี่ยนจาก "Live — กำลังโหลด..." เป็น "ยังไม่ได้อัปเดตราคา — กดปุ่มเพื่อดึงราคาล่าสุด" ตอนยังไม่เคยกด และ "อัปเดตล่าสุด HH:MM (ล้มเหลว N)" หลังกด (ตัดคำว่า "Live" ออกเพราะไม่ real-time ต่อเนื่องอีกแล้ว). ทดสอบจริงบน production แล้ว: กดปุ่มดึงราคาสำเร็จ (36 สินทรัพย์, ล้มเหลว 4 ตัวตรงกับกองทุนที่รู้อยู่แล้วว่า Yahoo ไม่มีข้อมูล), ยืนยันไม่มี `setInterval` ค้างอยู่หลัง refresh เสร็จ (`typeof _invRefreshTimer==='undefined'`), i18n key เพิ่ม `inv_refresh_prices` (th/en ตรงกัน 1347=1347)
  - **🐛 รอบแรกซ่อนปุ่มในแท็บหุ้นที่ชื่นชอบ ทำให้ user งงว่า "ไม่มีปุ่มเลย" (เจอ+แก้ต่อทันที 2026-08-07)** — ตัดสินใจตอนแรกว่าซ่อน `#inv-btn-refresh` บนแท็บ favstocks เพราะคิดว่ามีปุ่มดึงราคาแยกของตัวเองอยู่แล้ว แต่จริงๆ **แท็บ favstocks ไม่เคยมีปุ่มแบบนี้เลย** (มีแค่ `_loadFavStockPrices()` ที่ทำงานอัตโนมัติตอน render แท็บ ไม่มี UI ปุ่มให้กดเอง) — user ส่ง screenshot มาว่าไปที่แท็บ "หุ้นที่ชื่นชอบ" แล้วไม่เห็นปุ่มดึงราคาเลยตามที่บอกไว้. **แก้:** เปลี่ยน `switchInvTab()` จาก "ซ่อนปุ่มตอน favstocks" เป็น **โชว์ปุ่มทุกแท็บเสมอ** (portfolio/dca/favstocks) แค่สลับ `onclick` handler ให้ตรงกับแท็บ — favstocks เรียก `_loadFavStockPrices()` ตรงๆ (เคารพ cache 90 วิเดิมอยู่แล้ว ไม่ fetch ซ้ำถ้าข้อมูลยังสดอยู่ ปลอดภัยเรียกซ้ำได้), portfolio/dca เรียก `refreshAllInvPrices()` เหมือนเดิม — ทดสอบแล้วปุ่มโชว์ถูกต้องทั้ง 3 แท็บ กด "ดึงราคา" บนแท็บ favstocks ได้จริงไม่มี error. **ข้อจำกัดเล็กน้อยที่เหลืออยู่ (ไม่ใช่ bug แต่ควรรู้ไว้):** ป้ายสถานะด้านบน (`#inv-live-label`) เป็น element เดียวใช้ร่วมกันทุกแท็บ — ตอนกด "ดึงราคา" บนแท็บ favstocks ป้ายนี้จะไม่อัปเดตข้อความ (เพราะ `_loadFavStockPrices()` อัปเดตราคาต่อการ์ดหุ้นโดยตรง ไม่ได้ผูกกับป้ายสถานะรวมเหมือน `refreshAllInvPrices()`) — ไม่กระทบการทำงาน แค่ป้ายอาจค้างข้อความเดิมหลังกดบนแท็บนี้
- **🐛 หุ้นไทยไม่ใส่ `.BK` ต่อท้าย → ได้ข้อมูลบริษัทผิดตัวไปเลย ไม่ใช่แค่ error (เจอ+แก้ 2026-08-04 ดึกๆ)** — user รายงานว่า "ข้อมูลเชิงลึก" ของ SCB ไม่ขึ้น + KTB ขึ้นข้อมูล "ไม่ตรง" ต้องการธนาคารกรุงไทย; เช็ค `DB.favStocks` จริงพบว่า symbol ที่บันทึกไว้เป็น `KTB`/`SCB` เฉยๆ (ไม่มี `.BK`) — บน Yahoo Finance ticker `KTB` (ไม่มี suffix) ตรงกับ **Kontoor Brands, Inc.** (บริษัทเสื้อผ้าสหรัฐฯ เจ้าของแบรนด์ Wrangler/Lee, NYSE) คนละบริษัทกับธนาคารกรุงไทยโดยสิ้นเชิง ส่วน `SCB` เฉยๆ ไม่ resolve เป็นบริษัทจริงเลย (`shortName` คืนค่าประหลาดๆ ไม่ใช่ชื่อบริษัท) — ต้องมี `.BK` ต่อท้ายเสมอ (`KTB.BK`→Krung Thai Bank ถูกต้อง, `SCB.BK`→SCB X PCL ถูกต้องแต่งบการเงินไม่ครบเพราะปรับโครงสร้างเป็น SCBX ปี 2565 Yahoo ยังไม่มีข้อมูลย้อนหลังเต็ม — คนละปัญหากับเรื่อง suffix). **แก้ข้อมูลที่มีอยู่แล้วในบัญชีจริงให้ user โดยตรง** (แก้ symbol เป็น `KTB.BK`/`SCB.BK` ผ่าน `saveDB()`) + **แก้ต้นเหตุกันเกิดซ้ำ (2 ชั้น, เพิ่ม 2026-08-04 ดึกๆ รอบ 2 ตามคำขอ user):** (1) เปลี่ยน placeholder ช่อง symbol (`favstock_field_symbol_ph`) เป็น `'เช่น NVDA, AAPL, PTT.BK (หุ้นไทยต้องมี .BK ต่อท้ายเสมอ)'` (th+en) (2) **auto-append `.BK` จริง** — `_onFavStockCategoryChange()` (onchange ของ `<select id="_fs-category">`) เติม `.BK` ให้ symbol ทันทีถ้าเลือกหมวด "หุ้นไทย" และยังไม่มี suffix (เช็ค `symEl.disabled` ก่อนด้วย กันไปแก้ symbol ตอนแก้ไขหุ้นเดิมที่ field ถูกล็อกไว้) + **safety net ซ้ำใน `saveFavStock()`** (เช็ค `category==='thai' && !symbol.endsWith('.BK')` ก่อน save เสมอ) ครอบคลุมทุก order ที่ user เลือกหมวด/พิมพ์ symbol ก่อน-หลังกันได้ (ทดสอบยืนยันทั้ง 2 ทาง: พิมพ์ก่อนเลือกหมวดทีหลัง, เลือกหมวดก่อนพิมพ์ทีหลังไม่ trigger onchange ก็ยังโดน safety net จับ) — ไม่ append ซ้ำถ้ามี `.BK` อยู่แล้ว. **ยังไม่ทำ:** validate ว่า symbol ที่ resolve ได้จริงตรงกับที่ user ตั้งใจหรือเปล่า (เช่น ป้องกันเคส ticker ชนกับบริษัทอื่นในตลาดอื่นแบบ KTB/Kontoor Brands) — ถ้าเจอปัญหาแบบนี้อีกในหมวดอื่น (mega/china) อาจต้องพิจารณา validate ผ่าน `/v1/quote` ก่อน save จริง (เทียบชื่อบริษัทที่ resolve ได้กับที่ user คาดหวัง)
- **🐛 Dashboard widget "Net Worth ตามเวลา" (NW History) มีโค้ด+ข้อมูลจริงครบ แต่ไม่เคยแสดงผลเลยสักครั้ง (เจอ+แก้ 2026-08-05)** — user ขอฟีเจอร์ "ดูกราฟความมั่งคั่งย้อนหลังจนถึงปัจจุบัน" ราวกับไม่มีอยู่ — ตรวจโค้ดพบว่า **มีอยู่แล้วสมบูรณ์**: `snapshotNetWorth()` บันทึกทุกครั้งที่ login (patch บน `enterApp()`), `renderNWHistory()` วาดกราฟจริงจาก `DB.nwHistory` (patch บน `renderDash()`) แต่ container `<div id="dash-nw-history-section" style="display:none">` (บรรทัด ~1435) **มี `display:none` ฝังตายตัวมาตั้งแต่ต้น และไม่มีโค้ดจุดไหนเคย clear ค่านี้เลย** — `el.innerHTML=...` ที่ฟังก์ชันเรียกทุกครั้งแก้แค่ลูก ไม่แตะ `style` ของตัว container เอง จึงซ่อนถาวรไม่ว่าจะมีข้อมูลกี่เดือนก็ตาม คอมเมนต์ข้างๆ ในโค้ดเขียนไว้ว่า `"sections ที่ถอดออก — เหลือ div ว่างกัน JS error"` ยืนยันว่าเป็นการถอดออกจากหน้าจอ**โดยตั้งใจ**ในอดีต (ไม่ใช่บั๊กอุบัติเหตุ) แต่ไม่มีบันทึกใน CLAUDE.md ว่าทำไม/เมื่อไหร่. **ตรวจเจอ container แบบเดียวกันอีก 2 จุดที่ยังไม่ได้แตะ** (ตั้งใจปล่อยไว้ตามเดิม รอ user ตัดสินใจ ไม่ใช่ scope ของคำขอนี้): `dash-catbud-section` (Category Budget Progress, `renderCatBudgetProgress()`) และ `dash-rvv-section` (Recurring vs Variable, `renderRVV()`) — โค้ด render ทั้งคู่ยังทำงานสมบูรณ์เหมือนกัน แค่ไม่เคยโชว์เหมือนกัน ถ้า user อยากได้ฟีเจอร์เหล่านี้กลับมาแค่เอา `style="display:none"` ออกจาก 2 บรรทัดนั้นในบรรทัด ~1437-1438 ก็พอ (ของก็พร้อมใช้ทันที ไม่ต้องเขียนโค้ดใหม่). **แก้เฉพาะ NW History ตามที่ user ขอ:** เปลี่ยน `style="display:none"` → `style="margin-bottom:18px"` (ให้พฤติกรรมเหมือน section อื่นๆ ที่โชว์/ซ่อนเองผ่าน `innerHTML=''` เมื่อไม่มีข้อมูลพอ ไม่ต้อง hardcode display) พร้อม**เพิ่มฟีเจอร์ตามคำขอ**: เอา cap `.slice(-12)` เดิมออก (เดิมโชว์ได้สูงสุดแค่ 12 เดือนย้อนหลัง) เพิ่มปุ่มเลือกช่วงเวลาสไตล์ `.favcat-pill` เดิม (6 เดือน/1 ปี/ทั้งหมด — ค่าเริ่มต้น "ทั้งหมด" ตรงกับคำขอ "ย้อนหลังจนถึงปัจจุบัน") เก็บ state ใน `_nwHistoryRange` (session only เหมือน `_favStockCatFilter`) + เพิ่ม `maxTicksLimit:12` ที่แกน x กัน label ชนกันตอนมีข้อมูลหลายปี + เพิ่ม i18n key `nw_history_title`/`nw_history_6m`/`nw_history_1y`/`nw_history_all` (ของเดิม title เป็น hardcode ภาษาไทยล้วน ไม่เคยผ่าน `tr()` ทั้งที่ CLAUDE.md อ้างว่า Dashboard แปลครบทุก widget แล้ว — เป็นช่องโหว่ i18n ที่ไม่มีใครเจอเพราะ widget ไม่เคยโชว์). **บั๊กแถมที่เจอตอนทดสอบจริง (แก้ไปด้วย):** label ปี พ.ศ. บนแกน x คำนวณผิด — โค้ดเดิม `String(y-2500+43).slice(-2)` ให้ผลลัพธ์ `'31` แทนที่จะเป็น `'69` สำหรับปี 2026 (สูตรคิดเลขผิด ได้ค่าติดลบ) ไม่มีใครเจอเพราะ widget ซ่อนอยู่ตลอด — แก้เป็นเรียก helper `beYear()` เดิม (บรรทัด ~4444, รองรับ EN/TH ถูกต้องอยู่แล้ว) แทน. ทดสอบยืนยัน production จริง: container โชว์แล้ว (`computedDisplay:'block'`), กราฟวาดถูกจากข้อมูลจริง 4 เดือน (พ.ค.-ส.ค. '69 ถูกต้อง), ปุ่มช่วงเวลาสลับได้, ไม่มี console error, i18n sync (1340=1340). **บทเรียน:** เจอ pattern "component พร้อมใช้แต่ container ถูกซ่อนไว้ถาวรไม่มีใครรู้" แบบนี้ — ควรกวาดหา `id="dash-` ทุกตัวที่มี `display:none` ในอนาคตเทียบกับว่ามี render function จริงไหม ก่อนจะสรุปว่า "ฟีเจอร์ไม่มี" ต้องเขียนใหม่ทั้งหมด
- **🐛 Net Worth ในกราฟ "ตามเวลา" ต่ำกว่าความเป็นจริง — สูตรคำนวณคนละสูตรกับตัวเลขบน Dashboard (เจอ+แก้ 2026-08-05 ต่อจากบั๊กด้านบนวันเดียวกัน)** — พอ user เห็นกราฟที่เพิ่ง unhide ก็ทักทันทีว่า "net worth เหลือแค่ 4แสน ทั้งๆที่จริงมี 7แสน"; ตรวจ `snapshotNetWorth()` เทียบกับ `calcNetWorth()` (ตัวที่การ์ด Net Worth บน Dashboard ใช้จริง) พบว่าเป็นคนละสูตรกันเลย: **`calcNetWorth()`** = `invVal + extraAssetsTotal(จากหน้างบดุล) − trackedDebts − extraDebtsTotal` (**ไม่รวมยอดเงินในบัญชีธนาคาร**) ส่วน **`snapshotNetWorth()` เดิม** = `banks + invVal − debts` (**ไม่รวม extraAssets จากงบดุล**) — สองสูตรขาด/เกินคนละตัวกันคนละอย่าง ไม่ใช่แค่ปัดเศษต่าง; ทดสอบจริงบนบัญชี owner: `calcNetWorth()`=฿737,617 (รวม extraAssets ฿290,000, banks ฿7,399 ไม่นับ) vs `snapshotNetWorth()` เดิม=฿455,016 (รวม banks ฿7,399, extraAssets ฿290,000 ไม่นับ) — **ส่วนต่าง ~฿282,601 เท่ากับ extraAssets ลบ banks พอดี** ตรงกับที่ user รายงานเป๊ะ. **แก้:** เปลี่ยน `snapshotNetWorth()` ให้เรียก `calcNetWorth().netWorth` ตรงๆแทนคำนวณเองซ้ำ (DRY — เลิกมีสูตรคู่ขนานที่เพี้ยนออกจากกันได้อีก, "ตอนนี้" กับ "ย้อนหลัง" การันตีว่ามาจากสูตรเดียวกันเสมอในอนาคต) **⚠️ แก้ได้แค่เดือนปัจจุบันเท่านั้น** (สั่ง `snapshotNetWorth()`+`renderNWHistory()`+`saveDB()` ตรงๆ ยืนยันเดือน ส.ค. เปลี่ยนจาก 433,519→737,617 ถูกต้องแล้วในทั้ง local และ Firestore) — **เดือน พ.ค./มิ.ย./ก.ค. ที่บันทึกไว้ก่อนแก้ยังคงต่ำกว่าจริง** (คำนวณด้วยสูตรเก่าตอนนั้น ไม่มีทางย้อนไปรู้ extraAssets ของเดือนก่อนๆ ได้แม่นยำ) **ยังไม่ได้ backfill/แก้ย้อนหลังให้** — ถ้า user อยากได้เดือนเก่าแม่นขึ้นด้วย ทางเลือกคือประมาณด้วย extraAssets ปัจจุบัน (สมมติว่าไม่เปลี่ยนมาก, เป็นการเดา) หรือปล่อยไว้ตามจริง (ข้อมูลก่อนหน้าเชื่อถือได้แค่ระดับเทรนด์คร่าวๆ ไม่ใช่ตัวเลขแม่นยำ) — ยังไม่ได้ถามหรือทำเอง รอ user ตัดสินใจ
- **🐛 `[Firebase] save: หมดเวลา` ที่เจอซ้ำๆ หลายรอบระหว่าง session นี้ = false alarm ไม่ใช่ save จริงล้มเหลว (เจอสาเหตุจริง 2026-08-05)** — error นี้เจอมาแล้วหลายครั้งตลอด session (2:37, 2:47, และตอนแก้บั๊กนี้เอง) เคยสงสัยว่าอาจเกี่ยวกับที่ user รายงาน "เข้าเว็บไม่ได้"; วันนี้เรียก `saveDB()` ตรงๆแล้วเจอ error เดิมอีกครั้ง **แต่พออ่าน document กลับจาก Firestore เทียบดูจริงๆ พบว่าข้อมูลเซฟสำเร็จแล้ว** (`2026-08:737617` ขึ้นทั้ง local และ remote) — แปลว่า `_fs.collection('userdata').doc(...).set()` **เขียนสำเร็จบน Firestore จริง** แค่ client-side `Promise.race([...set(...), _fsTimeout(8000)])` ใน `fsSave()`/`fsLoad()` (index.html ~2828/2871) แข่งกับ timeout 8 วิเดิมไม่ทัน (เครือข่ายช้ากว่านั้นเล็กน้อยในบางครั้ง — เช็คขนาด DB แล้วแค่ 322KB ไม่ใกล้ limit 900KB/1MB เลย ไม่ใช่ปัญหาขนาดเอกสาร) จึง throw timeout error แล้วขึ้น toast "ซิงค์ไม่ได้" ทั้งที่ข้อมูลไปถึง Firestore แล้วจริงๆ — เป็น **false-positive error** ที่ทำให้ user เห็นข้อความน่ากังวลบ่อยเกินจำเป็นทั้งที่ไม่มีอะไรเสียหาย (เข้าข่ายอธิบายได้ว่าทำไม user รู้สึกเหมือนแอปมีปัญหา/"เข้าเว็บไม่ได้" ทั้งที่โหลดหน้าได้ปกติทุกครั้งที่ตรวจสอบ). **แก้:** เพิ่ม `_fsTimeout` จาก 8000ms → 20000ms ทั้ง 2 จุด (`fsSave()`+`fsLoad()`) ให้เวลาเครือข่ายมากขึ้นก่อนถือว่า fail จริง (ไม่กระทบ UX เพราะเป็น background async ไม่ block การใช้งานอยู่แล้ว) **ยังไม่ยืนยัน 100% ว่านี่คือสาเหตุที่แท้จริงของ "เข้าเว็บไม่ได้" ที่ user รายงานก่อนหน้านี้** (ทุกครั้งที่ตรวจสอบเว็บเองก็โหลดได้ปกติ ไม่เคยเจอ "เข้าไม่ได้" จริง) — แต่เป็นข้อสงสัยที่มีหลักฐานสนับสนุนมากที่สุดตอนนี้ ถ้า user ยังเจอปัญหาอีกหลัง deploy รอบนี้ควรถามอาการเจาะจงเพิ่ม (ข้อความ error ที่เห็นจริง, browser/แพลตฟอร์ม) ต่อ
- **🐛 เลื่อนหน้าจอฝืด/กระตุกบนแอปมือถือ — น่าจะเกิดจาก `backdrop-filter` เยอะเกินจาก Liquid Glass redesign (เจอ+แก้ 2026-08-05)** — user รายงาน "เลื่อนขึ้น-ลงไม่ค่อยลื่น ฝืดๆ" บนแอปมือถือ (WebView ห่อเว็บนี้ตรงๆ ตามหัวข้อ "Mobile App" ด้านบน) — grep พบ `backdrop-filter` **32 จุดทั่วไฟล์** จากการ rollout ธีม "Liquid Glass" ล่าสุด (ตาม git log ใกล้สุดก่อน session นี้: "Roll out Liquid Glass style to the whole app" ฯลฯ) ทุกการ์ด/topbar/sidebar ใช้ `blur(18-28px) saturate(160-180%)` ซึ่งเป็น CSS ที่แพงมากต้อง recomposite ทุกเฟรมเวลาเลื่อน โดยเฉพาะ `.topbar` (`position:sticky`) ที่ค้างอยู่บนจอตลอดเวลาเลื่อน — มือถือ/WebView มี GPU budget น้อยกว่าเดสก์ท็อปมาก ยิ่งมีการ์ดเบลอซ้อนกันหลายใบพร้อมกัน (เช่นหน้า Dashboard) ยิ่งหนัก. **แก้:** เพิ่ม `@media(max-width:700px)` block ใหม่ (index.html ~บรรทัด 529, ต่อจาก breakpoint มือถือเดิม) ลด `backdrop-filter` ของ `.topbar`/`.sidebar`/การ์ด Liquid Glass ทั้งชุด (`.card`,`.stat-card`,`.chart-card` ฯลฯ) เหลือ `blur(6px)` แบบ `!important` (override selector ผสม dark/light mode หลายชุดพร้อมกันโดยไม่ต้องเขียนซ้ำทุก selector) — ยังพอเห็นเป็นกระจกฝ้าอยู่บ้างแต่เบากว่าเดิมมาก ไม่ได้ตัดธีมออกทั้งหมด. **ทดสอบ:** ยืนยันด้วย `document.styleSheets` ว่า rule ถูก parse ถูกต้องและ syntax ไม่พัง (brace balance เช็คแล้ว, ไม่มี console error ใหม่) — **แต่ resize browser จำลองมือถือในสภาพแวดล้อม automation นี้ทำไม่ได้จริง** (`resize_window` ไม่มีผลกับ viewport ที่วัดได้) ยังไม่ได้ทดสอบความลื่นจริงบนอุปกรณ์มือถือจริง/DevTools responsive mode — ถ้า user ลองแล้วยังฝืดอยู่ อาจต้องลดเพิ่มอีก (เช่น blur(3px) หรือ 0 ไปเลยบางจุด) หรือพิจารณาสาเหตุอื่นร่วมด้วย (เช่น JS ทำงานหนักระหว่าง scroll, จำนวน DOM element เยอะในบางหน้า)
- **🐛 ล็อกอินบัญชี owner บนเครื่องใหม่ (เจอกรณีจริง: แอป iOS) เจอ "ข้อมูลของคนอื่น" — จริงๆ คือข้อมูลสำรองเก่าของตัวเอง + local storage ถูก "ฝัง" ข้อมูลผิดถาวร (เจอ+แก้ 2026-08-05)** — user ส่ง screenshot: ล็อกอิน username `warakorn` ถูกต้อง (ชื่อขึ้นตรง) แต่ตัวละคร/level/พอร์ตที่โชว์ไม่ตรงกับบัญชีจริงเลย (Gold Lv.4/8 พอร์ต ฿14,703 ตัวละครหญิง แทนที่จะเป็น Platinum Lv.5/8 พอร์ต ~฿891,836 ตัวละครชาย) พร้อม toast แดง "Sync ไม่ได้: Missing or insufficient permissions" — ลองออกจากระบบแล้วล็อกอินใหม่ก็ยังเป็นเหมือนเดิม. **วิเคราะห์โค้ด `tryLogin()` owner branch (index.html ~3286-3313) พบสาเหตุจริง 2 ชั้นซ้อนกัน:**
  1. **ข้อมูล "ผิด" ที่เห็นคือ snapshot สำรองของ owner เอง ฝังอยู่ในตัวไฟล์ `_ENC.data`** (เข้ารหัสด้วยรหัสผ่าน owner, ถอดรหัสได้ตอน login) — เป็นกลไก bootstrap ให้เข้าแอปได้แม้ยังต่อ Firestore ไม่ได้ (เช่น เครื่องใหม่/ออฟไลน์) แต่ snapshot นี้ **เก่ามาก ไม่เคยอัปเดตอัตโนมัติ** (นิ่งอยู่ ณ ตอนที่ฝังไฟล์ครั้งล่าสุด — เห็นได้จาก Lv./พอร์ตที่เล็กกว่าปัจจุบันมาก) — **ไม่ใช่บัญชีคนอื่นแต่อย่างใด** เป็นความเข้าใจผิดของ user เพราะข้อมูลดูไม่คุ้นเลย
  2. **บั๊กจริง: `ensureAuthForUser(_ENC.user,'',pass)` (เชื่อมต่อ Firebase Auth เพื่อดึงข้อมูลจริงมาทับ snapshot) ผลลัพธ์ไม่เคยถูกเช็คเลย** — ถ้า sign-in ล้มเหลว (เครื่องนี้ต่อ Cloud ไม่ได้ ไม่ว่าเหตุผลอะไร) โค้ดเดิมปล่อยผ่านเงียบๆ ไม่มี error ใดๆ ให้เห็นนอกจากตัวหนังสือเล็ก "ซิงค์ไม่ได้" ที่มุมล่างซึ่งมองข้ามง่ายมาก — ทำให้ user เข้าใจว่าแอปทำงานปกติทั้งที่กำลังโชว์ snapshot เก่ามาก. **แถมมี bug ซ่อนอีกชั้น:** เดิม `else saveLocalOnly()` เซฟ snapshot เก่านี้ลง `localStorage` **ทุกครั้งที่ไม่มี local cache อยู่ก่อน ไม่สนว่า auth สำเร็จหรือไม่** — พอ auth ล้มเหลวครั้งแรก snapshot เก่าจะถูก "ฝัง" ลง `mm_data_warakorn` ถาวรบนเครื่องนั้น; ครั้งถัดไปที่ล็อกอิน (แม้ auth จะสำเร็จแล้วก็ตาม) โค้ดจะเจอว่า local cache "มีอยู่แล้ว" แล้วเข้า branch `loadDB()` โหลด snapshot เก่าซ้ำอยู่ดี — **อธิบายได้ตรงว่าทำไม logout แล้ว login ใหม่ไม่ช่วยอะไรเลย** เพราะ `doLogout()` (บรรทัด ~3452) ไม่เคยลบ `mm_data_<user>` ออกจาก localStorage เลย ลบแค่ session pointer เฉยๆ. **แก้ 2 จุด:** (1) เช็คผลลัพธ์ `ensureAuthForUser()` แล้วขึ้น `toast` แดงชัดเจน 10 วิ ถ้าเชื่อมต่อ Cloud ไม่ได้ บอกตรงๆ ว่าอาจเห็นข้อมูลสำรองเก่า ไม่ใช่ล่าสุด (2) เปลี่ยน `else saveLocalOnly()` เป็น `else if(_authCred) saveLocalOnly()` — persist snapshot ลงเครื่องเฉพาะตอน auth สำเร็จจริงเท่านั้น กัน "ฝัง" ข้อมูลผิดถาวรตั้งแต่ต้น. **ยังไม่รู้สาเหตุที่แท้จริงว่าทำไม `ensureAuthForUser` ล้มเหลวบนไอโฟนเครื่องนั้นโดยเฉพาะ** (เครือข่าย/WebView-specific Firebase Auth quirk/หรืออื่นๆ — ไม่มีทาง debug จากระยะไกลได้ในตอนนี้) — สิ่งที่แก้ไปคือทำให้ "มองเห็นปัญหา" และ "ไม่ฝังปัญหาให้แย่ลง" ไม่ใช่แก้ต้นเหตุที่แท้จริง 100%. **workaround เร่งด่วนสำหรับเครื่องที่ข้อมูลถูกฝังไปแล้วก่อนแก้นี้:** ต้องลบแอปแล้วติดตั้งใหม่ (เครื่องนั้นเฉพาะ — ลบ WebView storage ทั้งหมดจริงๆ) เพราะเป็นแอป iOS ห่อ WebView ไม่มีทาง "ล้าง site data" ผ่าน UI ปกติเหมือนเบราว์เซอร์ — **ย้ำ: ต้องเป็นการติดตั้งใหม่จริงๆ ไม่ใช่ restore จาก iCloud backup** เพราะ backup อาจดึงข้อมูลเก่ากลับมาอีก
  3. **⚠️ ตามต่อ 2026-08-07: user ยืนยันลบแอป+ติดตั้งใหม่จาก App Store จริง (ไม่ใช่แค่ logout/login) แล้ว "ยังเป็นเหมือนเดิมทุกอย่าง" (Gold Lv.4, ฿14,703)** — สำคัญมาก เพราะ**ตัดทฤษฎี "local storage ถูกฝังข้อมูลผิด" ทิ้งไปได้เลย** (ติดตั้งใหม่ = WebView storage ว่างเปล่า 100% ไม่มีทางมี `mm_data_warakorn` ค้างอยู่) แปลว่า `ensureAuthForUser()`/`signInWithEmailAndPassword('warakorn@moneymind.local',pass)` **ล้มเหลวซ้ำๆ แบบ reproducible บนอุปกรณ์/บัญชีนี้จริง** ไม่ใช่ network blip ชั่วคราว. **ตรวจแล้วตัดทฤษฎี "รหัสผ่านไม่ตรงกับ Firebase Auth" ออก** — บัญชี owner **ปิดการเปลี่ยนรหัสผ่านจากหน้าเว็บไปเลย** (`renderChangePasswordSection()` ~บรรทัด 12406 เช็ค `isOwner` แล้วโชว์แค่ข้อความ "บัญชีหลักไม่รองรับการเปลี่ยนรหัสผ่านจากหน้านี้" ไม่มีฟอร์มให้กรอกเลย) จึงไม่มีทางที่รหัสผ่าน Firebase Auth จะเพี้ยนไปจากรหัสที่เข้ารหัส `_ENC` ไว้ผ่านฟีเจอร์นี้ (ยกเว้นเคยใช้ "ลืมรหัสผ่าน"/`sendPasswordResetEmail` ซึ่งต้องมี real email ผูกไว้ — `warakorn@moneymind.local` เป็น synthetic ไม่มีกล่องจดหมายจริง ไม่น่าจะโดนเผลอรีเซ็ตทางนี้ได้ แต่ยังไม่ได้ตรวจ 100%). **เพิ่ม diagnostic logging (2026-08-07)** เพราะ error code จริงจาก Firebase (`auth/network-request-failed`? `auth/too-many-requests`? `auth/user-disabled`? ฯลฯ) **ไม่เคยถูกเก็บไว้เลย** — `catch(e){}` ว่างเปล่าทุกจุดใน loop sign-in (index.html ~3029-3038 เดิม); ปัญหาคือ `_sendReport()` (ที่ปกติใช้ส่ง error เข้า Firestore `reports`→Telegram) **ต้อง authed() ถึงจะเขียนได้** — เป็น chicken-and-egg พอดีตอน auth พังจะรายงานผ่านทางนี้ไม่ได้เลย. **แก้:** เพิ่มตัวแปร `_lastEnsureAuthError` (module-level, ประกาศก่อน `ensureAuthForUser()` กัน TDZ) เก็บ `${e.code}: ${e.message}` จาก catch block ทุกจุดในลูป sign-in/create-user แล้วโชว์ต่อท้าย toast แดงตอน owner login (index.html ~3315) เป็น "...(เหตุผล: auth/xxx: ...)" ให้ user แคปหน้าจอส่งมาได้ทันทีรอบหน้าที่เจอ
  4. **🔍 2026-08-07 รอบบ่าย — user ส่ง screenshot ใหม่ 2 รูป (หลัง fix ข้อ 3 deploy แล้ว, ยืนยันด้วย `curl` ว่า production มีโค้ดใหม่จริง) แต่ toast ใหม่ที่เพิ่งเพิ่ม "ไม่ขึ้นเลย"** — เห็นแค่ toast เดิม 2 อัน: "โหลด cloud ไม่ได้: Missing or insufficient permissions." (จาก `fsLoad()`) และ "☁️ Sync ไม่ได้: Missing or insufficient permissions." (จาก `fsSave()`/`fsListen()`) — **ไม่เห็น** toast ใหม่ "⚠️ เชื่อมต่อ Cloud ไม่ได้..." เลยทั้งที่ตั้ง duration ไว้ 15 วิ (นานกว่า toast อื่นมาก ถ้าขึ้นจริงต้องเห็น) — **หมายความว่า `ensureAuthForUser()` สำเร็จจริง (`_authCred` เป็น truthy) รอบนี้** ไม่ใช่ sign-in ล้มเหลวแบบที่คิดไว้แต่แรก! เปลี่ยนทิศทางการสืบสวนทันที — ปัญหาไม่ได้อยู่ที่ "sign-in ไม่ผ่าน" แต่อยู่ที่ "sign-in ผ่านแต่ Firestore ปฏิเสธการอ่าน/เขียนข้อมูลอยู่ดี" **ตรวจ `firestore.rules` จริง (root repo) พบ:** `ownsAccount(userId)` ต้องการ `request.auth.uid == members/{userId}.uid` **เป๊ะๆ** ไม่มีทาง fallback อื่น + คอมเมนต์ในไฟล์เอง (บรรทัด 37-38) ยืนยันว่า **เคยมี exception ให้ userId=='warakorn' เขียนได้แบบไม่ต้อง auth เลย (ไว้ self-heal uid mismatch ได้) แต่ถูกตัดออกไปแล้วเพราะเป็นช่องโหว่ความปลอดภัย — ไม่มี self-heal path ใดๆ เหลืออยู่เลยสำหรับเคส "sign-in ผ่านแต่ uid ไม่ตรงกับที่ผูกไว้"**. **หลักฐานสนับสนุนเพิ่ม:** query `members/warakorn` ตรงผ่าน Firestore REST (SA token, บายพาส rules) พบ `uid: bgYl3ODPqPh2b9wXivivWLAgvOj1`, **`updateTime`/`lastSeen` ค้างที่ 2026-08-05T09:47 ไม่ขยับเลยแม้ user เพิ่งเปิดแอปพยายาม login วันนี้ (2026-08-07) หลายรอบ** — `_startHeartbeat()`→`_sendHeartbeat()` (เรียกทุกครั้งที่ `enterApp()` + ทุก 5 นาที) เขียน `members/{user}.lastSeen` ด้วย `.set(...,{merge:true}).catch(()=>{})` ซึ่งก็ต้องผ่าน `ownsAccount()`/`unclaimed()` เหมือนกัน — ถ้า uid ปัจจุบันไม่ตรงกับที่ผูกไว้ การเขียนนี้จะ fail เงียบๆ (catch ว่าง) ตรงกับที่เห็น (lastSeen ไม่ขยับเลย) **สอดคล้องกับทฤษฎี uid mismatch 100%**. **สรุปทฤษฎีปัจจุบัน (ยังไม่ยืนยัน 100% เพราะไม่มีสิทธิ์ Firebase Auth Admin API เพื่อดู uid จริงของ session บนไอโฟน — SA `firestore-rest` มีแค่ `roles/datastore.user` ไม่พอ ลองขอ scope กว้างขึ้นแล้วโดน `400 INSUFFICIENT_PERMISSION` ที่ระดับ IAM role ไม่ใช่แค่ scope):** Firebase Auth account `warakorn@moneymind.local` บนเซสชันปัจจุบันของไอโฟนเครื่องนี้น่าจะมี uid **ไม่ตรง** กับ `bgYl3ODPqPh2b9wXivivWLAgvOj1` ที่ผูกไว้ใน `members/warakorn` (เป็นไปได้ว่าบัญชีถูกลบ+สร้างใหม่ที่ไหนสักจุดในอดีต ทำให้ email เดิมได้ uid ใหม่ แต่ document เก่ายังชี้ uid เก่า) — **เพิ่ม diagnostic รอบ 2:** `fsLoad()` catch block (index.html ~2918) ตอนนี้แนบ `_auth.currentUser.uid` ปัจจุบันต่อท้าย toast error ด้วย (เช่น "...Missing or insufficient permissions. [uid: xxxxx]") ให้เทียบกับ uid ที่ query ได้จาก `members/warakorn` ตรงๆ ได้ทันทีรอบหน้าที่ user แคปหน้าจอมา — **ถ้า uid ที่โชว์ไม่ตรงกับ `bgYl3ODPqPh2b9wXivivWLAgvOj1` = ยืนยันทฤษฎีนี้ 100% แก้ได้ทันทีด้วยการสั่ง SA เขียนทับ `members/warakorn.uid` ให้ตรงกับ uid ใหม่** (Firestore rules บล็อกแค่ client เขียนเอง ไม่บล็อก SA token) — **รอ user แคปหน้าจอ toast ใหม่รอบถัดไป**
  5. **✅ แก้เสร็จสมบูรณ์ 2026-08-07 — ยืนยัน root cause 100% ด้วย Firebase Auth Admin API จริง (ไม่ต้องรอ screenshot รอบใหม่):** ใช้ `gcloud auth print-access-token` ของบัญชี owner (`warakornbest6@gmail.com`, role `roles/owner` บนโปรเจกต์ `moneymind-d97f3`) เรียก Identity Toolkit `accounts:query` โดยตรง (ต้องแนบ header `x-goog-user-project: moneymind-d97f3` ไม่งั้นโดน 403 "quota project ไม่ถูกตั้ง") — เจอว่า **uid จริงของ `warakorn@moneymind.local` คือ `7oxtLgXJiifQM0Ww9Sq9klZ0zrz1`** (ตรงกับที่เคยบันทึกไว้ใน CLAUDE.md ส่วน Firestore Security Rules ด้านบนว่า "owner Firebase account = `warakorn@moneymind.local` uid `7oxtLgXJ...`" — ถูกต้องอยู่แล้วแต่ document จริงเพี้ยนไป) **และ session ล่าสุดของ uid นี้คือ 2026-08-07T01:41:30Z ตรงกับเวลาที่ user พยายาม login จากไอโฟนเป๊ะ** — ยืนยันชัดว่า sign-in สำเร็จจริงเสมอมา ปัญหาไม่เคยอยู่ที่ auth เลย. ส่วน `bgYl3ODPqPh2b9wXivivWLAgvOj1` ที่ผูกผิดอยู่ใน `members/warakorn.uid` กลับเป็น uid ของบัญชี **`demo@moneymindth.com`** (บัญชี demo คนละตัวโดยสิ้นเชิง ไม่เกี่ยวกับ owner) — **สงสัยว่าเกิดจาก session ทดสอบก่อนหน้านี้ในระหว่างพัฒนา** ที่ authenticate เป็นบัญชี demo อยู่ แล้วมีจุดที่ set `currentUser='warakorn'` (เช่น debug/testing ผ่าน console) ชนกับ branch ใน `ensureAuthForUser()` ตอนที่ `existingUid` ยังว่างอยู่ (`unclaimed`) → เขียน uid ของ session ปัจจุบัน (demo) ทับเข้า `members/warakorn` โดยไม่ตั้งใจ — ไม่ใช่บั๊กจาก user เลย เป็นผลข้างเคียงจากการทดสอบ. **แก้:** `PATCH members/warakorn` field `uid` กลับเป็น `7oxtLgXJiifQM0Ww9Sq9klZ0zrz1` ผ่าน SA token (บายพาส rules ได้ เพราะ SA มี `roles/datastore.user`) — ยืนยัน `updateTime` เปลี่ยนเป็น 2026-08-07T02:19:01Z สำเร็จ. **ผลที่คาดว่าจะเกิด:** ครั้งถัดไปที่ user เปิดแอป (ไม่ต้องลบ/ติดตั้งใหม่ซ้ำ แค่เปิดแอปตามปกติ) `ensureAuthForUser()` จะ sign-in ด้วย uid `7oxtLgXJ...` เหมือนเดิม แต่คราวนี้ `ownsAccount()` จะผ่านเพราะ `members/warakorn.uid` ตรงกันแล้ว → `fsLoad()`/`fsSave()`/`fsListen()` ควรทำงานปกติ เห็นข้อมูลจริง (Platinum Lv.5/8, พอร์ต ~฿891,836) แทน snapshot เก่า. **✅ ยืนยันแล้วจาก user (2026-08-07 09:31 ตาม screenshot ถัดมา) — เข้าได้แล้วจริง เห็นข้อมูลจริง (เข้าหน้า Admin Dashboard ได้ เห็น 21 users/usage จริงทั้งหมด) แก้สำเร็จสมบูรณ์.** **บทเรียนสำคัญสำหรับ session ทดสอบในอนาคต:** ห้าม spoof/ตั้งค่า `currentUser` เป็นชื่อบัญชีจริงของ user (โดยเฉพาะ owner) พร้อมกับมี Firebase Auth session ของบัญชีอื่นค้างอยู่ — จุดตัดกันแบบนี้เขียนทับ `members/{u}.uid` ผิดได้แบบเงียบๆ ไม่มี error ให้เห็นทันที กว่าจะรู้ตัวคือ user จริงเข้าระบบไม่ได้ไปแล้ว; ถ้าต้องทดสอบ auth-related flows ควรใช้บัญชีทดสอบแยกทั้ง `currentUser` และ Firebase session ให้ตรงกันเสมอ ไม่ผสมกัน
  6. **🐛 2026-08-07 (ต่อทันทีหลังแก้ login) — Admin Dashboard หน้าจอ "หลุดขอบ" บนมือถือ** — พอ user login สำเร็จครั้งแรกแล้วเปิดหน้า Admin Dashboard บนไอโฟน (จอแคบ) พบว่า layout ล้นขอบจอ (บล็อกกราฟ "ผู้ใช้สะสม" + panel "แพลตฟอร์ม" เรียงข้างกันแทนที่จะซ้อนกัน ดันความกว้างรวมเกิน viewport). **สาเหตุ:** `renderAdmin()` (index.html ~12776) generate div ด้วย **inline style `grid-template-columns:1fr 260px` ตรงๆ ไม่มี class** — ต่างจาก grid อื่นในไฟล์ (เช่น `.tax-grid`,`.port-grid`,`#pg-forecast .page-body>div`) ที่ล้วนมี class + mobile override ใน `@media(max-width:700px)`/`@media(max-width:1100px)` ครบ จุดนี้จุดเดียวที่หลุดรอด เพราะ inline style ชนะทุก class selector ปกติ (ไม่มี class ให้ media query จับ). **แก้:** เพิ่ม `class="admin-growth-grid"` เข้า div นั้น (ไม่ลบ inline style เดิม เพื่อไม่กระทบ desktop) + เพิ่ม `.admin-growth-grid{grid-template-columns:1fr!important}` ใน `@media(max-width:700px)` block เดิม (index.html ~483, ต้อง `!important` เพราะเป็นการทับ inline style) — ตรวจสอบ grid-template-columns แบบ fixed-px อื่นๆ ทั่วไฟล์แล้ว (`grep grid-template-columns.*px`) ที่เหลือทั้งหมดใช้ `auto-fit`/`auto-fill`+`minmax()` (responsive เองอยู่แล้ว ไม่ต้องแก้) หรือมี mobile override อยู่แล้ว ยกเว้นจุดนี้จุดเดียว. **ยังไม่ทดสอบจริงบนมือถือ** (รอ user ยืนยันหลัง deploy)

- **📊 วิเคราะห์ churn จริงจากข้อมูล Firestore (`members`+`userdata`) แล้วเจอสาเหตุตัวจริง + แก้แล้ว (2026-08-07)** — user ขอให้วิเคราะห์ว่าทำไม stickiness ถึงต่ำ (churn 53%, stickiness 13% ตาม Admin Dashboard) ก่อนไปหาผู้ใช้ใหม่. **ขั้นแรกที่พลาดง่าย: ต้องกรอง test/demo account ออกก่อน** — ดึง `members` ทั้ง 21 บัญชีมาดู พบว่า **14 จาก 21 บัญชีเป็นบัญชีทดสอบ/เดโม่ชัดเจน** (`test1`-`test5`, `testuser`, `testuser123`, `testerios`, `demo`, `demo_6815`, `best_05`, `traveltestuser`, `warakorn`, `warakorn_k` — ส่วนใหญ่สร้างเพื่อทดสอบฟีเจอร์ระหว่างพัฒนา ไม่ใช่ user จริง) เหลือ **user จริงแค่ 6-7 บัญชี** (`ahafiz`,`firstsurachet`,`saowakhon`,`tobtobtob301163`,`wannaporn`,`yok07`,`yok2026`) — ตัวเลข churn/stickiness ที่เห็นใน Admin Dashboard **ถูกปนเปื้อนด้วยบัญชีทดสอบหนักมาก ไม่ได้สะท้อนพฤติกรรม user จริงตรงๆ** (ควรพิจารณากรอง test account ออกจากสถิติ Admin Dashboard ในอนาคตด้วย ยังไม่ได้ทำรอบนี้). **ดึง `userdata` ของ 6 user จริงมาดูว่าทำอะไรไปบ้าง (จำนวน transaction ที่มี) เจอ pattern ชัดเจนมาก:** 4 ใน 6 คน (`firstsurachet`,`saowakhon`,`ahafiz`,`yok2026`) **มี transaction = 0 รายการ** ทั้งที่บางคนเปิดแอปกลับมาหลายวัน (`ahafiz` เปิดต่อเนื่อง 4 วัน, `yok2026` เปิดต่อเนื่อง 8 วัน) — แปลว่า**ปัญหาไม่ใช่ "ใช้แล้วเลิก" แต่เป็น "ไม่เคยทำสำเร็จตั้งแต่ครั้งแรกเลย"** (activation failure ไม่ใช่ retention failure) มีแค่ `tobtobtob301163` (สมัคร 6 ส.ค., ยังใช้งานต่อเนื่องถึงวันนี้) ที่ activate สำเร็จจริง (11 tx, 9 investments, 1 savings goal). **ตามรอยไปดูจุดที่ทำให้ activate ไม่สำเร็จ:** `_showWelcomeOnboard()` (index.html ~15829, modal 4 ขั้นตอนหลัง register) step "1. บันทึกรายการแรก" เดิมแค่ `nav('transactions')` — ไปถึงหน้า transactions แล้ว **ยังต้องกดปุ่ม "+เพิ่มรายการ" อีกทีถึงจะเห็นฟอร์มจริง** (2 คลิกกว่าจะถึงฟอร์ม ทั้งที่ดูเหมือนกดครั้งเดียวจบ) เช่นเดียวกับปุ่มในการ์ด Empty State Dashboard (`renderDash` patch "2. EMPTY STATE — DASHBOARD" ~บรรทัด 16124, ปุ่ม "บันทึกรายจ่าย"/"บันทึกรายรับ") ที่เดิมก็ทำแค่ `nav('transactions');setTxType('expense')` — `setTxType()` แค่ตั้งค่า type ของฟอร์มที่**ยังไม่เปิด**ไว้ล่วงหน้า ไม่ได้เปิด modal ให้เลย. **แก้:** เปลี่ยนทั้ง 3 จุด (step 1 ของ welcome onboarding + ปุ่ม "บันทึกรายจ่าย"/"บันทึกรายรับ" ใน empty-state dashboard) ให้เรียก **`openTxModal({type:'expense'})`/`openTxModal({type:'income'})` ตรงๆ** แทน `nav()`+`setTxType()` — `openTxModal(pre)` เป็น modal ที่อยู่ใน DOM ทุกหน้าอยู่แล้ว (เหมือน `openImgModal()` สแกนใบเสร็จ) เปิดได้จากทุกที่โดยไม่ต้อง nav ก่อน ลดเหลือ 1 คลิกจริงๆ ตั้งแต่กดจนเห็นฟอร์มพร้อมกรอก (มี auto-focus ช่องจำนวนเงินให้ด้วยอยู่แล้วใน `openTxModal`). **พิจารณาแล้วไม่เลือกดันปุ่ม "สแกนใบเสร็จด้วย AI" เป็นทางเลือกหลักแทน** ทั้งที่เป็นจุดขายเด่นของแอป — เพราะฟีเจอร์นี้ต้องมี Gemini API key ของตัวเอง หรือ subscription ที่ใช้งานจริงอยู่ (ซึ่งยังไม่เปิดขายจริงกับ user ทั่วไป รอ Omise KYC) — ผลักดันเป็นทางหลักตอนนี้จะพา user ใหม่ส่วนใหญ่ไปเจอทางตัน (error "need_apikey_or_sub") แทนที่จะช่วย. **ทดสอบแล้วในเบราว์เซอร์จริง:** เรียก `openTxModal({type:'expense'})` ตรงๆ จากหน้า dashboard เปิด modal ถูกต้อง (type ตรง, ไม่มี error) + เปิด `_showWelcomeOnboard()` แล้วกด step 1 ปิด overlay + เปิดฟอร์มบันทึกรายการทันทีในคลิกเดียว ยืนยันด้วย screenshot จริงทั้ง 2 เคส ไม่มี console error. **ยังไม่ได้ทำ:** กรอง test account ออกจากสถิติ Admin Dashboard, วัดผลจริงว่าการแก้นี้ช่วยลด activation-failure ได้จริงหรือไม่ (ต้องรอ user ใหม่สมัครเพิ่มแล้วดูข้อมูลย้อนหลัง)

## วิธี Deploy

Static file — ไม่ต้อง build:
- เปิด `index.html` ใน browser โดยตรง
- หรือ host บน GitHub Pages, Netlify, Firebase Hosting
- **`index.html` ต้องอยู่ที่ root ของ repo เท่านั้น** — GitHub Pages serve จริงที่ `https://warakorn456.github.io/moneymind/` และแอปมือถือ (`mobile/src/screens/WebApp.tsx` → `WEB_URL`) ชี้ตรงมา URL นี้ ห้ามย้าย `index.html` เข้า subfolder โดยไม่แก้ GitHub Pages source + `WEB_URL` พร้อมกัน

## Growth Plan — เฟส 1 (เพิ่ม 2026-08-07)

ต่อจากเฟส 0 (กรองบัญชีทดสอบออกจากสถิติ + แก้ friction onboarding — ดู Admin gotcha ด้านบน) ทำ 2 ข้อที่ทำเองได้ในเฟส 1 ของแพลนหาผู้ใช้ใหม่ (Artifact `moneymind-growth-plan.html`, เผยแพร่ 2026-08-07) — ข้อที่ 1 ของเฟส (ปรับ screenshot/คำอธิบายบน App Store/Play Store) เป็นงานที่ user ต้องทำเอง ยังไม่ได้ทำ:

- **`landing.html`** (ไฟล์ใหม่ที่ root, ไม่แตะ `index.html`) — หน้า marketing/SEO แยกจากตัวแอป (ตัวแอปเองอยู่หลัง login gate ไม่เหมาะ index ด้วย Google) ธีมเดียวกับ `privacy.html`/`terms.html` (Sarabun, พื้นมืด `#0a0a15`, gradient ม่วง `#6366f1→#8b5cf6`) — hero + feature grid 8 ข้อ (สแกนใบเสร็จ AI/คำนวณภาษี/นำเข้าอีเมลธนาคาร/AI มายา/พอร์ตลงทุน/แผนเกษียณ/เป้าหมายออม/ซิงค์คลาวด์) + 3 ขั้นตอนเริ่มใช้งาน + ปุ่มดาวน์โหลด **badge จริงจาก Apple/Google** (`tools.applemediaservices.com`/`play.google.com` official badge CDN, ไม่ใช่ไอคอนปลอม) ลิงก์ App Store `apps.apple.com/th/app/id6788824042` + Play Store `play.google.com/store/apps/details?id=app.moneymind.th` (ยืนยันทั้งคู่ live จริงก่อนใช้ลิงก์) — มี Open Graph/Twitter Card/JSON-LD (`schema.org/MobileApplication`) + `robots.txt`/`sitemap.xml` ใหม่ (root, `Disallow: /` กัน Google index หน้า login ของแอปเอง แต่ `Allow` เฉพาะ landing/privacy/terms/support) เพื่อดัน SEO value ไปที่ `landing.html` แทน. เข้าถึงได้ที่ `https://app.moneymindth.com/landing.html` — ทดสอบสด production แล้ว โหลดสำเร็จ ปุ่ม/badge/ลิงก์ครบ ไม่มี console error
- **ปุ่ม "แชร์แอปให้เพื่อน"** (sidebar footer, เหนือปุ่ม "แจ้งปัญหา/ขอฟีเจอร์" — เห็นได้ทุกหน้าหลัง login) — `shareApp()` (ก่อน "4. FEEDBACK MODAL" ในโครง script, section ใหม่ "3.5 SHARE APP") ใช้ **Web Share API** (`navigator.share`) เป็นทางหลัก (WebView ทั้ง iOS/Android รองรับเองโดยไม่ต้องสร้าง native bridge เพิ่ม — ต่างจาก Google/Apple Sign-In ที่ต้อง bridge) แชร์ลิงก์ `landing.html` พร้อมข้อความชวนลอง; ถ้า browser ไม่รองรับ (`navigator.share` undefined) → fallback คัดลอกลิงก์เข้า clipboard + toast "คัดลอกลิงก์แอปแล้ว!"; ถ้า clipboard ก็ใช้ไม่ได้อีก (เช่น permission ปฏิเสธ) → toast โชว์ลิงก์ดิบให้คัดลอกเองเป็นทางสุดท้าย. เพิ่ม i18n keys `share_app_btn`/`share_app_message`/`share_link_copied` (TH+EN, ยืนยัน sync 1352=1352). **ทดสอบจริงบน production:** ยืนยัน `navigator.share` มีอยู่จริงในบราวเซอร์ (จะเปิด native share sheet เมื่อกด — ไม่ได้กดจริงเพราะเสี่ยง block automation เหมือน `alert()`/`confirm()`) + ทดสอบ fallback path แยก (stub `navigator.share=undefined`) ยืนยัน clipboard-copy + toast ทำงานถูกต้อง
- **`landing.html` — เอา "solo developer" framing ออกรอบ 8 (2026-08-11)** — user feedback ว่า "พัฒนาและดูแลโดยนักพัฒนาอิสระคนเดียว ไม่ใช่บริษัทใหญ่" (ที่ round 4/6/7 ใส่ไว้เป็น transparency signal) กลับทำให้ดูไม่น่าเชื่อถือแทนที่จะช่วย — แก้ 2 จุด (FAQ "ใครเป็นคนดูแลแอปนี้" + `.dev-note` ใต้ reviews) เปลี่ยนเป็นคำถาม/คำตอบเน้นช่องทางติดต่อโดยตรงแทน ("มีปัญหาหรือข้อเสนอแนะ ติดต่อได้ทางไหน" → "ส่งอีเมลมาได้โดยตรงที่ [email] เราตอบทุกข้อความ...") — **ไม่ได้อ้างทีม/บริษัทที่ไม่มีจริง** แค่ไม่พูดถึงจำนวนคนเฉยๆ (ตรงกับที่สินค้าจริงทั่วไปทำ ไม่ใช่การโกหก) พร้อมปรับข้อความ reviews section ที่พูดซ้ำ "เพิ่งเปิดตัว มีรีวิวน้อยมาก" 2 รอบติดกันให้เหลือรอบเดียวมั่นใจขึ้น (ข้อเท็จจริงเดิมยังอยู่ครบ ไม่ได้ซ่อนอะไร) — deploy ขึ้น production แล้ว
- **`landing.html` — full-page audit + polish รอบ 7 (2026-08-10)** — user ขอวิเคราะห์ทั้งหน้าอย่างละเอียดให้ "ดูดีกว่านี้ น่าเชื่อถือกว่านี้"; audit จากการ screenshot หน้าจริงพบ 2 ปัญหา: (1) trust gap — ไม่มี FAQ ตอบคำถามที่คนลังเลมักถาม (ฟรีจริงไหม/ข้อมูลปลอดภัยแค่ไหน/เลิกใช้เอาข้อมูลออกได้ไหม/ใครดูแล/เป็นที่ปรึกษาการเงินไหม) ทั้งที่คำตอบจริงมีอยู่แล้วใน `privacy.html`/`terms.html` แค่ไม่เคยรวบรวมมาโชว์; (2) visual polish — 5 section ติดกันใช้ "การ์ดมืดบนกริด" แบบเดียวกันหมดจนดูซ้ำ, section รีวิวมีพื้นที่ว่างเยอะดูไม่เสร็จ, marquee pill ใต้ hero contrast อ่อนกว่าองค์ประกอบอื่น. **แก้ 4 จุด:** เพิ่ม `<section id="faq">` (native `<details>/<summary>` ไม่ใช้ JS เลย, 5 คำถาม ทุกคำตอบอ้างอิงข้อเท็จจริงที่มีอยู่แล้วในโค้ด ลิงก์กลับ `#security`/`terms.html#data-ownership`), restructure `#reviews` เป็น `.reviews-inner` สองคอลัมน์ (การ์ดคะแนน + invite text) บน desktop แก้ปัญหาพื้นที่ว่างโดยไม่เพิ่มเนื้อหาปลอม, เพิ่ม background glow บางๆ ใกล้ `#preview`/`#rank` (`::before` radial-gradient ใช้ตัวแปร `--glowb`/`--glowd` เดิม) ให้จังหวะการเลื่อนดูมีมิติขึ้น, เพิ่ม contrast ให้ `.bank-pill`/`.feature-pill` (background `var(--surface2)` + border ม่วงอ่อน + shadow). **ไม่ทำ:** ไม่ใส่รีวิว/สถิติ/สื่อที่แต่งขึ้นเอง — กฎเดิมของทุกรอบ. ทดสอบครบทั้ง 2 ธีมผ่าน `getComputedStyle` (ยืนยัน glow gradient ใช้ตัวแปรถูกต้องในทั้ง dark/light ไม่ใช่แค่ดูด้วยตา), FAQ accordion เปิด-ปิดถูกต้อง (native behavior, chevron หมุน), deploy ขึ้น production แล้วยืนยันสด

**เฟส 2 (เพิ่ม 2026-08-07):** จาก 3 ข้อในเฟส มี 2 ข้อที่ต้องทำเอง (โพสต์จากบัญชีจริง, ติดต่อ YouTuber/TikToker) — ข้อที่ช่วยได้คือร่างคอนเทนต์ให้พร้อมใช้ (Claude ร่าง, user เป็นคนโพสต์เอง ตามที่แพลนวางไว้เพื่อไม่ให้เกิดปัญหาแบบระบบโพสต์อัตโนมัติเดิม) — ร่างโพสต์ Facebook กลุ่มการเงิน (2 แบบ: มุมภาษี/มุม pain point จดบันทึก), กระทู้ Pantip ห้องสินธร (1 แบบ), โพสต์ Reddit r/ThailandFinance (แบบเปิดเผยตัวตนชัดเจนตามธรรมเนียมของ subreddit), แคปชั่นโซเชียล 5 อัน (Facebook Page/IG/TikTok) — เผยแพร่เป็น artifact ส่วนตัว ยังไม่ได้โพสต์จริงที่ไหนเลย (รอ user เลือกกลุ่ม/ปรับคำเป็นสำนวนตัวเอง/โพสต์เอง) — ยังไม่เริ่มติดต่อ YouTuber/TikToker

## Mobile App (`mobile/`)

**รวมเข้า repo นี้แล้ว 2026-07-02** — เดิมเป็น repo แยกที่ `D:\Mobiy\finance-app` (Expo/React Native) ย้ายเข้ามาด้วย `git subtree add --prefix=mobile` (เก็บ git history เดิมทั้ง 6 commits ไว้ครบ ดูได้ด้วย `git log -- mobile/`)

- **สถาปัตยกรรม:** ไม่ใช่แอป native เต็มรูปแบบ — เป็น **WebView wrapper** ที่โหลด `WEB_URL` (`https://app.moneymindth.com/` — custom domain, เปลี่ยนจาก `warakorn456.github.io/moneymind/` เดิมช่วง 2026-07 หลังจดโดเมนและตั้ง CNAME ผ่าน GitHub Pages, ยังชี้ไปไฟล์ `index.html` ตัวเดียวกับ repo นี้) ฟีเจอร์ทั้งหมดอยู่ในเว็บ ฝั่ง native ทำแค่ bridge ที่เว็บ browser ทำเองไม่ได้
- **🐛 Android push notification ลงทะเบียนไม่ได้เลยตั้งแต่แรก — ไม่มี `google-services.json` (เจอ+แก้ 2026-08-07)** — เจอจากช่อง Telegram feedback/error report (`report_relay.py` → topic 907): `push register: ... Default FirebaseApp is not initialized in this process app.moneymind.th. Make sure to call FirebaseApp.initializeApp(Context) first.` — `Notifications.getExpoPushTokenAsync()` (`WebApp.tsx` ~บรรทัด 399) ต้องพึ่ง Firebase Android SDK ตัวจริงที่ฝั่ง native เพื่อขอ FCM registration token ก่อนแลกเป็น Expo push token แต่โปรเจกต์ `mobile/` **ไม่เคยมีไฟล์ `google-services.json` เลย** และ `app.json` ก็ไม่เคยอ้างถึง `android.googleServicesFile` — Firebase SDK เลยไม่มีวัน initialize ได้บน Android ทำให้ push notification บน Android **ไม่เคยใช้งานได้จริงเลยตั้งแต่เพิ่มฟีเจอร์นี้มา** (ไม่ใช่บั๊กเฉพาะเครื่อง/เฉพาะ user คนใดคนหนึ่ง). **แก้:** เช็ค Firebase project (`moneymind-d97f3`) ผ่าน Firebase Management API พบว่ามี Android app ผูก package `app.moneymind.th` จดทะเบียนไว้แล้วจริง (มี SHA-1 2 ตัวสำหรับ Google Sign-In ด้วย) — ดึง config ผ่าน `androidApps/{appId}/config` endpoint (ใช้ gcloud owner token) ได้ `google-services.json` ตัวจริงมาวางที่ `mobile/google-services.json` + เพิ่ม `"googleServicesFile": "./google-services.json"` ใน `app.json` ส่วน `android` — commit แยกต่างหาก (ไฟล์นี้สร้างผ่าน Bash ไม่ผ่าน auto-commit hook ปกติที่ผูกกับ Edit/Write tool เลย push มือ). **API key ใน `google-services.json` (`AIzaSyCgh0...`) ปลอดภัยที่จะ commit เข้า repo** — เป็น Android API key ที่ Google ออกแบบให้ฝังใน APK ได้ตามปกติ (ผูกกับ package name + SHA-1 fingerprint ที่ restrict ไว้แล้ว) คนละแบบกับ Telegram bot token ที่เคยหลุดไปก่อนหน้านี้ (อันนั้นเป็น bearer credential ล้วนๆ ไม่มี restriction ใดๆ). **⚠️ ยังไม่มีผลกับแอปที่ติดตั้งอยู่แล้ว** — ต้อง `eas build` (Android) รอบใหม่ + อัปโหลดขึ้น Play Store ก่อน APK ที่มี Firebase config ตัวนี้ถึงจะไปถึงมือ user จริง (ตอนนี้แค่แก้ source code/config ในโปรเจกต์เท่านั้น ยังไม่ได้ build/submit)
- **ไฟล์หลัก:** `mobile/src/screens/WebApp.tsx` — native bridge ทั้งหมดอยู่ที่นี่:
  - **Google Sign-In** ผ่าน `@react-native-google-signin/google-signin` (native SDK, ไม่ใช้ browser redirect เพราะ Google บล็อก OAuth ใน WebView)
  - **Android back button** — `injectJavaScript` ถาม `window.handleBackButton()` ในเว็บก่อนเสมอ (ปิด modal/nav กลับ) ออกจากแอปเฉพาะเว็บส่ง `{action:'exitApp'}` กลับมา
  - **Export/Print bridge** — `saveFile`/`printHtml` message handler ใช้ `expo-file-system`+`expo-sharing`+`expo-print` เพราะ `<a download>` บน blob URL และ `window.print()` ใช้ไม่ได้ใน WebView
  - `window._isAndroidApp`/`window._isIOSApp` — inject ตาม `Platform.OS` จริง (เพิ่งแก้ 2026-07-02 เดิม hardcode true ทั้ง 2 platform ทำให้ปุ่ม Apple Sign-In ฝั่งเว็บไม่มีวันทำงาน — **Apple Sign-In native bridge ยังไม่ได้สร้างจริง** รอ Apple Developer account activate ก่อน)
  - **🐛 Apple reject 2.1.0 "App launches into a blank screen" (เจอ 2026-07-12, แก้ 2026-07-13)** — ส่ง iOS build แรกเข้า review แล้วโดน reject ทันที ("Bug description: App launches into a blank screen", ทดสอบบน iPhone 17 Pro Max iOS 26.5.1, Internet Connection: Active) สาเหตุที่แท้จริงยังไม่ยืนยัน 100% เพราะ error-reporting pipeline ของเว็บ (`window.addEventListener('error',...)` → Firestore `reports`) **ลงทะเบียนช้าเกินไป** (เดิมอยู่ท้ายไฟล์บรรทัด ~14977 หลัง `checkLogin()`/render UI ไปนานแล้ว) ทำให้ error ที่เกิดช่วง init ต้นๆ (ก่อน login screen โผล่) ไม่มีวันถูกจับ/รายงานเลย ไม่มี log ให้ดูสาเหตุจริง. **แก้ 2 จุด (defense-in-depth ไม่ต้องรู้ root cause ก็ป้องกันได้):** (1) ย้าย global error handler IIFE มาไว้ทันทีหลัง `initFirebase()` (บรรทัด ~2594, เร็วที่สุดเท่าที่ `_fs`/`_sendReport` พร้อมใช้) แทนท้ายไฟล์ — ครอบคลุม error ตลอด `checkLogin()`/init flow แล้ว ถ้าเกิดซ้ำจะมี log จริงให้ดูใน Firestore `reports`/Telegram (2) เพิ่ม **native watchdog timeout** ฝั่ง `WebApp.tsx`: เว็บยิง `{action:'appReady'}` กลับมาทันทีหลัง `checkLogin()` เสร็จ (บรรทัด ~8398 ใน index.html) → native ตั้ง `setTimeout` 15 วิหลัง `onLoadEnd` ถ้าไม่ได้รับ `appReady` ภายในเวลานี้ (เช่น JS crash ก่อน checkLogin ทำงานเสร็จ) จะโชว์หน้าจอ retry ที่มีอยู่แล้วแทนปล่อยจอว่างค้างถาวร — แก้ตรงจุดที่ Apple reject ได้แน่นอนไม่ว่า root cause จริงจะเป็นอะไร. **ยังไม่ได้ยืนยันสาเหตุจริง** เพราะ SSH เข้า VM ดู `report_relay.log`/Telegram archive ถูก autonomous-mode blocker กันไว้ (ไม่ใช่ host ที่ user สั่งชัดเจน) — ถ้า resubmit แล้วโดน reject ซ้ำด้วยอาการเดิม ต้องเข้าไปดู Firestore `reports` collection จริงหลัง fix นี้ deploy (จะมี log ให้เห็นแล้วเพราะย้าย handler มาเร็วขึ้น)
  - **🐛 แอปเด้งออกไป Safari ทันทีที่เปิด ทุกครั้ง (เจอ+แก้ 2026-07-31, build 6) — น่าจะเป็นสาเหตุจริงของ "blank screen" ด้านบนด้วย** — user รายงานว่าเปิดแอป (TestFlight build 5, ยืนยันแล้วว่าไม่ใช่ build เก่า) แล้วเด้งไป Safari ทันที ก่อนแตะอะไรเลย; ส่ง screenshot มาเห็นหน้าเปล่าที่ URL `moneymind-d97f3.firebaseapp.com` — คือ **Firebase Auth SDK's authDomain** (ตั้งใน `_ENC`/`_fbConfig`) ที่ SDK โหลด hidden iframe ไปเองอัตโนมัติทุกครั้งที่ auth เริ่มทำงาน (ทุกครั้งที่เปิดแอป ไม่ต้อง login) เพื่อจัดการ session/redirect-result — **ไม่ใช่ user แตะลิงก์แต่อย่างใด**. `handleShouldStartLoad` ใน `WebApp.tsx` เดิมเช็ค host ของ**ทุก** navigation request (รวม iframe เบื้องหลังที่มองไม่เห็น) แล้วเปิด `Linking.openURL` ออกไป Safari ถ้า host ไม่ตรง `WEB_HOST` — เจตนาเดิมคือกัน user กดลิงก์ข่าว/ลิงก์นอกแอปหลุดออกไป แต่ผลข้างเคียงคือ iframe ภายในของ Firebase เองก็โดนเหวี่ยงออกไปด้วยเหมือนกัน. **แก้:** เช็ค `req.isTopFrame` ก่อนเสมอ — อนุญาต non-top-frame request (iframe) ทุกกรณีแบบไม่ผ่าน host allowlist เลย บล็อกเฉพาะ top-level navigation จริงๆ (`ShouldStartLoadRequest.isTopFrame` มีอยู่ใน type ของ react-native-webview 13.15.0 อยู่แล้ว). **build 5 ที่กำลังรอ Apple ตรวจตอนพบบั๊กนี้ ถูกถอนออกจากการรีวิว** (`ยกเลิกการส่ง` ใน App Store Connect, สถานะเปลี่ยนเป็น "นักพัฒนาได้ปฏิเสธ") ทันทีเพราะมีบั๊กนี้ติดไปด้วย เกือบแน่นอนจะโดน Apple ปฏิเสธซ้ำถ้าปล่อยให้รีวิวต่อ — bump `buildNumber` เป็น 6, `eas build` + `eas submit` (internal testing ไม่ต้องรอ Apple review) แล้ว, ยังไม่ได้ resubmit เข้า App Store review รอบใหม่ (รอทดสอบ build 6 ก่อนว่าหายจริง)
- **`node_modules/`, `.expo/`, `mobile/android`, `mobile/ios`** — gitignored ผ่าน `mobile/.gitignore` (nested, มาพร้อม subtree) ต้อง `cd mobile && npm install` เองก่อนรัน/build
- **`.gitignore` root มี `*.png` (กัน screenshot หลุด commit)** — เพิ่ม exception `!mobile/**/*.png` แล้ว ไม่งั้นไอคอนแอปที่ mobile/assets/ จะโดนกันไม่ให้ commit
- **`D:\Mobiy\finance-app` (repo เดิม) ยังอยู่ ไม่ได้ลบ** — แต่**ถือว่า deprecated แล้ว** ต่อจากนี้แก้ไขที่ `wabbest/mobile/` เป็นหลัก อย่าแก้ 2 ที่พร้อมกันจะทำให้ diverge
- **ไม่ได้เอามาด้วย:** โฟลเดอร์ `download/` (Expo อีกโปรเจกต์ซ้อนอยู่ข้างในของเดิม ไม่เคยถูก commit) และไฟล์ `*.bak` (ของเดิมก็ untracked อยู่แล้ว)

---

## Infrastructure & Integrations

โปรเจกต์นี้เชื่อมต่อกับ external services หลายตัว:

| Service | รายละเอียด | หน้าที่ |
|---------|-----------|---------|
| **n8n** | `localhost:5678` (run ด้วย `npx n8n`) | Workflow automation |
| **Google Sheets** | "MoneyMind" (`1IdfyUExUkkDFK7es4mnUWE5gvXpspHx6PaOJtBR05zg`) | พักข้อมูล bank transactions |
| **Firebase Firestore** | config ใน `_ENC` (ต้นไฟล์ index.html) | Cloud sync ข้ามอุปกรณ์ |
| **GitHub** | repo นี้ | deploy via GitHub Pages |
| **Gmail (OAuth2)** | credential ID `v7EQvnFH4mbTwfHL` ใน n8n | ดัก bank email |
| **Telegram Bot** | `@moneymind_alert_bot` token: **ดูใน `.env` (`MM_TG_TOKEN`)** — ไม่จดตรงนี้อีก (rotate แล้ว 2026-07-02 เพราะ token เก่าโชว์อยู่ใน public repo มานาน) chat_id: `8172260229` | แจ้งเตือนและตอบคำสั่ง |
| **Telegram Group** | MoneyMind Hub — group_id: `-1004296300749` topic หุ้น: thread_id=`3`, topic การเงิน: thread_id=`4`, topic แชทคุย: thread_id=`689` | รับแจ้งเตือนทุก script + AI chat ใน topic 689 |
| **Google Cloud VM** | e2-micro, us-central1-a, IP: `34.134.61.137` (⚠️ ephemeral — เคยเปลี่ยนมาแล้วครั้งหนึ่งจาก `34.16.55.125` ตอน VM restart ทำให้ TLS setup พังไปพักหนึ่งเพราะ domain ผูกกับ IP เก่า **เช็ค IP จริงด้วย `gcloud compute instances describe moneymind-bot --zone=us-central1-a --format="value(networkInterfaces[0].accessConfigs[0].natIP)"` ก่อนอ้างอิงทุกครั้ง** ถ้าจะกันปัญหานี้ถาวรต้อง reserve static IP เพิ่ม) project: `teak-perigee-497404-b7` | รัน Telegram bot 24/7 + AI proxy (Caddy TLS) |

---

## AI Proxy — Gemini key กลางสำหรับ subscription (เริ่ม 2026-07-03, ยังไม่เปิดใช้จริง)

**เป้าหมาย:** แผนอนาคตให้ผู้ใช้ subscription ใช้ AI (Maya chat/สแกนใบเสร็จ/ภาษี) ได้โดยไม่ต้องมี Gemini API key ของตัวเอง — ใช้ key "ของเรา" ผ่าน proxy กลาง (ห้าม key หลุดไปฝั่ง client เด็ดขาด บทเรียนจาก Telegram token เดิม)

**สถานะ:** infra + client wiring + Gemini proxy key + checkout/webhook + auth bug fix พร้อมหมดแล้ว **ทดสอบ end-to-end ผ่านจริงแล้ว 2026-07-06** (subscription active + AI chat ผ่าน proxy ได้คำตอบจริง) **ยังไม่เปิดใช้จริงกับผู้ใช้ทั่วไป** เพราะยังไม่มีระบบรับเงินจริง (Omise KYC submit แล้ว 2026-07-03 รอผลตรวจ, ตอนนี้ใช้ได้แค่ test-mode + manual grant ผ่าน `grant_subscription.py`)

| ส่วนประกอบ | รายละเอียด |
|---|---|
| `ai_proxy.py` | Flask app — endpoint `POST /v1/chat` (pass-through ไป Gemini OpenAI-compat) + `GET /healthz`; source of truth `C:\Users\warakorn\Documents\ai_proxy.py` |
| `/v1/quote` (เพิ่ม 2026-08-04, ไม่ใช่ AI feature) | `POST /v1/quote` `{symbols:[...]}` (max 50) → `yfinance` server-side → `{quotes:{SYM:{price,currency}}}`; auth แค่ Firebase ID token ถูกต้อง (`verify_id_token()`) **ไม่เช็ค subscription/quota** (ฟรีทุกคนที่ login) — ใช้แก้ปัญหา "หุ้นที่ชื่นชอบ"/หน้า Investments ดึงราคาไม่ได้ (เดิมพึ่ง free CORS proxy สาธารณะที่ rate-limit ต่ำ) โดยไม่เสียเงินเพิ่ม (reuse VM+yfinance ที่มีอยู่แล้ว); client เรียกผ่าน `QUOTE_PROXY_URL`/`_fetchQuotesBatch()` ดูรายละเอียดที่หัวข้อ Gotchas ด้านล่าง ("หุ้นที่ชื่นชอบดึงราคาไม่ได้เป็นแถว") |
| `/v1/financials` (เพิ่ม 2026-08-04 ค่ำ, ไม่ใช่ AI feature; ขยายเพิ่ม business profile ค่ำเดียวกันรอบ 2; เพิ่มแปลไทยรอบ 3 ดึกๆ) | `POST /v1/financials` `{symbol:"AAPL"}` (เดี่ยว ไม่ใช่ batch) → `yfinance` `.income_stmt`/`.cashflow` server-side → `{symbol,currency,available,annual:[{year,revenue,netIncome,operatingCF,freeCF}], businessSummary,sector,industry,employees,country,website}` (เรียงปีใหม่→เก่า) หรือ `{symbol,available:false,...profile fields}` ถ้าไม่มีงบการเงิน (กองทุน/ETF/ticker เล็กเกินไป/error — ทดสอบยืนยันแล้วว่า QQQ คืน `available:false` ถูกต้องแต่**ยังมี businessSummary ของกองทุนติดมาด้วย**, AAPL/CRWV(เพิ่ง IPO)/PTT.BK/BABA ได้ข้อมูลจริงครบ); auth เหมือน `/v1/quote` (แค่ login, ไม่เช็ค subscription/quota) เพราะ yfinance ฟรี. **cache แรกในไฟล์ทั้งหมด** (`_financials_cache`, in-memory dict, TTL 24 ชม.). ชื่อ row ใน yfinance DataFrame (`'Total Revenue'`/`'Net Income'`/`'Operating Cash Flow'`/`'Free Cash Flow'`) verify ตรงบน VM จริงแล้วก่อนเขียนโค้ด มี fallback หลายชื่อ (`_REVENUE_KEYS`/`_NET_INCOME_KEYS`/ฯลฯ). **Business profile (เพิ่มรอบ 2):** `_fetch_business_profile(t)` เรียก `t.info` แยกจาก income/cashflow เพราะกองทุน/ETF ก็มี `.info` (คำอธิบายกองทุน) ให้ใช้แม้ไม่มีงบการเงินแบบบริษัท — ดึง `longBusinessSummary`/`sector`/`industry`/`fullTimeEmployees`/`country`/`website`, merge เข้า response เดียวกัน (`dict(profile, symbol=..., ...)`) ไม่แยก endpoint ใหม่; `.info` ช้ากว่า `.fast_info` แต่วัดจริงบน VM แค่ ~0.2-0.4s/ticker ไม่ใช่ปัญหา. **ใช้กับ UI:** ปุ่ม "ดูข้อมูลเชิงลึก" (`fa-chart-column`) ใน `_favStockCard` **แสดงทุกหมวดรวมกองทุน** (เดิมซ่อนถ้า `category==='fund'` เพราะคิดว่าไม่มีข้อมูล — แก้แล้วหลังพบว่า `.info` มีคำอธิบายกองทุนให้ใช้จริง) เปิด `openFavStockDetailModal(id)`/`closeFavStockDetailModal()` (คนละอันจาก `openFavStockModal` เดิมที่เป็น modal แก้ไข/เพิ่มหุ้น) — ส่วนบนสุดโชว์ `_favStockDetailProfileHtml()`: chip แถว sector/industry/employees/country + คำอธิบายธุรกิจเต็ม (ตัดที่ 220 ตัวอักษรพร้อมลิงก์ "อ่านเพิ่มเติม"/"แสดงน้อยลง" ผ่าน `_toggleFsdSummary()`) — **แปลไทยอัตโนมัติ (เพิ่มรอบ 3 ดึกๆ, user เลือกวิธีเอง):** ถามผ่าน `AskUserQuestion` ระหว่าง Google Translate ฟรี vs Gemini มีต้นทุนเล็กน้อย → user เลือก **Google Translate ฟรี** เพื่อคุมต้นทุนเป็นศูนย์ตามธรรมเนียมฟีเจอร์หุ้นทั้งชุดวันนี้; backend เพิ่ม `_translate_to_thai(text)` เรียก `translate.googleapis.com/translate_a/single` (unofficial free endpoint, ไม่ต้อง API key) แบ่งข้อความเป็นชิ้นละ ~450 ตัวอักษรตามประโยคก่อนแปล กันข้อความยาวเกิน/โดนตัด — คืน field ใหม่ `businessSummaryTh` แยกจาก `businessSummary` เดิม (ไม่ทับ) ให้ client เลือกโชว์ตาม `currentLang`: UI ไทย → ใช้ `businessSummaryTh` ถ้าแปลสำเร็จ พร้อม disclaimer "แปลอัตโนมัติจาก Yahoo Finance ด้วย Google Translate — อาจมีคลาดเคลื่อนบ้าง"; UI อังกฤษหรือแปลไม่สำเร็จ → fallback `businessSummary` ต้นฉบับอังกฤษพร้อม disclaimer เดิม "ยังไม่มีคำแปลไทย" — ทดสอบยืนยันคุณภาพคำแปลดี อ่านลื่น (AAPL ~1825 ตัวอักษร แปลเสร็จใน VM ~4-5s รวม fetch งบการเงิน, แปลอย่างเดียว ~0.2-0.9s/ชิ้น) แถมแปลปี ค.ศ.→พ.ศ. ให้อัตโนมัติด้วย (Google Translate ทำเอง ไม่ใช่ logic ที่เขียน). **⚠️ เป็น unofficial endpoint ไม่มี SLA** ถ้า Google บล็อก/เปลี่ยนในอนาคต `_translate_to_thai` จะ fail เงียบๆ (คืน `''`) ไม่ทำให้ทั้ง request ล้ม แค่ fallback ไปอังกฤษเหมือนเดิม — sector/industry/country ยังไม่แปล (เป็นภาษาอังกฤษเสมอ ยังไม่ทำรอบนี้ ขอบเขตจำกัดแค่ businessSummary ตามที่ user ขอ) แล้วต่อด้วย 2 กราฟ Chart.js (กำไรขาดทุน: รายได้รวม vs กำไรสุทธิ, กระแสเงินสด: จากการดำเนินงาน vs อิสระ) ด้วย `_chartTickColor()`/`_chartGridColor()` เดิม + ฟังก์ชันใหม่ `_fmtFinNum()` ย่อเลขใหญ่เป็น K/M/B/T; ถ้า `available:false` ยังโชว์ profile ได้ถ้ามีข้อมูล ก่อนขึ้นข้อความ "ไม่มีข้อมูลงบการเงิน" แทน (ไม่ใช่ empty state เปล่าทั้งหมดเหมือนเดิม). **ต้อง destroy chart ตอนปิด modal ด้วย** (`_fsdIncomeChart`/`_fsdCashflowChart` module-level, destroy+null ใน `closeFavStockDetailModal()` ก่อน `ov.remove()`) — precedent ใหม่ของไฟล์นี้; client cache แยกจากราคาหุ้น (`_favStockFinancialsCache`, TTL 12 ชม.). ทดสอบ end-to-end บนบัญชี production จริง (owner account) ผ่านหมด: AAPL โชว์ chip+summary+กราฟครบ, ปุ่ม "อ่านเพิ่มเติม"/"แสดงน้อยลง" ทำงานถูกต้อง, ไม่มี console error, i18n sync (1335=1335) |
| Auth flow (`/v1/chat`,`/v1/generate`,`/v1/checkout`) | verify Firebase ID token (`google.oauth2.id_token`, ไม่ใช้ firebase-admin SDK) → เช็ค uid ตรงกับ `members/{username}.uid` → เช็ค `subscriptionActive`+`subscriptionExpiry` (field ใหม่ใน `members/{username}` ยังไม่มี user ไหนตั้งจริง) → เช็ค/เพิ่ม quota เดือนละครั้งใน `aiUsage/{username}` (default 200 req/เดือน) → forward Gemini |
| CORS (`ALLOWED_ORIGINS`, แก้บั๊ก 2026-08-04) | เดิมเป็น `ALLOWED_ORIGIN` ตัวแปรเดี่ยว hardcode แค่ `warakorn456.github.io` — ไม่เคยอัปเดตตอนย้ายโดเมนจริงเป็น `app.moneymindth.com` (ก.ค.) ทำให้ endpoint AI proxy ทั้งหมดโดน CORS บล็อกเงียบๆ บนโดเมนจริงมานาน; แก้เป็น tuple 2 โดเมน + `_cors()` echo origin ที่ตรงกลับ — **โดเมนใหม่ในอนาคตต้องเพิ่มเข้า tuple นี้ด้วยเสมอ** |
| Secrets | `mm_secrets.PROXY_GEMINI_KEY` (`.env` → `MM_PROXY_GEMINI_KEY`, **ตั้งแล้ว** — key แยกจาก key ส่วนตัวที่ใช้ทำ Maya/reports, สร้างผ่าน AI Studio 2026-07-03) + `MM_PROXY_MONTHLY_LIMIT` (default 200) |
| Deploy บน VM | `/home/warakornbest6/moneymind/ai_proxy.py` รันผ่าน **systemd `moneymind-ai-proxy`** (`waitress-serve --host=127.0.0.1 --port=8081 ai_proxy:app`, log ที่ `ai_proxy.log`) — bind localhost เท่านั้น ไม่เปิดสู่ภายนอกตรง |
| Reverse proxy + TLS | **Caddy** (`/etc/caddy/Caddyfile`, systemd service `caddy` ติดตั้งจาก apt repo ทางการ) reverse proxy `https://<sslip-domain>` → `127.0.0.1:8081`, ออก cert อัตโนมัติจาก Let's Encrypt (auto-renew) |
| Domain | ใช้ **sslip.io** (ฟรี ไม่ต้องซื้อโดเมน) — `<IP-ขีดคั่น>.sslip.io` เช่น IP ปัจจุบัน `34.134.61.137` → `34-134-61-137.sslip.io` — **⚠️ ถ้า VM IP เปลี่ยนอีก (ephemeral) ต้องแก้ `/etc/caddy/Caddyfile` ให้ตรง IP ใหม่ + `sudo systemctl reload caddy` ไม่งั้น TLS cert ขอไม่ผ่าน (Let's Encrypt ต่อ IP เดิมไม่ได้)** |
| Firewall | GCP firewall rule `allow-ai-proxy-https` (tcp:80,443 จาก 0.0.0.0/0) ผูกกับ network tag `ai-proxy-server` บน VM instance นี้ — เดิม VM เปิดแค่ SSH/RDP เท่านั้น |
| ทดสอบแล้ว | `curl https://34-134-61-137.sslip.io/healthz` → 200 (cert Let's Encrypt จริง, ยืนยันด้วย `openssl s_client`) + `/v1/chat` ไม่มี auth → 401 ถูกต้อง |
| Checkout + Webhook (เพิ่ม 2026-07-03) | เพิ่มเข้า `ai_proxy.py` เดิม (ไม่ตั้ง service ใหม่ ใช้ Caddy/TLS เดิม): `POST /v1/checkout` (สร้าง Omise charge — รองรับ `method: 'card'` ส่ง token, `method: 'promptpay'` สร้าง source ก่อนแล้วสร้าง charge, ราคา `SUBSCRIPTION_PRICE_THB=299`) + `POST /v1/webhook/omise` (Omise เรียกกลับตอน charge เปลี่ยนสถานะ — **ไม่เชื่อ payload ตรงๆ** ดึง charge จริงจาก Omise API ด้วย id เสมอกัน webhook ปลอม) ผูก user เข้า charge ผ่าน `metadata[username]`. Deploy แล้ว + ทดสอบสร้าง PromptPay source จริงผ่าน test key สำเร็จ (`src_test_...`) + `/healthz` คืน `hasOmiseKey:true` |
| Webhook URL (ตั้งแล้ว 2026-07-03) | Omise dashboard → Settings → Webhooks → `https://34-134-61-137.sslip.io/v1/webhook/omise` — ตั้งใน **test mode แล้ว** (ต้องตั้งซ้ำใน live mode ทีหลังตอน KYC ผ่าน เพราะ Omise แยก webhook ของ 2 mode) |
| Client UI (เพิ่ม 2026-07-03) | Settings page ใหม่มี card "AI Subscription" (`#subscription-status-box`, เรียกจาก `renderSubscriptionBox()` ใน `renderSettings()`) แสดงสถานะ subscription หรือปุ่ม "สมัคร ฿299/เดือน"; modal `#m-subscribe` มี 2 ทาง — บัตร (Omise.js `Omise.createToken('card',...)` แล้วส่ง token ไป `/v1/checkout`) และ PromptPay (เรียก `/v1/checkout` ตรง ได้ QR image กลับมาโชว์ + poll `_subPollStatus()` ทุก 3 วิ นาน ~60 วิ รอ webhook confirm). ค่าคงที่ `OMISE_PUBLIC_KEY` (test key, ปลอดภัยฝัง client) + `CHECKOUT_URL` ผูก IP เดียวกับ `MAYA_PROXY_URL` — **แก้ 2 จุดพร้อมกันถ้า VM IP เปลี่ยน**. เพิ่ม `<script src="cdn.omise.co/omise.js">` ใน head. **⚠️ ปิด UI ชั่วคราวแล้ว (2026-08-10)** — user แจ้งว่าฟังก์ชันยังใช้ไม่ได้จริงในเว็บ (Omise ยังไม่ผ่าน KYC/เป็น test-mode) จึงคอมเมนต์ทั้ง card (`.set-card.ios-hide` ที่มี `#subscription-status-box`, index.html ~1854-1864) ออกจาก `renderSettings()` กันคนกดแล้วเจอ flow พัง — **โค้ดข้างในไม่ได้ลบ** (`renderSubscriptionBox()`/`openSubscribeModal()`/modal `#m-subscribe`/backend `/v1/checkout` ทั้งหมดยังอยู่ครบ) แค่เอา comment `<!-- -->` ครอบ card ออก — เปิดกลับได้ทันทีตอน subscription พร้อมใช้งานจริง (รอ Omise KYC ผ่านก่อน ดูหัวข้อ "สิ่งที่เหลือทำก่อนเปิดใช้จริง" ด้านล่าง) |
| **🐛 บั๊ก `window._auth` undefined เสมอ (เจอ+แก้ 2026-07-06)** | `_auth` ประกาศด้วย `let _auth=null` ที่ top-level ของ `<script>` (บรรทัด ~2565) — ใน JS ตัวแปร `let`/`const` top-level **ไม่ผูกเข้ากับ `window` object โดยอัตโนมัติ** (ต่างจาก `var`) แต่โค้ด `_subGetIdToken()` และ `mayaAgent()` proxy path ถูกเขียนให้เช็ค `window._auth`/`window._auth.currentUser` ซึ่ง**เป็น `undefined` เสมอไม่ว่า login สำเร็จแค่ไหน** — ทำให้ subscription checkout และ AI chat ผ่าน proxy พังทั้งคู่มาตั้งแต่เขียนครั้งแรก ขึ้น error "ต้อง login ใหม่" ตลอด แม้ user จะ login ใหม่กี่ครั้งก็ไม่หาย. **แก้แล้ว:** เปลี่ยนทั้ง 2 จุดจาก `window._auth`→`_auth` (เข้าถึงตัวแปรตรงๆ ในสโคปเดียวกันของ script). ทดสอบยืนยันหลังแก้: `_auth.currentUser` มีจริงตลอด (uid ตรง owner account) + เรียก mayaAgent ผ่าน proxy สำเร็จได้คำตอบจริงจาก Gemini. **บทเรียน:** เวลา debug ต้องเช็ค `typeof X` ตรงๆ ในสโคปเดียวกัน ไม่ใช่ `window.X` เพราะ `let`/`const` top-level script variable ไม่ขึ้น window — เคยทำให้วินิจฉัยผิดว่า "ไม่มี Firebase Auth session" ทั้งที่มีอยู่จริงตลอด |
| **🐛 บั๊ก `members/{u}.uid` ค้างไม่ตรงกับ Firebase session จริง (เจอ+แก้ 2026-07-06 บ่าย)** | ทดสอบ proxy path ใหม่ (สแกนใบเสร็จ/ภาษี) ในเบราว์เซอร์จริงแล้วเจอ `403 username/uid mismatch` — เช็คแล้วพบว่า **มายาแชทผ่าน proxy ที่เพิ่งทดสอบผ่านตอนเช้าวันเดียวกันก็พังด้วย error เดียวกัน** (ไม่ใช่บั๊กใหม่จากโค้ดที่เพิ่งเขียน) เทียบ Firestore `members/warakorn.uid` (`w03QiziB...`) กับ `_auth.currentUser.uid` จริงในเบราว์เซอร์ (`7oxtLgXJ...`) พบว่าไม่ตรงกัน — สาเหตุที่แท้จริงยังไม่สรุปชัด (สงสัยว่ามี session/login อื่นเขียนทับ `lastSeen`+`uid` ทีหลัง). **แก้ชั่วคราว:** เรียก `ensureAuthForUser(currentUser,'',''`) ตรงๆ ผ่าน console — ฟังก์ชันนี้ที่แอปเรียกอยู่แล้วทุกครั้งที่ login ถ้ามี `_auth.currentUser` อยู่แล้วจะแค่ merge `uid` ปัจจุบันทับ Firestore (ไม่ต้องรหัสผ่าน) → resync สำเร็จ ทดสอบยืนยันทั้ง `/v1/chat` (มายา, คืน "test-ok" HTTP 200) และ `/v1/generate` (สแกน, คืนคำตอบจริงจาก Gemini) ผ่านทั้งคู่หลัง resync. **✅ เจอสาเหตุจริง + แก้ถาวรแล้ว 2026-07-16:** สาเหตุ**กลับด้าน**จากที่เดา — ไม่ใช่ "มีอะไรเขียน uid ผิดทับ" แต่คือ path Google login (`_handleOAuthResult` ฝั่งเว็บ + `handleNativeGoogleToken` ฝั่ง Android native) อัปเดตแค่ `lastSeen` **แต่ไม่เคยเขียน `uid`** → พอ user ที่สมัครด้วย password (uid_A) มากด "Login with Google" อีเมลเดียวกัน Firebase สร้าง**คนละ account คนละ uid** (uid_B, เพราะโปรเจกต์ตั้ง multiple-accounts-per-email) แต่ `members/{u}.uid` ค้างเป็น uid_A ของ password → session (uid_B) ≠ stored (uid_A) → 403 (warakorn โดนเพราะสมัคร password แล้วมา login Google). **ทำไม resync เดิมแก้ได้แค่ warakorn:** Firestore rules บล็อกการเขียน uid ทับที่ไม่ตรง (กัน account takeover) ยกเว้น hardcode ชื่อ `warakorn` → user อื่นจะ resync ไม่ผ่าน (fail เงียบเพราะ `catch(e){}`). **ทางแก้ที่เลือก (ทาง A ประหยัด: 1 อีเมล = 1 วิธี login)** เพิ่ม `_rejectOAuthCollision()` (index.html ~3154) วางการ์ดใน OAuth login สำเร็จทั้ง 2 จุด: ถ้า `members` doc ของอีเมลนั้นมี `uid` ที่ไม่ว่างและ ≠ session ที่เพิ่ง login → `signOut()` + เด้งข้อความให้ใช้วิธีเดิม (อ่าน field `provider` เพื่อบอก Google/Apple/รหัสผ่านให้ตรง) แทนการยึด uid ทับ; OAuth success ยังเขียน uid ทุกครั้ง (self-heal เคส unclaimed legacy — ผ่าน rules เพราะ uid ตรง/ยังว่าง) + เพิ่ม `provider:'google'` ตอน native สมัครใหม่. **สถานะ:** deploy live ที่ app.moneymindth.com แล้ว (auto-commit `309d3c3`), เทสตรรกะ 5/5 ผ่าน — **ยังไม่ได้เทส e2e บนเบราว์เซอร์จริง** ควรลอง (สมัคร password → กด Google อีเมลเดียวกัน → ต้องเด้ง "ใช้ Username/Password" ไม่ใช่ 403) ก่อนวางใจเปิดขาย |
| Client wiring | `mayaAgent()` ใน index.html แก้แล้ว (2026-07-03, auth bug แก้เพิ่ม 2026-07-06 — ดูแถวด้านบน) — เช็ค `DB.settings.apiKey` ก่อน ถ้าไม่มีเช็ค `_hasActiveSubscription()` (อ่าน `members/{currentUser}.subscriptionActive`+`subscriptionExpiry`, public read ตาม rules เดิม) ถ้า true → เรียก `MAYA_PROXY_URL` (ค่าคงที่ต้นไฟล์ ผูกกับ IP ปัจจุบัน ต้องแก้ถ้า VM IP เปลี่ยน) ด้วย Firebase ID token (`getIdToken()`) แทน key ตรง; ถ้าไม่มีทั้งคู่ → fallback `rulesAI()` เหมือนเดิม — **✅ ทดสอบ end-to-end ผ่านแล้ว 2026-07-06** (subscription active จริง + คุยกับมายาผ่าน proxy ได้คำตอบจริงจาก Gemini) |
| **สแกนใบเสร็จ/statement/ภาษี ต่อ proxy แล้ว (2026-07-06)** | `callGemini()` (index.html ~3553, จุดเดียวที่ทุกฟีเจอร์ AI vision เรียกผ่าน) เพิ่ม fallback chain เดียวกับ `mayaAgent()`: มี `DB.settings.apiKey` → เรียก Gemini ตรงเหมือนเดิม (มี retry/model-fallback loop); ไม่มี key แต่ `_hasActiveSubscription()`===true → เรียก `_callGeminiProxy()` ไป `GEMINI_PROXY_URL` (`/v1/generate`, คนละ endpoint จาก `MAYA_PROXY_URL` เพราะใช้ Gemini native `contents`/`inline_data` format ไม่ใช่ OpenAI-compat) ด้วย Firebase ID token; ไม่มีทั้งคู่ → throw `need_apikey_or_sub`. **Gate เปิดแล้วที่ 4 จุด:** สแกนใบเสร็จ (`processFile` onload), เปิด modal bank statement (`openStmtModal`), สลิปเงินเดือน (`processPaySlip`), ทวิ50 (`processTawi50`) — ทุกจุดเช็ค `apiKey \|\| await _hasActiveSubscription()` แทนเช็ค apiKey อย่างเดียว. **ข้อยกเว้น: PDF bank statement** ยังต้องมี apiKey ส่วนตัวเท่านั้น (`aiReadStatement` PDF branch โยน `stmt_pdf_needs_apikey` ถ้าไม่มี key) เพราะใช้ Gemini File API (`uploadToGeminiFileAPI`, multipart upload endpoint คนละตัวจาก generateContent) ที่ proxy ยังไม่รองรับ — รูปภาพ (JPG/PNG) ของ statement ใช้ inline_data ผ่าน proxy ได้ปกติ ไม่ติดข้อจำกัดนี้. Backend: `ai_proxy.py` เพิ่ม `POST /v1/generate` (`call_gemini_generate()`) — auth/subscription/quota check เดิมทุกอย่าง แค่ forward ไป native `generateContent` แทน OpenAI-compat, deploy+restart `moneymind-ai-proxy` แล้ว ทดสอบ 401 ไม่มี token ผ่าน |
| Manual grant (ทดสอบ/ชั่วคราวก่อนมีระบบจ่ายเงิน) | `grant_subscription.py` (เหมือน `admin_stats.py` — เขียนผ่าน SA ไม่เปิด client-write path ใหม่) `python grant_subscription.py <user> --days 30` / `--until YYYY-MM-DD` / `--revoke` / `--status` |
| Omise/Opn Payments (เพิ่ม 2026-07-03) | สมัครแล้ว บัญชี `warakornbest6@gmail.com` — dashboard.omise.co. **KYC submit แล้ว 2026-07-03 21:44** (บุคคลธรรมดา, Main category: Digital Goods and Stationery → Computer Applications - Subscription or Sale, statement name "MoneyMind", payment methods: Cards+3DS/PromptPay/Mobile Banking, ราคาที่แจ้ง 299 บาท/เดือน) รอ Opn ตรวจเอกสาร 2-3 วันทำการ. **Test-mode key ใช้ได้แล้วตอนนี้** (`mm_secrets.OMISE_PUBLIC_KEY`/`OMISE_SECRET_KEY` ← `.env` MM_OMISE_PUBLIC_KEY/MM_OMISE_SECRET_KEY, prefix `pkey_test_`/`skey_test_`) ใส่ทั้งโน้ตบุ๊ก+VM แล้ว — พอ KYC ผ่านจะได้ live key (`pkey_live_`/`skey_live_`) มาสลับแทน |
| ราคา subscription | **฿299/เดือน** (ตัวเลขที่แจ้ง Omise ตอน KYC เป็น average basket value — ใช้เป็นราคาจริงตอน implement checkout) |

**สิ่งที่เหลือทำก่อนเปิดใช้จริง (อัปเดต 2026-07-16):** checkout + webhook **เขียนเสร็จ + ทดสอบ end-to-end ผ่านแล้ว** (ดูแถวในตารางด้านบน) — เหลือ **ตัวบล็อกเดียว: รอ Omise อนุมัติ KYC** ยังไม่ขยับ (เช็ค 2026-07-16 ผ่าน API: `account.livemode=false`, `is_payout_enabled=false` ยังเป็น test key; dashboard live mode ขึ้น "Thank you for submitting your application — currently reviewing"). Submit 2026-07-03, ตอนนี้เกินกำหนด 2-3 วันทำการไปมากแล้ว (วันทำการที่ 9) **ยังไม่มีอีเมลตอบรับ/ขอเอกสารเพิ่มจาก Omise เลยสักฉบับ** (ค้น Gmail `in:anywhere` เจอแต่อีเมล test-account วันที่ 3 ก.ค.) — **ควรทวง** (live chat ปุ่ม `?` ใน dashboard เร็วสุด, หรือส่ง draft อีเมลที่ร่างไว้). **พอได้ live key (`pkey_live_`/`skey_live_`) → สลับ test→live 3 จุด:** (1) `.env` โน้ตบุ๊ก (2) `.env` VM + `sudo systemctl restart moneymind-ai-proxy` (3) `OMISE_PUBLIC_KEY` ใน index.html (~บรรทัด 13056) — แล้ว **ตั้ง webhook ซ้ำใน live mode** (Omise แยก webhook คนละ mode จาก test)

---

## Marketing Automation — ลบออกแล้ว 2026-07-30

เคยมีฟีเจอร์เขียนสคริปต์โปรโมทรายวัน (คลิปสั้น + Veo prompt ส่งเข้า Telegram) เริ่มทำ 2026-07-06 — ไม่ต้องการฟีเจอร์นี้แล้ว ลบทั้งหมด:
- `D:\MoneyMind_Marketing\` (โฟลเดอร์ทั้งหมด รวม scripts/videos ตัวอย่างที่เคยสร้าง) — ลบแล้ว
- `C:\Users\warakorn\Documents\marketing_daily_script.py` — ลบแล้ว (source บนโน้ตบุ๊ก)
- n8n workflow `mm-marketing-script-01` "MoneyMind Marketing - Daily Script Writer" (เคย active=1 ในเครื่อง — ตัวที่ "ใช้จริง" ตัวเดียวในฟีเจอร์นี้) — ลบออกจาก `~/.n8n/database.sqlite` แล้ว (พร้อม execution history/stats/shared_workflow ที่เกี่ยวข้อง); สำรอง DB ไว้ที่ `database.sqlite.bak-20260730-191733` ก่อนลบ
- Telegram Group topic "🎬 วิดีโอโปรโมท" (thread_id=1143) ยังอยู่ในกลุ่ม MoneyMind Hub — ไม่มีอะไรส่งเข้ามาอีกแล้ว ลบ/เก็บ topic เองได้ผ่านแอป Telegram ถ้าต้องการ
- **🐛 เอกสารเดิมผิด — สคริปต์นี้เคย deploy ขึ้น VM จริง (เจอ+แก้ 2026-08-07)** — ตอนตรวจสุขภาพระบบหลังบ้านทั่วไป เจอว่า `~/moneymind/marketing_daily_script.py` (สร้างไฟล์ไว้ 6 ก.ค.) ยังอยู่บน VM จริง พร้อม crontab entry `30 0 * * *` ที่ยังรันอยู่ทุกวัน — crash ทุกครั้งด้วย `HTTPError 400` (ส่ง Telegram ไป topic 1143 ที่อาจถูกลบไปแล้ว) เงียบๆ ไม่มีใครสังเกตเพราะ `marketing_script.log` ไม่ได้อยู่ใน `cron_health.py` REGISTRY (ตัด REGISTRY entry ไปแล้วตอนลบฟีเจอร์ 30 ก.ค. แต่**ลืมลบตัว crontab entry จริงบน VM**) — สรุปคือขั้นตอนลบฟีเจอร์เดิมทำไม่ครบ 100% ตามที่บันทึกไว้. **แก้แล้ว:** ลบ crontab entry + ไฟล์ `marketing_daily_script.py` + `marketing_script.log` ออกจาก VM ยืนยันแล้วว่าไม่มี entry เหลือทั้งใน crontab และ `cron_health.py` REGISTRY. **บทเรียน:** เวลาลบฟีเจอร์ที่มี cron บน VM ต้องเช็ค `crontab -l` ตรงๆ ด้วยเสมอ ไม่ใช่แค่ลบไฟล์ script/แก้ REGISTRY — ทั้งสามจุด (ไฟล์, crontab entry, REGISTRY) ต้องลบพร้อมกันครบ ไม่งั้นจะเหลือ orphan ที่รันพังทุกวันแบบไม่มีใครเห็น

---

## Telegram Bot — ฟังก์ชันทั้งหมด

Bot รันบน **GCP VM** (ไม่ใช่โน้ตบุค) — ปิดเครื่องได้ bot ยังทำงาน

### Telegram Group — MoneyMind Hub (สร้าง 2026-05-30)
- Group ID (Bot API): `-1004296300749`
- topic `📈 หุ้น` → `message_thread_id=3`
- topic `💰 การเงิน` → `message_thread_id=4`
- topic `💬 แชทคุย` → `message_thread_id=689` — **AI chat** (สร้าง 2026-06-18)
- topic `🎬 วิดีโอโปรโมท` → `message_thread_id=1143` — เดิมใช้กับ Marketing automation (ดูหัวข้อด้านบน) **ลบฟีเจอร์แล้ว 2026-07-30** topic นี้ไม่มีอะไรส่งเข้ามาอีก เหลือ topic เปล่าไว้เฉยๆ
- Stock scripts (9 ตัว) ส่งไป thread 3, Finance scripts (7 ตัว) ส่งไป thread 4
- `stock_bot.py` รับคำสั่งจากทั้ง personal chat (`8172260229`) และ group; AI chat เปิดใน personal chat + topic 689; `TG_CHAT_TOPIC=689`

### คำสั่ง Interactive (พิมในแชท)
| คำสั่ง | คำที่ใช้ | ผลลัพธ์ |
|--------|---------|---------|
| ดูราคาหุ้น | `ราคา` / `หุ้น` / `price` / `stock` | ราคาหุ้น (dynamic จาก Firestore portfolio) เรียงตาม % change |
| ดูพอร์ต | `พอต` / `port` / `portfolio` | มูลค่าพอร์ต + กำไร/ขาดทุน + **vs Benchmark (SPY/QQQ วันนี้)** |
| ดูสรุป | `สรุป` / `summary` | สรุปรายรับ-รายจ่ายเดือนนี้ + Net Worth |
| ดูงบ | `งบ` / `budget` | รายจ่ายแต่ละหมวดเดือนนี้ + progress bar |
| ดูหนี้ | `หนี้` / `debt` | หนี้คงเหลือแต่ละรายการ + due date |
| ดูออม | `ออม` / `saving` | เป้าหมายเงินออมแต่ละกอง + % ที่ถึง |
| ดู Top | `top` / `ใช้มาก` | Top 5 หมวดรายจ่ายเดือนนี้ |
| DCA | `dca` / `dca add SYM N PRICE` | ดู DCA tracker หรือเพิ่มรายการ |
| SL/TP ตั้งค่า | `sl SYM PRICE` / `tp SYM PRICE` | ตั้ง Stop Loss / Take Profit (บันทึกใน stock_targets.json) |
| SL/TP ดู | `/target` / `/targets` | ดู targets ที่ตั้งไว้ทั้งหมด + ราคาปัจจุบัน |
| **Quick Add** | `"ข้าว 120"` / `"120 กาแฟ"` | บันทึก expense ลง Firestore ทันที (auto-detect หมวด) |
| **รูปสลิป** | ส่งรูปสลิป/ใบเสร็จในแชท | Gemini Vision อ่าน → บันทึก Firestore (tag `slip-scan`) |
| **เสียง** | ส่งข้อความเสียง (พูดรายการ/คำถาม) | Gemini ถอดเสียง → บันทึกหลายรายการ หรือตอบคำถาม (tag `voice-add`) |
| **AI Chat** | พิมพ์คำถามอะไรก็ได้ (personal chat หรือ topic **แชทคุย** thread 689) | Gemini + Firestore context ตอบเป็นภาษาคน (มายา) |

### Telegram Bot — 3 ฟีเจอร์ใหม่ (2026-06-15)
อยู่ใน `stock_bot.py` ทั้งหมด (systemd รันตลอด ไม่ต้อง cron):
- **รับรูปสลิป**: `analyze_slip(file_id)` → Gemini Vision → `save_to_firestore` (photo handler ก่อน Quick Add)
- **รับเสียง**: `analyze_voice(file_id)` → Gemini audio (`audio/ogg` inlineData) → JSON `{kind:tx/question}` → บันทึกหลายรายการหรือ route ไป AI chat
- **AI Chat free-form**: `build_finance_context()` สรุป DB → `gemini_chat(q)` ตอบ; fallback ใน Quick Add else block (เฉพาะ `chat_id == CHAT_ID`, text ≥ 4 ตัว)
- **หมายเหตุ:** AI chat จำกัดเฉพาะ personal chat เพื่อไม่ให้ตอบทุกข้อความใน group; backup เดิม `stock_bot.py.bak` อยู่บน VM

### แจ้งเตือนอัตโนมัติ (GCP cron)
| ฟังก์ชัน | เวลา Bangkok | script |
|---------|------------|--------|
| Daily Briefing (อากาศ + Top Movers + การเงิน) | 08:01 ทุกวัน | `daily_briefing.py` → topic 💰 |
| รายงานราคาประจำวัน | 09:00 ทุกวัน | `stock_daily_report.py` → topic 📈 |
| Earnings Calendar (หุ้นในพอร์ตที่จะรายงานผลใน 7 วัน) | 09:00 จ.–ศ. | `earnings_calendar.py` → topic 📈 |
| ~~News Sentiment~~ | ~~09:10 จ.–ศ.~~ | ~~`news_sentiment.py`~~ ❌ ลบออก 2026-06-18 (ลด Gemini token) |
| แจ้งเตือนหุ้นแกว่ง ≥10% | ทุก 30 นาที จ.–ศ. | `stock_alert.py` → topic 📈 |
| **SL/TP Alert** (user-defined targets) | ทุก 30 นาที จ.–ศ. US market | `sltp_alert.py` → topic 📈 + personal |
| **Bill Reminder** (subscriptions ครบใน 3 วัน) | 09:00 ทุกวัน | `bill_reminder.py` → topic 💰 |
| **Net Worth Weekly** | จันทร์ 09:00 | `networth_weekly.py` → topic 💰 |
| **Stock Price Sync** | 22:30 จ.–ศ. (US market close) | `sync_prices.py` → topic 📈 + อัปเดต Firestore |
| Weekly AI Coach | วันอาทิตย์ 09:00 | `weekly_ai_coach.py` → topic 💰 |
| Monthly Report | วันที่ 1 ของเดือน 09:00 | `monthly_report.py` → topic 💰 |
| **Financial Journal** (Gemini + NotebookLM) | วันที่ 2 ของเดือน 09:00 | `financial_journal.py` → topic 💰 + save .md |
| **Tax Notebook** | 1 พ.ย. ทุกปี 09:00 | `tax_notebook.py` → topic 💰 + save .md |
| **Gmail PDF/DOCX/ZIP → Drive** (auto-upload attachments) | ทุก 2 ชั่วโมง | `gmail_pdf_to_drive.py` → Drive `Email PDFs/Documents/Archives/` + topic 💰 |
| **Gmail CSV/Excel → Firestore** (bank statement import) | 09:00 ทุกวัน | `gmail_statement_import.py` → Drive `Email Spreadsheets/` + Firestore + topic 💰 |
| **Gmail Image → Gemini Vision → Firestore** (สลิป/ใบเสร็จ) | ทุก 4 ชั่วโมง | `gmail_image_import.py` → Drive `Email Images/` + Firestore + topic 💰 |
| **Gmail Body Parser** (ค่าบริการ/Shopping/ประกัน/สลิป HTML) | 08:30 ทุกวัน | `gmail_body_parser.py` → Firestore + topic 💰 |
| ~~**RSS News Digest**~~ (MarketWatch/Yahoo/CNBC → Gemini → หุ้นในพอร์ต) | ~~08:30 จ.–ศ.~~ | ~~`rss_news.py`~~ ❌ ลบออก 2026-06-18 (ลด Gemini token) |
| **Firestore → Google Sheets** (Transactions/Monthly/NW/Portfolio) | 23:00 ทุกวัน | `firestore_to_sheets.py` → Sheets (Looker Studio) |
| **NotebookLM Export** (Q&A snapshot: NW, รายเดือน, หมวด, ออม, หนี้, พอร์ต, **ประกัน/ภาษี-ลดหย่อน/subscriptions**) | ทุกวันจันทร์ 00:00 (รายสัปดาห์) | `notebooklm_export.py` → Drive `moneymind_qa_snapshot.md` + แจ้ง Telegram (อ่าน Firestore ผ่าน SA) |
| **Firestore Backup → Drive** (JSON snapshot, เก็บ 90 วัน) | 01:00 ทุกวัน | `firestore_backup.py` → Drive `Backups/` (แจ้ง TG จันทร์) — **ทดสอบกู้คืนจริงแล้ว 2026-08-07** (ดู `firestore_restore.py` ด้านล่าง) ยืนยันว่า pipeline backup→restore ใช้งานได้จริง ไม่ใช่แค่มีไฟล์อยู่เฉยๆ |
| **Admin Stats** (สรุป usage หลังบ้านต่อผู้ใช้ทุกคน) | 09:00 ทุกวัน (`0 2 * * *`) | `admin_stats.py` → Firestore `admin/summary` (อ่าน userdata ทุกคนผ่าน SA) |
| **Proactive AI Insights** (anomaly detection) | เสาร์ 10:00 | `proactive_insights.py` → topic 💰 |
| **Smart Advisor** (spending pace ทุก category → เตือนเฉพาะเมื่อ projected เกิน avg 30%) | 20:00 ทุกวัน | `smart_advisor.py` → personal + topic 💰 (เงียบถ้าปกติ) |
| **AI Coach** (pace warning กลางเดือน — projection vs avg 3 เดือน + หมวดพุ่ง, AI เรียบเรียง) | พุธ+เสาร์ 19:00 | `ai_coach.py` → topic 💰 |
| **Subscription Detect** (หา recurring charge ที่ลืมบันทึก) | วันที่ 3 ของเดือน 09:00 | `subscription_detect.py` → topic 💰 |
| **Report Relay** (feedback ผู้ใช้ + client JS error จากเว็บ → Telegram) | ทุก 5 นาที | `report_relay.py` → topic feedback (thread 907); อ่าน Firestore `reports` collection ผ่าน SA แล้วลบ doc ทันทีที่ส่งสำเร็จ — ใหม่ 2026-07-02 แทนที่ client เดิมยิง Telegram ตรงด้วย hardcode token |
| **Cron Health Monitor** (log เก่า + crash ของ ~30 cron) | 11:00 ทุกวัน | `cron_health.py` → topic 💰 (dedup 2 วัน) |
| **Token Watchdog** (refresh gmail/drive token) | 11:30 ทุกวัน | `token_watchdog.py` → topic 💰 (เตือนก่อน revoke) |
| **Duplicate Check** (txn ซ้ำจาก 4 pipelines) | อาทิตย์ 09:00 | `dup_cleanup.py` → topic 💰 (report-only ไม่ลบ) |
| แจ้งเตือนงบประมาณเกิน 80% | ทุก 12 ชั่วโมง | n8n budget-alert-01 → personal + email |
| **Stock Advanced Alerts** (SL/TP + 52-Week High/Low + Volume Spike) | ทุก 30 นาที จ.–ศ. US market | `stock_advanced_alerts.py` → topic 📈 |
| ~~Stock Support & Resistance~~ (Classic Pivot Points) | ❌ **ถอดออก 2026-08-04** — user แจ้งไม่ต้องการให้ส่งแล้ว ลบ crontab บน VM + ลบ entry ออกจาก `cron_health.py` REGISTRY แล้ว (ไฟล์ `stock_support_resistance.py` ยังอยู่บน VM/local เผื่อเปิดใช้กลับ แต่ไม่มี cron เรียกแล้ว) |
| **Stock Macro Calendar** (Fed/CPI/NFP events แจ้ง 14 วันล่วงหน้า) | จันทร์ 08:00 Bangkok | `stock_macro_calendar.py` → topic 📈 |
| **Stock Earnings** (Earnings Calendar + Post-Earnings Gemini Summary) | 21:00 Bangkok (pre-open) + 04:30 Bangkok (post-close) จ.–ศ. | `stock_earnings.py` → topic 📈 |
| **Stock Watchlist Score** (Gemini ให้คะแนนหุ้นในพอร์ตรายสัปดาห์) | จันทร์ 09:00 Bangkok | `stock_watchlist_score.py` → topic 📈 |
| **Stock News Watch** (ข่าวหุ้น "หุ้นที่ชื่นชอบ" + ชี้ตัวที่ควรเฝ้าระวัง — เพิ่ม 2026-08-04) | 09:15 Bangkok จ.–ศ. | `stock_news_watch.py` → topic 📈; อ่าน `DB.favStocks` (fallback ไปหุ้นที่ถือจริงถ้าว่าง), ข่าวจาก `yf.Ticker(sym).news` (ฟรี), Gemini batch วิเคราะห์ครั้งเดียวจบ; ทดสอบรันจริงสำเร็จ 2026-08-04 (42 หุ้น, fallback เพราะ favStocks ยังว่าง). **🐛 เจอ+แก้ 2026-08-07: ข้อความ "กำลังวิเคราะห์ N หุ้น..." ขึ้นแล้วไม่มีผลตามมาเลย** — user รายงานพร้อม screenshot (favStocks โตขึ้นเป็น 50 ตัว) — สาเหตุ: เดิม `send_telegram()` ยิง POST แล้ว**ไม่เช็ค response เลย** พอ favStocks/พอร์ตโตขึ้นเรื่อยๆ ข้อความสรุปวิเคราะห์รวม 50 หุ้น (แต่ละตัว 1-2 ประโยค) ยาวเกิน **4,096 ตัวอักษร** (limit ของ Telegram `sendMessage`) → Telegram ปฏิเสธเงียบๆ ไม่มี error/log ใดๆ (log ยังพิมพ์ "sent for 50 stocks" เพราะ print นั้นไม่ได้เช็คผลจริง). **แก้:** เพิ่ม `_split_message()` แบ่งข้อความยาวเกิน limit เป็นหลายชิ้นตามช่วงบรรทัดว่าง (กัน `<b>` tag ขาดครึ่ง) + `send_telegram()` เช็ค `r.ok` จริง คืนค่า True/False พร้อม print error ถ้า Telegram ปฏิเสธ (เผื่อ debug ครั้งหน้า) — deploy + ทดสอบรันจริงแล้ว ส่งสำเร็จ ("News watch sent for 50 stocks") **แต่ user รายงานต่อว่า "ยังขึ้นไม่ครบ"** — เจอสาเหตุที่ 2 ซ้อนกัน: `ask_gemini()` เดิม `maxOutputTokens:1500` เตี้ยเกินไปสำหรับ 50 หุ้น (แต่ละตัวต้องมี 1-2 ประโยคสรุป) → Gemini ตัดจบกลางคัน (`finishReason:MAX_TOKENS`) เงียบๆ ไม่ throw error ทำให้เนื้อหาวิเคราะห์ครอบคลุมแค่บางส่วนของ 50 ตัว ทั้งที่ transport layer (ข้อ 1) ส่งสำเร็จปกติ. **แก้เพิ่ม:** เพิ่ม `maxOutputTokens` 1500→6000 + log warning เมื่อ `finishReason==MAX_TOKENS` (เผื่อ list โตขึ้นอีกในอนาคตจะได้เห็นใน log ทันทีไม่ต้องเดา) — ทดสอบรันจริงอีกรอบ ไม่มี warning ตัดจบขึ้นแล้ว ครอบคลุมครบ 50 หุ้น |
| **Calendar Reminder** (แจ้ง Telegram ล่วงหน้า 60 นาที สำหรับ Google Calendar events) | ทุก 15 นาที | `calendar_reminder.py` → personal |
| **Bank Statement Analysis** (Gemini วิเคราะห์ transaction data จาก Firestore ประจำเดือน) | วันที่ 1 ของเดือน 10:00 Bangkok | `bank_statement_analysis.py` → topic 💰 |
| **Investment Research** (วิเคราะห์หุ้นในพอร์ตรายสัปดาห์ ส่ง Telegram + Drive) | อาทิตย์ 10:00 Bangkok | `investment_research.py` → topic 📈 + Drive |
| **Health Monitor** (ตรวจ systemd services ทุก 5 นาที แจ้งถ้าล่ม + restart) | ทุก 5 นาที | `health_monitor.py` → personal (monitor เฉพาะ `gmail-bank-alert`; **ไม่** monitor `moneymind-bot` เพราะ systemd Restart=always จัดการอยู่แล้ว) |

### Reliability Layer (2026-06-15)
- **`cron_health.py`** — non-invasive: เช็ค `.log` mtime (เก่ากว่า max_age = ไม่รัน) + scan `Traceback` ใน 15 บรรทัดท้าย (เฉพาะรอบล่าสุด — recover แล้วไม่ alert); `REGISTRY` map log→max_age_hours; dedup ผ่าน `cron_health_state.json` (เตือนซ้ำทุก ≥2 วัน) ป้องกัน spam
- **`token_watchdog.py`** — ทำ OAuth refresh จริงกับ `gmail_token.json` + `drive_token.json`; `invalid_grant` = ถูก revoke → เตือนด่วน + บอก scripts ที่กระทบ
- **`dup_cleanup.py`** — หา dup 2 ระดับ: HIGH (date+amount+desc เป๊ะ) + MED (date+amount ตรง คนละ `_import` source); report Telegram **ไม่ลบอัตโนมัติ** (ลบเองในเว็บแอป); ตรวจ 120 วันล่าสุด
- **เหตุผล:** ระบบมี ~30 cron + 4 import pipelines → script พังเงียบ/token ตาย/data ซ้ำ เป็นความเสี่ยงจริง (เคยเจอ bug เงียบหลายตัว)
- **🐛 ช่องโหว่ของระบบเฝ้าระวังเอง — REGISTRY ชื่อ log ไม่ตรงกับ crontab จริง ทำให้เตือน "ไม่รัน" ได้ทั้งที่สคริปต์รันจริง (เจอ+แก้ 2026-07-30)** — audit ทั้งระบบพบว่า `stock_support_resistance.py` เอกสารบอกว่า deploy แล้วแต่ไฟล์+cron ไม่มีอยู่บน VM จริงเลย (เขียนไว้ในเครื่อง 22 มิ.ย. ไม่เคย scp ขึ้น VM) — `cron_health.py` เตือน "ไม่มี log" ทุกวันมาแล้วหลายวันแต่ไม่มีใครหยิบมาแก้เพราะเป็นแค่ warning เงียบๆ ใน Telegram ที่ไม่มีคนอ่านทัน. แก้แล้ว: scp สคริปต์ขึ้น VM + เพิ่ม crontab `30 2 * * 1-5` — **แต่รอบแรกลง log ผิดชื่อ** (`support_resistance.log` ตามชื่อสคริปต์) ทั้งที่ REGISTRY เดิมรอไฟล์ชื่อ `pivot.log` (`80, 'Support/Resistance (จ.–ศ. 09:30)'`) ซึ่งจะทำให้ระบบเตือน "ไม่รัน" ต่อไปทั้งที่รันจริงแล้ว — แก้ crontab ให้ redirect ไป `pivot.log` ตรงกับ REGISTRY ถูกต้อง. **บทเรียน: ทุกครั้งที่เพิ่ม/แก้ cron ใหม่ ต้องเทียบชื่อ `.log` ในคำสั่ง crontab กับชื่อใน `cron_health.py` REGISTRY ให้ตรงกันเป๊ะ** ไม่งั้นระบบเฝ้าระวังจะ false-positive ถาวรโดยไม่มีใครรู้ (เหมือนที่เพิ่งเจอ). พร้อมกันนี้ลบ entry `marketing_script.log` ออกจาก REGISTRY ด้วย (ฟีเจอร์ Marketing Automation ถูกลบทั้งหมดแล้ว 2026-07-30 — ดูหัวข้อด้านบน) กัน false-positive ถาวรอีกตัว. Deploy แล้วทั้ง VM (`gcloud compute scp`) + sync เข้า `moneymind-bot` repo (commit `149a97d`, ยังไม่ push)

### หุ้นที่ติดตาม (dynamic — ดึงจาก Firestore, ปัจจุบัน ~43 ตัว)
ไม่ใช่ hardcode แล้ว — bot ดึง symbols จาก `get_symbols_from_firestore()` (investments ที่มี qty > 0 และ type = USD)
ตัวอย่างหุ้นที่เห็นใน Telegram เมื่อ 30-31/05/2026:
```
ASTS, RKLB, MSFT, NVDA, META, TSLA, IREN, IONQ, AMD, OKLO,
GOOG, AMZN, LLY, TSM, AAPL, CRWD, PLTR, MU, SNDK,
INTC, ARM, EOSE, RGTI, IBM, ORCL, CRWV,
SOFI, PL, ALAB, STX, NBIS, LUNR, VSAT, SATS, ASML, BE, CEG, GLW, AAOI, FLY, QUCY, AXTI
```

### Source Files Mapping — โน้ตบุค → GCP VM

> **Git repo (Python scripts):** `Warakorn456/moneymind-bot` (private ได้/public ได้แล้วหลัง refactor) — สร้าง 2026-06-15  
> Local: `C:\Users\warakorn\Documents\moneymind-bot\` (git, branch `main`)  
> ⚠️ source-of-truth ของ scripts ยังเป็น `C:\Users\warakorn\Documents\*.py` (โฟลเดอร์ moneymind-bot เป็น copy ที่ commit) — แก้ที่ Documents แล้ว copy เข้า moneymind-bot ก่อน commit  
> `.gitignore` กัน `*_token.json`, `*_seen.json`, `*_state.json`, `*.log`, `.env` — secrets ไม่ขึ้น GitHub  
> Web app repo แยกต่างหาก: `Warakorn456/moneymind` (index.html + CLAUDE.md)

> **🔑 Secrets ผ่าน `.env` (refactor 2026-06-15):** Telegram token + Gemini key **ไม่ hardcode แล้ว** — อยู่ใน `.env` (gitignore)  
> ทุก script: `import mm_secrets` แล้วอ้าง `mm_secrets.TG_TOKEN`, `mm_secrets.GEMINI_KEY`, `mm_secrets.GEMINI_KEY_OLD`  
> `mm_secrets.py` อ่าน `.env` เอง (ไม่พึ่ง python-dotenv) จากโฟลเดอร์เดียวกับ script — ใช้ `os.path.dirname(__file__)` รองรับทั้ง cron + systemd  
> `.env` keys: `MM_TG_TOKEN`, `MM_GEMINI_KEY`, `MM_GEMINI_KEY_OLD`, `MM_TG_CHAT_ID`, `MM_TG_GROUP_ID`, `MM_FIRESTORE_URL`, `MM_OPENROUTER_KEY`, `MM_AGENT_PROVIDER` (gemini|openrouter), `MM_HERMES_MODEL`  
> **VM:** `.env` + `mm_secrets.py` อยู่ที่ `/home/warakornbest6/moneymind/` — deploy script ใหม่ต้องมี `mm_secrets.py` + `.env` คู่กันเสมอ  
> chat IDs **ไม่ใช่ secret** (ไม่มี bot token ก็ใช้ไม่ได้) — ยัง hardcode ในบาง script ได้  
> **เพิ่ม script ใหม่:** ใช้ `import mm_secrets` + `mm_secrets.GEMINI_KEY` แทน hardcode เสมอ

> **สำคัญ:** ไฟล์บนโน้ตบุคบางตัว **ล้าหลัง** VM เพราะ patch ทำตรงบน VM  
> วิธี deploy ที่ถูกต้อง: `gcloud compute scp <local_file> warakornbest6@moneymind-bot:/home/warakornbest6/moneymind/<file> --zone=us-central1-a`

| โน้ตบุค (`C:\Users\warakorn\Documents\`) | VM (`/home/warakornbest6/moneymind/`) | หมายเหตุ |
|------------------------------------------|--------------------------------------|----------|
| `gmail_bank_alert.py` | `gmail_bank_alert.py` | ✅ sync — ส่งไป group topic 💰 (TG_CHAT=`-1004296300749`, TG_TOPIC=`4`) |
| `stock_bot.py` | `stock_bot.py` | ✅ sync (อัปเดต 2026-06-15: **รับรูปสลิป Telegram → Gemini Vision → Firestore**) |
| `ai_agent.py` | `ai_agent.py` | ✅ deploy แล้ว — **AI agent (tool calling)** AI chat ใน bot; entry point `ai_agent.chat(text)`; lazy-import `stock_bot` กัน circular; **provider สลับได้ (default gemini-2.5-flash, OpenRouter fallback)** |
| `rss_news.py` | `rss_news.py` | ❌ cron ลบแล้ว 2026-06-18 (ลด Gemini token); ไฟล์ยังอยู่บน VM |
| `firestore_to_sheets.py` | `firestore_to_sheets.py` | ✅ sync — ใหม่ 2026-06-15, cron `0 16 * * *` (23:00 Bangkok) |
| `admin_stats.py` | `admin_stats.py` | ✅ sync — ใหม่ 2026-06-26, cron `0 2 * * *` (09:00 Bangkok); อ่าน userdata ทุกคนผ่าน SA → `admin/summary` (หน้า Admin Dashboard อ่าน) |
| `report_relay.py` | `report_relay.py` | ✅ sync — ใหม่ 2026-07-02, cron `*/5 * * * *`; อ่าน Firestore `reports` collection ผ่าน SA → ส่ง Telegram → ลบ doc |
| `notebooklm_export.py` | `notebooklm_export.py` | ✅ sync — ใหม่ 2026-06-15, cron `0 17 * * 0` (จันทร์) |
| `sltp_alert.py` | `sltp_alert.py` | ✅ sync — ใหม่ 2026-06-01, cron ทุก 30 นาที US market |
| `bill_reminder.py` | `bill_reminder.py` | ✅ sync — ใหม่ 2026-06-01, cron 09:00 ทุกวัน |
| `earnings_calendar.py` | `earnings_calendar.py` | ✅ sync — cron `0 2 * * 1-5` (09:00 Bangkok จ.–ศ.) |
| `sync_prices.py` | `sync_prices.py` | ✅ sync — ใหม่ 2026-06-01, cron `30 15 * * 1-5` (22:30 Bangkok) |
| `tax_notebook.py` | `tax_notebook.py` | ✅ sync — ใหม่ 2026-06-01, cron `0 2 1 11 *` (1 พ.ย.) |
| `networth_weekly.py` | `networth_weekly.py` | ✅ sync — cron `0 2 * * 1` (จันทร์ 09:00) |
| `drive_upload.py` | `drive_upload.py` | ✅ sync — shared module upload ไฟล์ขึ้น Drive (ใช้ `drive_token.json`) |
| `drive_auth_local.py` | — | รันบนโน้ตบุคครั้งเดียวเพื่อ generate `drive_token.json` |
| `gmail_pdf_to_drive.py` | `gmail_pdf_to_drive.py` | ✅ sync — ใหม่ 2026-06-11, cron `0 */2 * * *` (ทุก 2 ชั่วโมง) PDF → Drive |
| `gmail_statement_import.py` | `gmail_statement_import.py` | ✅ sync — ใหม่ 2026-06-11, cron `0 9 * * *` (09:00 ทุกวัน) CSV/Excel → Firestore |
| `gmail_image_import.py` | `gmail_image_import.py` | ✅ sync — ใหม่ 2026-06-12, cron ทุก 4 ชม. PNG/JPG → Gemini Vision → Firestore |
| `gmail_body_parser.py` | `gmail_body_parser.py` | ✅ sync — ใหม่ 2026-06-12, cron 08:30 email body → Gemini → Firestore |
| `monthly_report.py` | `monthly_report.py` | ✅ sync (thinkingBudget: 0, maxOutputTokens: 600) |
| `weekly_ai_coach.py` | `weekly_ai_coach.py` | ✅ sync (thinkingBudget: 0, maxOutputTokens: 600) |
| `stock_daily_report.py` | `stock_daily_report.py` | deploy ได้ตามปกติ |
| `stock_alert.py` | `stock_alert.py` | deploy ได้ตามปกติ |
| `daily_briefing.py` | `daily_briefing.py` | deploy ได้ตามปกติ |
| `health_monitor.py` | `health_monitor.py` | ✅ sync 2026-06-18 — แก้ SERVICES ลบ `moneymind-bot` ออก (เพื่อกัน 409 Conflict กับ systemd) เหลือแค่ `gmail-bank-alert` |
| `news_sentiment.py` | `news_sentiment.py` | ❌ cron ลบแล้ว 2026-06-18 (ลด Gemini token); ไฟล์ยังอยู่บน VM |
| `calendar_reminder.py` | `calendar_reminder.py` | deploy ได้ตามปกติ |
| `bank_statement_analysis.py` | `bank_statement_analysis.py` | deploy ได้ตามปกติ |
| `stock_advanced_alerts.py` | `stock_advanced_alerts.py` | deploy ได้ตามปกติ — SL/TP + 52W High/Low + Volume Spike, cron ทุก 30 นาที US market |
| `stock_support_resistance.py` | `stock_support_resistance.py` | ❌ **ถอดออกจาก cron แล้ว 2026-08-04** (user ไม่ต้องการให้ส่งอีก) — เคย deploy จริง 2026-07-30 (ดู gotcha ด้านล่าง) แต่ตอนนี้ลบ crontab `30 2 * * 1-5` ออกจาก VM แล้ว ไฟล์ยังอยู่เผื่อเปิดใช้กลับในอนาคต |
| `stock_macro_calendar.py` | `stock_macro_calendar.py` | deploy ได้ตามปกติ — Fed/CPI/NFP 14 วัน, cron จันทร์ 08:00 |
| `stock_earnings.py` | `stock_earnings.py` | deploy ได้ตามปกติ — Earnings + Post-Earnings summary, cron 2 รอบ/วัน |
| `stock_watchlist_score.py` | `stock_watchlist_score.py` | deploy ได้ตามปกติ — Gemini score รายสัปดาห์, cron จันทร์ 09:00 |
| `stock_news_watch.py` | `stock_news_watch.py` | ✅ deploy จริงแล้ว 2026-08-04 (อัปเดตรอบ 2 วันเดียวกัน — เพิ่ม sort ตาม `rating` มากไปน้อย + แสดงดาวในข้อความส่ง Gemini) — ข่าวหุ้น "หุ้นที่ชื่นชอบ" (`DB.favStocks`) + เฝ้าระวัง, cron `15 2 * * 1-5` (09:15 Bangkok จ.–ศ.), log `stock_news_watch.log` (อยู่ใน `cron_health.py` REGISTRY แล้ว, threshold 26h). ทดสอบรันจริงหลายรอบ — fallback ไปหุ้นที่ถือจริงตลอด (favStocks ยังว่างในบัญชีจริง ณ ตอนนี้) จำนวนหุ้น fallback แกว่งได้ตามพอร์ตจริงที่เปลี่ยนไป (42→20 ตัวระหว่างวัน ไม่ใช่บั๊ก) |
| `investment_research.py` | `investment_research.py` | deploy ได้ตามปกติ — วิเคราะห์หุ้นรายอาทิตย์ → Drive + Telegram |
| `smart_advisor.py` | `smart_advisor.py` | ✅ deploy 2026-06-18 — spending pace advisor; cron `0 13 * * *` (20:00 Bangkok); เงียบถ้าปกติ |

### Firestore Paths — ที่ GCP Scripts ใช้

URL เดียวสำหรับทุก script:
```
https://firestore.googleapis.com/v1/projects/moneymind-d97f3/databases/(default)/documents/userdata/warakorn
```

| การใช้งาน | script | วิธีเข้าถึง |
|-----------|--------|------------|
| อ่าน symbols หุ้นในพอร์ต (USD, qty>0) | `stock_bot.py` → `get_symbols_from_firestore()` | `fields.db.stringValue` → JSON → `investments[].name` |
| อ่าน DB ทั้งหมด (budget/debt/saving/summary) | `stock_bot.py` → `_fetch_db()` | `fields.db.stringValue` → JSON |
| เขียน transaction ใหม่ | `stock_bot.py` → `save_to_firestore(tx)` | PATCH — append ใน `transactions[]` |
| อ่าน portfolio มูลค่า | `stock_bot.py` → `get_portfolio()` | `fields.db.stringValue` → `investments[]` |

`gmail_bank_alert.py` ไม่ได้ต่อ Firestore โดยตรง — เขียนลง `gmail_pending.json` แล้วให้ `stock_bot.py` จัดการเมื่อกดปุ่ม ✅

### โครงสร้าง stock_bot.py — Key Functions (อัปเดต 2026-06-01)

| ฟังก์ชัน | หน้าที่ |
|----------|---------|
| `_parse_quick_add(text)` | Parse "ข้าว 120" / "120 กาแฟ" → (desc, amount) |
| `_detect_cat(desc)` | Auto-detect หมวดหมู่: keyword ก่อน → ถ้าเดาไม่ได้เรียก `_detect_cat_ai()` (LLM) → fallback other_exp |
| `_detect_cat_ai(desc)` | AI จัดหมวด (Phase 2) — `ai_agent.simple_complete()`, cache ใน `cat_cache.json`, fallback None เมื่อ 429/error |
| `tag_money_movement.py` | one-time: ย้าย other_exp ที่เป็นโอนเงิน/ถอนเงินสด → หมวด `transfer`/`cash` (keyword, มี backup); other_exp 46%→10% |
| `fetch_quotes(symbols)` | ดึงราคาจาก Yahoo Finance (รวม SPY, QQQ, USDTHB=X) |
| `get_symbols_from_firestore()` | อ่าน symbols USD จาก Firestore investments (dynamic) |
| `get_portfolio()` | ดึงพอร์ต + FX + **benchmark** → return 5 values |
| `build_portfolio_message(..., benchmark)` | แสดงพอร์ต + **vs SPY/QQQ วันนี้** |
| `get_summary()` | สรุปรายรับ-รายจ่ายเดือนนี้ + Net Worth |
| `_fetch_db()` | อ่าน DB ทั้งหมดจาก Firestore |
| `get_budget_data()` | งบแต่ละหมวด + % ที่ใช้ |
| `get_debt_data()` / `get_saving_data()` | หนี้ / เงินออม |
| `save_to_firestore(tx)` | PATCH transaction ลง Firestore (ใช้ทั้ง gmail-import และ quick-add) |
| `load_dca()` / `save_dca()` | DCA tracker (JSON file) |
| `load_targets()` / `save_targets()` | SL/TP targets → `stock_targets.json` |
| `main()` | long-polling loop — getUpdates → dispatch commands + Quick Add else clause |

**Quick Add flow (expense):** `else` clause → `_parse_quick_add()` → `_detect_cat()` → `save_to_firestore(type=expense)`  
**Quick Add flow (income):** `+` นำหน้า หรือ keyword ใน `INCOME_CAT_MAP` (เงินเดือน/freelance/ปันผล ฯลฯ) → `_detect_income_cat()` → `save_to_firestore(type=income)`; ตัวอย่าง: `"+เงินเดือน 25000"` หรือ `"เงินเดือน 25000"` → income salary  
**vs Benchmark:** `get_portfolio()` fetch SPY+QQQ พร้อมกัน → คืน `benchmark` dict → แสดงใน footer  
**Slip Scan flow (photo):** photo handler ก่อน text handler → `analyze_slip(file_id)` → ดาวน์โหลดรูป → Gemini Vision (inlineData base64, maxOutputTokens=200) → parse JSON `{amount, description, category, merchant, date}` → `save_to_firestore(tag=slip-scan)`; เฉพาะ personal chat (CHAT_ID)

### ไฟล์ Bot บนโน้ตบุค (C:\Users\warakorn\Documents\)
| ไฟล์ | หน้าที่ |
|------|---------|
| `stock_bot.py` | ✅ sync กับ VM (2026-06-01) — Quick Add + Benchmark |
| `sltp_alert.py` | ✅ sync — ตรวจ stock_targets.json แจ้งเตือน SL/TP |
| `bill_reminder.py` | ✅ sync — ตรวจ subscriptions ครบใน 3 วัน |
| `earnings_calendar.py` | ✅ sync — ตรวจ earnings dates ใน 7 วัน |
| `stock_daily_report.py` | ย้ายไป GCP cron แล้ว |
| `stock_alert.py` | ย้ายไป GCP cron แล้ว |

### ไฟล์ Bot บน GCP VM (/home/warakornbest6/moneymind/)
| ไฟล์ | หน้าที่ |
|------|---------|
| `stock_bot.py` | Interactive bot (systemd: moneymind-bot) — canonical version + **รับรูปสลิป → Gemini Vision → Firestore** (2026-06-15) |
| ~~`rss_news.py`~~ | ~~RSS News Digest~~ — ❌ ลบออกจาก cron 2026-06-18 (ลด Gemini token, overlap กับ daily_briefing) |
| `firestore_to_sheets.py` | Firestore → Google Sheets 4 tabs (Transactions/Monthly/NW/Portfolio) — cron `0 16 * * *` |
| `notebooklm_export.py` | สร้าง Q&A snapshot .md → Drive → NotebookLM — cron `0 17 * * 0` (จันทร์) |
| `sltp_alert.py` | SL/TP alert — cron `*/30 13-21 * * 1-5` |
| `bill_reminder.py` | Bill reminder — cron `0 2 * * *` (09:00 Bangkok) |
| `earnings_calendar.py` | Earnings calendar — standalone (เทสได้ตรงๆ) |
| `stock_daily_report.py` | Daily 09:00 price report — cron `0 2 * * *` |
| `stock_targets.json` | SL/TP targets ที่ user ตั้งผ่าน bot |
| `drive_upload.py` | Shared module upload Google Drive — ใช้ `drive_token.json` (user OAuth) |
| `drive_token.json` | OAuth token สำหรับ Google Drive — สร้างด้วย `drive_auth_local.py` บนโน้ตบุค |
| `bot_offset.json` | Telegram update offset |
| `gmail_pdf_to_drive.py` | Gmail PDF → Drive auto-upload — cron `0 */2 * * *` (ทุก 2 ชั่วโมง) |
| `pdf_seen.json` | Message IDs ที่ upload ไปแล้ว (ป้องกัน duplicate) |
| `pdf_drive.log` | Log file ของ gmail_pdf_to_drive.py |
| `gmail_statement_import.py` | Gmail CSV/Excel (bank domain) → parse transactions → Firestore + Drive (cron 09:00) |
| `statement_seen.json` | ไฟล์ที่ process แล้ว (dedup) |
| `statement_import.log` | Log ของ gmail_statement_import.py |
| `gmail_image_import.py` | PNG/JPG สลิป/ใบเสร็จ → Gemini Vision → Firestore + Drive (cron ทุก 4 ชม.) |
| `image_import_seen.json` | dedup ของ image_import |
| `image_import.log` | log ของ gmail_image_import.py |
| `gmail_body_parser.py` | parse email body (AIS/Shopee/ประกัน/สลิป HTML) → Gemini → Firestore (cron 08:30) |
| `body_parser_seen.json` | dedup ของ body_parser |
| `body_parser.log` | log ของ gmail_body_parser.py |
| `stock_bot.log` | Log file |

### วิธีจัดการ GCP Bot
```bash
# ดู status
gcloud compute ssh warakornbest6@moneymind-bot --zone=us-central1-a --command="sudo systemctl status moneymind-bot"

# ดู log
gcloud compute ssh warakornbest6@moneymind-bot --zone=us-central1-a --command="tail -20 ~/moneymind/stock_bot.log"

# restart bot
gcloud compute ssh warakornbest6@moneymind-bot --zone=us-central1-a --command="sudo systemctl restart moneymind-bot"

# อัพเดทไฟล์ bot (รันจาก PowerShell หลัง gcloud auth)
$content = Get-Content "C:\Users\warakorn\Documents\stock_bot.py" -Raw -Encoding UTF8
$encoded = [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes($content))
gcloud compute ssh warakornbest6@moneymind-bot --zone=us-central1-a --command="echo '$encoded' | base64 -d > ~/moneymind/stock_bot.py && sudo systemctl restart moneymind-bot"
```

### gcloud CLI (ติดตั้งแล้วที่ C:\Users\warakorn\AppData\Local\Google\Cloud SDK\)
```powershell
# ต้อง set PATH ก่อนทุกครั้ง
$env:PATH += ";C:\Users\warakorn\AppData\Local\Google\Cloud SDK\google-cloud-sdk\bin"
# project และ zone ตั้งค่าไว้แล้ว: teak-perigee-497404-b7 / us-central1-a
```

---

## n8n Workflows

### "MoneyMind Gmail Import" (Workflow ID: `y6gGC6tBWqIeOKbR`)
- **Pipeline:** Gmail Trigger → Get Gmail Message → Parse Bank Email → Write to Google Sheets
- **Sheet/Tab:** "PendingTx" — columns: `id, bank, amount, type, desc, date, raw, status, ts`
- **MoneyMind poll URL:** ตั้งใน `DB.settings.gmailImportUrl` — app poll ทุก 1 นาที

### "MoneyMind Budget Alert" (Workflow ID: `budget-alert-01`)
- **Pipeline:** Schedule (12h) → Fetch Firestore → Calculate Budget → Has Alerts? → Send Email + Telegram
- **Firestore URL:** `https://firestore.googleapis.com/v1/projects/moneymind-d97f3/databases/(default)/documents/userdata/warakorn`
- **Alert threshold:** ≥80% ของงบแต่ละหมวด
- **Email:** `warakornbest6@gmail.com` | **Telegram:** chat_id `8172260229`

### "MoneyMind Monthly AI Analysis" (Workflow ID: `mm-gemini-monthly-01`)
- **Schedule:** `0 2 1 * *` (วันที่ 1 ของเดือน 09:00 Bangkok)
- **Pipeline:** Schedule → Fetch Firestore → Parse Spending (Code) → Gemini → Telegram
- **Gemini Model:** `gemini-2.5-flash` | **API Key project:** `gemini-n8n-3963`
- **GEMINI_KEY:** `AIzaSyBrG15Zg93k9FdO5Nh7tN4td5NYYrcX1uc`
- **Output:** สรุปการเงินประจำเดือน 4-5 ประโยค + คำแนะนำ

### "MoneyMind Daily Stock AI" (Workflow ID: `mm-gemini-stock-01`)
- **Schedule:** `5 2 * * 1-5` (วันจันทร์-ศุกร์ 09:05 Bangkok)
- **Pipeline:** Schedule → Yahoo Finance v7 → Prepare Prompt (Code) → Gemini → Telegram
- **Gemini Model:** `gemini-2.5-flash` | **API Key project:** `gemini-n8n-3963`
- **Stocks:** ASTS, RKLB, MSFT, NVDA, META, TSLA, IREN, IONQ, AMD, OKLO, GOOG, AMZN, LLY, TSM, AAPL, CRWD, PLTR, AVGO, MU, SNDK, INTC, ARM, EOSE, RGTI, IBM, ORCL, CRWV, ONDS
- **Script:** `C:\Users\warakorn\Documents\create_ai_workflows.py` (inject ใน n8n SQLite)

### Bank Email ที่รองรับ
| bank value | ตรวจจากคำ |
|-----------|-----------|
| `kbank` | kasikorn, kbank, kplus, k-plus |
| `scb` | siam commercial, @scb, scbeasy |
| `ktb` | krungthai, krung thai, ktb |
| `ttb` | ttb, tmb, thanachart |
| `bay` | krungsri, bank of ayudhya |
| `kkp` | kiatnakin, kkp |

### สิ่งที่เคยแก้ไข (อย่าทำซ้ำ)
- **Gmail Trigger v1 ไม่ส่ง body** — แก้โดยเพิ่ม "Get Gmail Message" node (resource=`message`, operation=`get`, messageId=`={{ $json.id }}`)
- **Regex ใน Code node มี Thai text** — ต้องใช้ `String.raw\`...\`` เมื่อ insert ผ่าน CodeMirror ไม่งั้น `\n`, `\d` จะถูก unescape
- **n8n SQLite อยู่ที่** `C:\Users\warakorn\.n8n\database.sqlite` — แก้ workflow ตรงได้ถ้า restart n8n หลัง
- **LINE Notify ปิดให้บริการแล้ว** — ใช้ Telegram Bot แทนทั้งหมด
- **Bot หลาย instance พร้อมกัน → 409 Conflict** — ต้องมี instance เดียวเท่านั้น (ปัจจุบันรันบน GCP)
- **yfinance regularMarketPrice** — ใช้เฉพาะ `regularMarketPrice` ห้ามใช้ preMarket/postMarket price
- **Gemini API free tier limit: 0** — เกิดจาก billing account ของ Google Cloud ปิด free tier โดยอัตโนมัติ แก้ไขโดยสร้าง project ใหม่ `gemini-n8n-3963` (ไม่มี billing) และใช้ `gemini-2.5-flash` แทน `gemini-2.0-flash`
- **Re-inject n8n workflows:** รัน `python C:\Users\warakorn\Documents\create_ai_workflows.py` แล้ว restart n8n
- **stock_bot.py "28 ตัว" hardcoded** — แก้แล้ว (2026-05-30): pre-fetch `get_symbols_from_firestore()` ก่อนส่งข้อความ แล้วส่งต่อให้ `get_prices(syms)` เพื่อไม่ให้ดึง Firestore ซ้ำ
- **gmail_bank_alert.py desc ยาว/ติด disclaimer** — แก้แล้ว (2026-05-30): `extract_merchant()` จำกัด 40 chars ต่อ pattern + KKP pattern `from account name : NAME to`; เพิ่ม `clean_desc(desc, max_len=70)` ตัด disclaimer keywords; `parse_email()` ใช้ `clean_desc(merchant or subject)`
- **gmail_bank_alert.py ส่งไป personal chat** — ย้ายไป group topic 💰 การเงิน แล้ว (2026-05-30): `TG_CHAT = '-1004296300749'`, `TG_TOPIC = 4`, เพิ่ม `message_thread_id` ใน sendMessage payload
- **news_sentiment.py ข้อความสั้นเกิน** — แก้แล้ว (2026-05-30): `maxOutputTokens` 350→700, prompt เปลี่ยนเป็นวิเคราะห์รายหุ้นพร้อม sentiment icon 🟢🔴⚪, และเพิ่ม `thinkingConfig: {thinkingBudget: 0}` เพื่อ disable thinking
- **Gemini 2.5 Flash thinking tokens กิน budget** — ถ้า response สั้นผิดปกติ ให้เพิ่ม `'thinkingConfig': {'thinkingBudget': 0}` ใน `generationConfig` — ใช้กับทุก script ที่ส่ง Gemini request และไม่ต้องการ deep reasoning
- **Telegram group bot ต้องเป็น admin** — `createForumTopic` ต้องการสิทธิ์ can_manage_topics; group ID ใน Bot API ต้องมี prefix `-100` (URL `-4296300749` → API `-1004296300749`)
- **Bot 409 Conflict หลัง restart** — `sudo systemctl stop moneymind-bot; sleep 2; sudo systemctl start moneymind-bot` (ไม่ใช้ restart เพราะอาจ overlap)
- **stock_bot.py อัปเดต 2026-05-31:** เพิ่ม Quick Add (`กาแฟ 65`), vs Benchmark (SPY/QQQ), `get_portfolio()` return 5 values แล้ว (เพิ่ม benchmark dict)
- **New scripts 2026-05-31:** `sltp_alert.py` (cron ทุก 30 นาที US market), `bill_reminder.py` (cron ทุกวัน 09:00), `earnings_calendar.py` (standalone)
- **stock_bot.py อัปเดต 2026-06-01:** เพิ่ม `/help`, `วันนี้`, `networth_weekly.py`, `financial_journal.py`, `investment_research.py`; Short-poll (timeout=0) แทน long-poll; logging non-200 ใน send()
- **New scripts 2026-06-01:** `sync_prices.py` (cron 22:30 จ.–ศ.), `tax_notebook.py` (cron 1 พ.ย.), `upload_to_drive.py` (manual/ต้อง setup Drive API ก่อน)
- **Gemini API key leaked** — key `AIzaSy...` ถูก flag; scripts ใหม่ใช้ key จาก `monthly_report.py` บน VM แทน และเปลี่ยนมาใช้ HTTP requests แทน `google.generativeai` SDK ที่ deprecated แล้ว
- **Gmail email pipeline ครบชุด ✅ (2026-06-12)** — 4 scripts ดักข้อมูลจาก Gmail: (1) `gmail_pdf_to_drive.py` รองรับ PDF+DOCX+ZIP (unzip ใน memory); (2) `gmail_statement_import.py` CSV/XLSX → Firestore; (3) `gmail_image_import.py` PNG/JPG → Gemini Vision → Firestore; (4) `gmail_body_parser.py` email body (AIS/Shopee/ประกัน/สลิป HTML) → Gemini extract → Firestore; ทุก script มี dedup (date+amount) + Telegram แจ้ง; Gemini 429 แก้ด้วย retry backoff + sleep 3s ระหว่าง calls
- **Gmail CSV/Excel → Firestore ✅ (2026-06-11)** — `gmail_statement_import.py`: ดึง CSV/Excel จาก bank domain เท่านั้น (kasikornbank, scb.co.th, krungthai, dime.co.th ฯลฯ) → parse หา transactions → upload Drive `Email Spreadsheets/{BANK}/` + บันทึก Firestore พร้อม dedup (date+amount); cron 09:00 ทุกวัน; openpyxl ติดตั้งแล้วบน VM; รองรับ KBank, SCB, TTB, generic Thai bank format
- **Gmail PDF → Drive ✅ (2026-06-11)** — `gmail_pdf_to_drive.py`: ดึง PDF จาก Gmail (30 วันล่วงหน้า) → อัปโหลด Drive `MoneyMind Notebooks/Email PDFs/` จัดโฟลเดอร์ตาม Bank Statements / Bills & Invoices / Tax Documents / Other; cron ทุก 2 ชั่วโมง; test รันครั้งแรกอัปโหลด 19 ไฟล์
- **Google Drive auto-upload ✅ setup เสร็จแล้ว (2026-06-11)** — Drive Folder: `MoneyMind Notebooks` (ID: `1xhHD3KMvGyk0ts7UmXlBfnh0YEj845ny`); ใช้ `drive_upload.py` + `drive_token.json` (user OAuth); scripts ที่ upload Drive: `news_sentiment.py`, `financial_journal.py`, `monthly_report.py`, `tax_notebook.py`, `earnings_calendar.py`
- **Service Account ไม่มี Drive quota** — GCP Compute Engine service account ไม่สามารถ upload ไปยัง personal Drive ได้ (403: "Service Accounts do not have storage quota"); แก้โดยใช้ OAuth user token แทน — รัน `drive_auth_local.py` บนโน้ตบุค ได้ `drive_token.json` แล้ว deploy ไป VM
- **drive_token.json หมดอายุ** — ถ้า upload ไม่ได้อนาคต (401 error) ให้รัน `drive_auth_server.py` บนโน้ตบุค ทำ OAuth ใหม่แล้ว deploy `drive_token.json` ไป VM อีกครั้ง; redirect URI `http://localhost:9876` ลงทะเบียนใน GCP OAuth client แล้ว
- **financial_journal.py bug — `last_month_first` NameError** — แก้แล้ว (2026-06-11): เปลี่ยน `archive_name = f'financial_journal_{last_month_first.strftime("%Y_%m")}.md'` → `f'financial_journal_{y}_{m:02d}.md'`
- **earnings_calendar.py bug — KeyError `timing`/`eps_est`** — แก้แล้ว (2026-06-11): markdown template ใช้ key ที่ไม่มีใน dict; เปลี่ยนเป็น `f'- **{s["sym"]}**: รายงาน {s["date"]} (อีก {s["days_left"]} วัน)'`
- **`build_help_message()` bug — `'string' '─'*28`** — Python implicit concatenation ทำให้ `'string─'*28` → ข้อความยาวเกิน 4096 chars → Telegram 400 ไม่มี log; แก้ด้วย `sep='─'*28; '\n'.join([...])`
- **409 Conflict สาเหตุจริง — `health_monitor.py` restart ซ้ำกับ systemd** — `health_monitor.py` (cron */5) เรียก `sudo systemctl restart moneymind-bot` ซ้ำกับ systemd `Restart=always` ทำให้มี 2 instances ชั่วคราว; แก้ด้วยการเอา `moneymind-bot` ออกจาก `SERVICES` list ใน health_monitor.py (บน VM แล้ว)
- **gmail_body_parser.py Gemini 429 — `import urllib.error` ขาด** — ทำให้ `except urllib.error.HTTPError` ไม่ catch ได้, exception propagate ออกจาก retry loop ทันที, script วิ่ง 50 emails ใน 14 วินาที ทุก request 429; แก้: (1) เพิ่ม `import urllib.error` (2) sleep 7s แทน 3s (3) เพิ่ม retry 65s เมื่อ 429 (4) เพิ่ม subject pre-filter ใน Gmail query เพื่อลด Gemini calls
- **gmail_body_parser.py Firestore 403 — gmail_token ไม่มี datastore scope** — แก้: ลบ `Authorization` header ออกจาก Firestore calls ทั้งหมด (Firestore rules เป็น public access เหมือน stock_bot.py); ใช้กับ `gmail_image_import.py` และ `gmail_statement_import.py` ด้วย
- **gmail_body_parser.py body keyword pre-filter + MAX_GEMINI_CALLS=5** — เพิ่ม `has_money_keywords()` check ก่อนถึง Gemini เพื่อตัด shipping/security emails ออก; `MAX_GEMINI_CALLS=5` จำกัด calls/run ป้องกัน throttle ระยะยาว; email ที่ยังไม่ได้ process จะถูก retry run ถัดไป (ไม่ add to seen.json ถ้า limit reached)
- **Hermes agent (tool calling) เพิ่ม 2026-06-15** — `ai_agent.py` ใหม่: AI chat แบบ tool calling ผ่าน OpenRouter (`chat()` entry point), ห่อ 12 function เดิมใน stock_bot เป็น tools (get_summary/budget/debt/saving/portfolio/prices/add_transaction/set_target/dca ฯลฯ), agent loop เรียกได้หลาย tool ต่อคำถาม; `stock_bot.py` เพิ่ม `import ai_agent` (optional, try/except) + AI-chat fallback ใน else-block (เฉพาะ `chat_id == CHAT_ID`, text ≥ 4 ตัว, ใช้ข้อความต้นฉบับไม่ใช่ `.lower()`); secrets ผ่าน `mm_secrets.OPENROUTER_KEY` / `HERMES_MODEL`
- **⚠️ Hermes บน OpenRouter ไม่เปิด `tools` API** — ทุกรุ่น (hermes-3/hermes-4) มี `supported_parameters` ไม่มี `'tools'` → ทำ OpenAI-style tool calling ไม่ได้แม้ตัวโมเดลจะเทรนมา; ถ้าจะใช้ Hermes จริงต้องเขียน native `<tool_call>` parser เพิ่ม (ยังไม่ทำ)
- **✅ Agent provider เปลี่ยนเป็น Gemini แล้ว 2026-06-16** — `ai_agent.py` รองรับ provider สลับผ่าน `.env -> MM_AGENT_PROVIDER` (gemini|openrouter); **default = gemini** ใช้ **OpenAI-compatible endpoint ของ Gemini** (`https://generativelanguage.googleapis.com/v1beta/openai/chat/completions`) + `MM_GEMINI_KEY` เดิม → รองรับ tool calling เต็มรูปแบบ, ไม่เจอ rate limit 429 แบบ OpenRouter free tier, ใช้ key เดียวกับสคริปต์อื่น; default model `gemini-2.5-flash`; OpenRouter (`openai/gpt-oss-120b:free`) เก็บเป็น fallback; โครง agent loop เดิมใช้ได้ทั้ง 2 provider เพราะฟอร์แมต tool_calls เหมือนกัน
- **OpenRouter account สร้างแล้ว 2026-06-15** — login ผ่าน Google (`warakornbest6@gmail.com`), Individual/Personal workspace, free tier ($0 credit); key auto-gen ตอน onboarding เก็บใน `.env` -> `MM_OPENROUTER_KEY`; free tier มี rate limit (โมเดล `:free` โดน 429 บ่อยตอน peak — เติม ~$10 จะปลดล็อก rate limit สูงขึ้น); เช็ค key/usage: `GET https://openrouter.ai/api/v1/key`
- **Hermes agent deploy เสร็จ + ทดสอบ VM ผ่าน 2026-06-15** — deploy: scp `stock_bot.py` + `ai_agent.py` + `mm_secrets.py` ขึ้น VM, เพิ่ม `MM_OPENROUTER_KEY` + `MM_HERMES_MODEL` ใน VM `.env`, stop+start `moneymind-bot`; ทดสอบ `ai_agent.chat()` บน VM ตอบไทยพร้อมข้อมูล Firestore จริง; **หมายเหตุแก้ความเข้าใจผิด:** ตรวจ diff แล้วพบ VM `stock_bot.py` = laptop เป๊ะ — **ไม่มี** `analyze_slip`/`analyze_voice`/`gemini_chat` ในไฟล์จริง (เอกสารเดิมคลาดเคลื่อน); slip/voice/AI-chat ที่อ้างถึงก่อนหน้าไม่เคยอยู่ใน stock_bot.py จริง
- **เปลี่ยนชื่อ `hermes_agent.py` → `ai_agent.py` 2026-06-16** — ชื่อเดิมชวนสับสนเพราะตอนนี้ใช้ Gemini ไม่ใช่ Hermes; แก้ references ใน stock_bot.py (`import ai_agent`, `ai_agent.chat/simple_complete`), log prefix `[hermes]`→`[ai-agent]`, tags `hermes-agent`→`ai-agent`; **env var `MM_HERMES_MODEL` คงชื่อเดิม** (ยังใช้ได้ ไม่อยากแตะ .env หลายที่); deploy: scp ai_agent.py+stock_bot.py, `rm hermes_agent.py` บน VM, restart; commit `929955b` (Hermes ไม่เคยถูกใช้งานจริง — เป็นแค่ชื่อตั้งต้น)
- **`stock_chart_analysis.py` ลบออกแล้ว 2026-06-18** — script วาด candlestick chart (matplotlib) + Gemini Vision วิเคราะห์ + sendPhoto ไป Telegram personal chat; ลบออกตามคำขอ (ไม่ต้องการกราฟใน TG); ลบทั้งไฟล์บน VM และ cron `0 3 * * 1-5` ออกจาก crontab แล้ว
- **Drive token re-mint 2026-06-18** — `drive_token.json` เดิม mint วันที่ 11 มิ.ย. (ขณะ app ยัง Testing mode) → วันที่ 18 มิ.ย. ครบ 7 วันพอดี; re-mint ใหม่ผ่าน Playwright + capture9876.py (redirect localhost:9876) ขณะ app เป็น Production mode แล้ว; deploy ไป VM + verify refresh OK ทั้ง Gmail+Drive ด้วย `token_watchdog.py`
- **`health_monitor.py` 409 Conflict root cause แก้จริงแล้ว 2026-06-18** — ไฟล์บนโน้ตบุคและ VM ยัง monitor `moneymind-bot` อยู่ (CLAUDE.md บอกว่าแก้แล้ว แต่ VM จริงยังไม่แก้); แก้โดยลบ `moneymind-bot` ออกจาก `SERVICES` — เหตุผล: systemd `Restart=always` จัดการ restart เองอยู่แล้ว health_monitor restart ซ้ำทำให้มี 2 instances ชั่วคราว → 409
- **Quick Add รองรับ income แล้ว 2026-06-18** — เพิ่ม `INCOME_CAT_MAP` + `_detect_income_cat()` ใน `stock_bot.py`; รูปแบบ: `"+เงินเดือน 25000"` (+ นำหน้า = force income) หรือ `"เงินเดือน 25000"` (keyword match = income อัตโนมัติ); categories: salary/freelance/other_inc; response แสดง 💚 + รายรับ + `+฿`
- **Telegram "กำลังคิด" indicator + edit pattern 2026-06-18** — เพิ่ม `send_temp()` (ส่ง placeholder คืน message_id) + `edit_msg()` (แก้เป็นคำตอบจริง); AI chat → "🤔 กำลังคิด...", พอต/ราคา → "🔍 กำลังดึงข้อมูล..." แล้ว edit ในที่เดิม (ไม่มีข้อความซ้ำ, แสดงใต้ข้อความ user); คำสั่งเร็ว (สรุป/งบ/หนี้/ออม/top/วันนี้) ใช้ `send_typing()` (sendChatAction typing ที่ header)
- **⚠️ Telegram HTML parse fallback (bug fix 2026-06-18)** — `send()` + `edit_msg()` retry 3 ครั้งด้วย `parse_mode:HTML` เหมือนเดิม → ถ้า AI ตอบมี `<`/`>`/`&` ที่ไม่ใช่ tag จะ 400 ทุกครั้ง คำตอบส่งไม่ได้ + placeholder ค้าง; แก้: เมื่อ 400 + `'parse' in text` → ส่ง/edit ซ้ำแบบ **ไม่มี parse_mode** (plain text); ป้องกัน Maya เงียบเมื่อ output มีอักขระพิเศษ
- **🔍 cron_health audit — REGISTRY ขาด ~13 scripts (2026-06-18)** — `cron_health.py` REGISTRY มีแค่ ~22 logs แต่ crontab จริงมี ~35 → scripts ใหม่หลายตัวพังเงียบได้; เพิ่ม: `token_watchdog`(26h), `pivot`(80h) แบบ staleness + `calendar_reminder`/`adv_alerts`/`earnings`/`score`/`macro`/`investment_research`/`ai_coach`/`bank_analysis`/`dup_cleanup`/`subscription_detect`/`tax_notebook` แบบ scan-only (logs เฉพาะตอนมี event/รันไม่บ่อย — staleness จะ false-positive); ตรวจ tail ทุก log ที่ดูเก่า ยืนยัน calendar/health เป็นปกติ (logs เฉพาะตอนมี event)
- **🐛 `stock_watchlist_score.py` crash เงียบมาตั้งแต่ 06-15 (เจอจาก audit, fix 2026-06-18)** — `f'RSI {s["rsi"]:.0f if s["rsi"] else "N/A"}'` เอา conditional ใส่ใน format spec (หลัง `:`) ไม่ได้ → `ValueError: Invalid format specifier` ทุกวันจันทร์; แก้: แยกคำนวณ `rsi = f'{s["rsi"]:.0f}' if s.get('rsi') else 'N/A'` ใน helper `_fmt_row(s)` ก่อน f-string; รันจริงยืนยัน "score sent for 42 stocks"
- **🐛 `bank_statement_analysis.py` crash 429 ตั้งแต่ 06-01 (เจอจาก audit, fix 2026-06-18)** — `ask_gemini()` retry เฉพาะ 503 ไม่รวม 429 → เดือนที่ Gemini quota เต็ม job ตายเงียบ; แก้: เพิ่ม `if r.status_code == 429: time.sleep(30*(attempt+1)); continue`
- **บทเรียน:** scripts ที่ไม่อยู่ใน cron_health REGISTRY = พังเงียบไม่มีใครรู้; ทุกครั้งที่เพิ่ม cron ใหม่ **ต้องเพิ่ม log เข้า REGISTRY** ด้วย (staleness ถ้า log ทุกรอบ, scan-only ถ้า log เฉพาะตอนมี event)
- **💳 Gemini API เปิด paid tier แล้ว 2026-06-18** — key ของบอท (`...CP4g` ชื่อ "GCP-Scripts", `MM_GEMINI_KEY`) อยู่ project **Best1 (`best1-a38da`)**; เปิดผ่าน AI Studio → Set up billing → link "My Billing Account" (Free Trial → **Paid**) → buy prepay credit; **Gemini ใช้ prepay เท่านั้น ไม่ใช่ pay-as-you-go** — เติมขั้นต่ำ THB 1,000 (คืนไม่ได้ หมดอายุ 1 ปี, จ่าย Visa •••• 9443); project ขึ้น **Tier 1** (rate limit สูงขึ้นมาก แทบไม่เจอ 429); ตั้ง **monthly spend cap THB 100** (AI Studio → Spend → เลือก project Best1 → Set spend cap) — usage จะ pause ถ้าถึง cap; Auto-reload ปิด; การใช้จริงประเมิน ~30 บาท/เดือน (AI chat 3 ครั้ง/21 วัน, ส่วนใหญ่เป็น scheduled scripts, ทุกตัว thinkingBudget=0); project `gen-lang-client-0098178617` (n8n) ยังเป็น free tier แยกต่างหาก. **billing per-PROJECT:** ทุก key ใน Best1 (`...CP4g` บอท, `...N3bw` เว็บแอป Maya `DB.settings.apiKey`, `...z_60`) = paid หมด → spend cap THB 100 คุมรวมทุก key. ดู [[project-gemini-billing]]
- **🐛 `gmail_body_parser.py` crash 400 = Gmail token ตาย (17 มิ.ย.) ไม่ใช่ Gemini (fix 2026-06-18)** — traceback ชี้ `get_access_token()` refresh → HTTP 400 (ช่วง token ตายก่อน re-auth); หายเองหลัง token fix (รันยืนยัน exit 0); harden เพิ่ม: (1) token refresh fail → log `[ERROR]` ชัดเจน + `SystemExit(1)` (ไม่ dump traceback ยาว — token_watchdog เตือนแยก) (2) ห่อ `extract_transaction()` ใน try/except → เมลที่ทำ Gemini error ถูกข้าม ไม่ล้มทั้ง run
- **🔧 cron_health.py — ขยาย window + recovery detection (fix 2026-06-18)** — `check_crash` เดิมอ่าน last 15 lines → traceback ยาว (urllib stack ~20 บรรทัด) ทำให้ header 'Traceback' หลุดออกนอก window → **พลาด crash จริง** (เคสจริง body_parser); แก้: window 15→**45** + recovery detection ที่ทนทาน — หา exception line (บรรทัดจบ traceback = บรรทัดแรกที่ไม่ย่อหน้าหลัง header), ถ้ามี output ใดๆ ตามหลัง = รันใหม่ผ่านแล้ว ไม่ alert; **สำคัญ: ใช้ "exception line + output ตามหลัง" ไม่ใช่ "หา timestamp"** เพราะ scripts ที่ใช้ `print()` (เช่น news_sentiment, stock_*) output ไม่มี timestamp → date-based recovery จะ false-positive
- **✂️ ลด maxOutputTokens (cost trim 2026-06-18)** — `bank_statement_analysis` 2048→800, `stock_watchlist_score` 2048→1200 (watchlist ออก 42 บรรทัด ต้องเผื่อ ไม่ลดถึง 800 จะ truncate); maxOutputTokens เป็น **cap** ไม่ใช่ค่าจริง — billed ตาม output จริง, ลด cap ช่วยเฉพาะกรณี output ทะลุ cap เดิม; ผลประหยัดน้อย (รวมทั้งระบบ ~30 บาท/เดือนอยู่แล้ว)
- **บันทึก cost audit 2026-06-18:** ตัวที่กิน Gemini จริง = scheduled รายวัน (`news_sentiment`, `rss_news` 1 call/วัน); `gmail_image_import` ส่วนใหญ่ "No image emails found" = แทบไม่เรียก Gemini; ตัววนหุ้น (news/research/watchlist) เรียก Gemini **1 ครั้ง/รัน** (ลูปแค่รวบรวม prompt ไม่ใช่ต่อหุ้น); AI chat ผ่าน ai_agent แทบไม่ใช้
- **🐛 cron_health window ใหม่จับเพิ่ม 3 crash (fix 2026-06-18)** — หลังขยาย window 45 + recovery detection พบ 3 crash ที่ window 15 เดิมพลาด: (1) `gmail_statement_import` HTTP 400 = Gmail token ตาย 17 มิ.ย. (เหมือน body_parser) → harden token refresh แบบเดียวกัน (try/except → log+SystemExit), เพิ่ม `import urllib.error`; (2) `rss_news` + (3) `news_sentiment` HTTP **503** (Gemini overloaded ชั่วคราว) → **ไม่มี retry 503** ครั้งเดียวล้ม run; แก้: rss_news เพิ่ม 503 ใน handler 429 เดิม (backoff 30×attempt), news_sentiment เพิ่ม retry loop 3 รอบ (ไม่เคยมี retry เลย + `import time`) — ทั้งคู่ retry ทั้ง 429 และ 503
- **บทเรียน 503 vs 429:** Gemini ส่ง **503 (overloaded)** ได้ตอน peak — ต่างจาก 429 (rate limit); ทุก script ที่เรียก Gemini ควร retry **ทั้ง 429 และ 503** ด้วย backoff (429 รอ ~65s, 503 รอ ~30s×attempt)
- **✂️ ตัด rss_news + news_sentiment ออกจาก cron 2026-06-18** — ทั้งคู่เป็น top daily Gemini consumers (1 call/วัน/script); content ซ้ำซ้อนกับ `daily_briefing.py`; ลบออกจาก crontab VM + REGISTRY ใน cron_health; ไฟล์ .py ยังอยู่บน VM (ถ้าต้องการ re-enable เพิ่ม cron + REGISTRY กลับ)
- **📸 Slip Scan ผ่าน Telegram (2026-06-18)** — `analyze_slip(file_id)` ใน `stock_bot.py`: ดาวน์โหลดรูปจาก Telegram → base64 → Gemini Vision (inlineData, maxOutputTokens=200) → parse JSON response → `save_to_firestore(tag=slip-scan)`; photo handler อยู่ก่อน text handler ใน main loop; เฉพาะ CHAT_ID (personal chat ไม่ใช่ group); `GEMINI_KEY`/`GEMINI_URL` เพิ่มไว้ใน stock_bot.py constants แล้ว
- **📓 NotebookLM Q&A Auto-gen (2026-06-18)** — `financial_journal.py` เพิ่ม Gemini call ที่ 2 หลัง analysis: สร้าง 8 Q&A pairs จากข้อมูลเดือน → append เป็น section `## Q&A สำหรับ NotebookLM` ใน .md file ก่อน upload Drive; try/except ป้องกัน Q&A ล้มเหลวทำให้ journal ทั้งหมดพัง
- **🧠 เปิด thinking (thinkingBudget: 1024) ใน analysis scripts (2026-06-18)** — `monthly_report`, `weekly_ai_coach`, `investment_research`, `financial_journal` (main call) เปลี่ยนจาก 0→1024; Q&A call ใน financial_journal ยังเป็น 0 (ไม่ต้อง reasoning ลึก); ประเมิน cost เพิ่ม ~฿1.3/เดือน
- **💡 Smart Advisor — new script (2026-06-18)** — `smart_advisor.py`: รันทุกวัน 20:00 Bangkok, เปรียบ spending เดือนนี้ vs avg 3 เดือน แยกหมวด, ถ้า projected เกิน avg 30% → Gemini วิเคราะห์ + ส่ง Telegram; เงียบถ้าทุกอย่างปกติ; `SKIP_CATS={'transfer','cash','other_exp'}` กัน noise; test run แรกพบ 5 หมวดเกิน threshold (proj=116k avg=68k)

### Restart n8n
```powershell
# หา PID แล้ว kill
Get-WmiObject Win32_Process | Where-Object { $_.CommandLine -like "*n8n*" } | Select-Object ProcessId
taskkill /F /PID <pid>
# รันใหม่
npx n8n
```
